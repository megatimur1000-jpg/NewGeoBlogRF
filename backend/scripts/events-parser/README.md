# Events Parser

Парсер событий для заполнения календаря событиями в городах.

## Описание

Этот скрипт генерирует и добавляет события в базу данных для городов из конфигурации. Для каждого города создается по 5 событий разных категорий с полными названиями и реалистичными датами.

## Возможности

- 🏙️ Парсинг событий для конкретного города
- 📊 Отслеживание прогресса парсинга
- 📋 Просмотр списка всех городов
- 🔄 Сброс прогресса
- ✨ **ТОЛЬКО реальные уникальные события** - парсер создает события ТОЛЬКО из базы реальных событий
- 🔍 **Поиск всех реальных событий** - опция для просмотра всех городов с реальными событиями
- ⏭️ **Пропуск городов без событий** - города без реальных событий автоматически пропускаются
- 📅 Уникальные даты для каждого события (без дублирования)
- 📍 Автоматическое геокодирование координат для каждого события
- 🏙️ Полные названия событий с указанием города
- 🔤 Сортировка городов по алфавиту

## Установка

```bash
cd backend/scripts/events-parser
npm install
```

## Настройка API ключа Yandex Maps

Для работы геокодирования адресов необходимо указать API ключ Yandex Maps. Скрипт автоматически ищет ключ в следующих переменных окружения (в порядке приоритета):

1. `VITE_YANDEX_MAPS_API_KEY`
2. `YANDEX_MAPS_API_KEY`
3. `YANDEX_API_KEY`
4. `YANDEX_GEOCODER_API_KEY`

### Где разместить ключ:

Создайте файл `.env` в одном из следующих мест (скрипт проверит все варианты):

1. **В корне проекта** (`D:\Best_Site\.env`):
   ```
   VITE_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
   ```

2. **В папке backend** (`D:\Best_Site\backend\.env`):
   ```
   VITE_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
   ```

3. **В папке events-parser** (`D:\Best_Site\backend\scripts\events-parser\.env`):
   ```
   VITE_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
   ```

4. **В config.env** (`D:\Best_Site\backend\scripts\wayatom-parser\config.env`):
   ```
   VITE_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
   ```

**Важно:** Если ключ не найден, скрипт будет пропускать геокодирование и события будут созданы без координат.

## Использование

```bash
npm start
```

или

```bash
node index.js
```

## Структура событий

Каждое событие содержит:
- **title** - полное название события с указанием города (например: "Культурный фестиваль в Москве")
- **description** - подробное описание события (например: "Культурный фестиваль в Москве с участием местных и приглашенных артистов...")
- **start_datetime** - дата и время начала (реалистичное время: 10:00-17:00, предпочтение выходным дням)
- **end_datetime** - дата и время окончания (длительность события: 2-6 часов)
- **location** - местоположение (город, область)
- **latitude/longitude** - координаты, полученные автоматически через геокодирование адреса (Yandex Geocoder API)
- **category** - категория события
- **hashtags** - минимальные теги
- **is_public** - публичность (true)
- **creator_id** - ID создателя (1 - системный)
- **is_user_modified** - изменено пользователем (false)
- **used_in_blogs** - используется в блогах (false)

## Категории событий

1. **Фестиваль** - культурные фестивали и праздники
2. **Концерт** - музыкальные выступления
3. **Выставка** - художественные и исторические экспозиции
4. **Спортивное событие** - соревнования и турниры
5. **Ярмарка** - торговые мероприятия

## Файлы

- `index.js` - основной файл парсера
- `parsers/eventParser.js` - логика парсинга событий
- `parsers/uniqueEvents.js` - база РЕАЛЬНЫХ уникальных событий для городов России
- `services/databaseService.js` - работа с базой данных
- `progress/events-progress.json` - файл прогресса (создается автоматически)

## Зависимости

- inquirer - интерактивные меню
- chalk - цветной вывод в консоль
- axios - HTTP запросы
- PostgreSQL - база данных

## Примечания

- **ТОЛЬКО реальные события**: Парсер создает события ТОЛЬКО из базы реальных событий `uniqueEvents.js`
- **Нет фейковых событий**: Города без реальных событий автоматически пропускаются (не создаются выдуманные события)
- **Поиск реальных событий**: Используйте опцию "Показать все города с реальными событиями" для просмотра доступных городов
- **Уникальные даты**: Каждое событие получает уникальную дату (без дублирования в один день)
- **Автоматическое геокодирование**: Координаты получаются автоматически через Yandex Geocoder API для каждого события
- **Фиксированные данные**: В статической базе событий можно указать фиксированные даты и координаты через поля `start_datetime`, `end_datetime`, `latitude`, `longitude`
- Даты событий распределены на ближайшие 6 месяцев (если не указаны фиксированные)
- Предпочтение отдается выходным дням (суббота, воскресенье) для большей реалистичности
- Время начала событий: 10:00-17:00 (если не указано фиксированное)
- Длительность событий: 2-6 часов (случайная, если не указана)
- Города отсортированы по алфавиту
- Прогресс сохраняется в JSON файле

## Обновление координат существующих событий

Если события были запарсены до добавления логики геокодирования, их координаты могут быть NULL. Для обновления координат существующих событий используйте скрипт:

```bash
# Windows
update-coordinates.bat

# Или напрямую через Node.js
node updateEventCoordinates.js
```

Скрипт найдет все события без координат и попытается геокодировать их адреса через Yandex Geocoder API.

## Проверка прогресса парсинга

Для проверки статистики по обработанным городам используйте:

```bash
# Windows
check-cities.bat

# Или напрямую через Node.js
node check-cities.js
```

Скрипт покажет:
- Общее количество городов в конфигурации
- Количество обработанных городов
- Статистику по регионам
- Список регионов без обработанных событий
- Статистику по приоритетам городов

## Парсинг всех городов (включая районные центры)

Парсер теперь поддерживает два режима работы:

1. **Парсинг только городов с уникальными событиями** - парсит только те города, для которых есть уникальные события в базе `uniqueEvents.js`

2. **Парсинг всех городов из конфигурации** - парсит ВСЕ города из конфигурации, включая районные центры. Для городов без уникальных событий генерируются события по шаблонам (Фестиваль, Концерт, Выставка, Спортивное событие, Ярмарка).

Это позволяет заполнить событиями все регионы России, что повышает чувство "полноты" проекта и мотивирует пользователей исправлять и дополнять события.

**Важно:** События, сгенерированные по шаблонам, помечаются как публичные и могут быть отредактированы пользователями для повышения геймификации.

## Как работает парсер

1. Парсер проверяет, есть ли РЕАЛЬНЫЕ события для города в базе `uniqueEvents.js`
2. Если есть - используются они (например, "День огурца в Суздале", "Шоу фейерверков в Костроме", "День семьи в Муроме")
3. Если нет - город пропускается (НЕ создаются фейковые события)
4. Каждое событие получает уникальную дату и координаты

## Примеры уникальных событий

- **Астрахань**: Фестиваль бардовской песни, День рыбака
- **Суздаль**: День огурца, Фестиваль русской бани
- **Кострома**: Шоу фейерверков, Фестиваль "Снегурочка"
- **Муром**: День семьи, любви и верности, Фестиваль русской кухни
- **Москва**: Фестиваль "Московская осень", Московский марафон
- **Санкт-Петербург**: Фестиваль "Белые ночи", Регата на Неве
- И многие другие...

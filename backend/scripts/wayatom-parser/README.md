# 🗺️ WayAtom Parser

**Парсер для автоматического заполнения карты WayAtom метками из OpenStreetMap и Wikipedia**

## 📋 Описание

WayAtom Parser - это мощный инструмент для автоматического сбора и добавления точек интереса (POI) на карту WayAtom. Парсер использует данные из OpenStreetMap через Overpass API и дополняет их информацией из Wikipedia для создания богатой базы маркеров.

## 🚀 Возможности

- **Автоматический парсинг городов** - пошаговое заполнение карты метками
- **Множественные категории** - достопримечательности, музеи, рестораны, отели, парки
- **Интеграция с Wikipedia** - автоматическое дополнение описаний
- **Система прогресса** - отслеживание обработанных городов
- **Валидация данных** - проверка корректности координат и данных
- **Батчевая обработка** - эффективная работа с большими объемами данных

## 🏗️ Архитектура

```
wayatom-parser/
├── index.js                 # Главный файл запуска
├── master-parser.js         # Мастер-парсер
├── config/                  # Конфигурационные файлы
│   ├── cities.js           # Список городов для парсинга
│   ├── categories.js       # Категории POI
│   └── database.js         # Настройки БД
├── services/               # Сервисы
│   ├── databaseService.js  # Работа с БД
│   ├── overpassService.js  # Overpass API
│   └── wikipediaService.js # Wikipedia API
├── parsers/               # Парсеры
│   └── cityParser.js      # Парсер городов
├── utils/                 # Утилиты
│   ├── logger.js          # Логирование
│   └── validator.js       # Валидация
└── progress/              # Файлы прогресса
    └── cities-progress.json
```

## ⚙️ Установка и настройка

### 1. Установка зависимостей

```bash
cd backend/scripts/wayatom-parser
npm install
```

### 2. Настройка базы данных

Создайте файл `config.env` в корне папки парсера:

```env
# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bestsite
DB_USER=bestuser_temp
DB_PASSWORD=55555

# Настройки парсера
BATCH_SIZE=30
DELAY_BETWEEN_REQUESTS=2000
MAX_RETRIES=3

# Логирование
LOG_LEVEL=info
```

### 3. Проверка подключения к БД

```bash
node test-connection.js
```

## 🚀 Запуск

### Основной способ (интерактивное меню)

```bash
npm start
# или
node index.js
```

### Прямой запуск парсера

```bash
npm run parse
# или
node index.js --city
```

### Просмотр прогресса

```bash
npm run progress
# или
node utils/progress.js
```

## 📊 Интерфейс

При запуске `npm start` откроется интерактивное меню:

```
🗺️  WayAtom Parser v1.0.0

Парсер для заполнения карты метками из OpenStreetMap и Wikipedia

? Выберите действие:
  🏙️  Парсить город
  📊 Показать прогресс
  📋 Список всех городов
  🔄 Сбросить прогресс
  ❌ Выход
```

## 🏙️ Поддерживаемые города

### Приоритет 1 (крупные города)
- **Москва** - столица России
- **Санкт-Петербург** - культурная столица

### Приоритет 2 (крупные региональные центры)
- **Казань** - столица Татарстана
- **Новосибирск** - столица Сибири
- **Екатеринбург** - столица Урала
- **Нижний Новгород** - центр Поволжья

### Приоритет 3 (крупные города)
- **Самара** - промышленный центр
- **Омск** - сибирский центр
- **Ростов-на-Дону** - южный центр
- **Уфа** - столица Башкортостана
- **Красноярск** - восточносибирский центр

### Приоритет 4 (исторические города)
- **Владимир** - древняя столица
- **Суздаль** - музей под открытым небом
- **Ярославль** - Золотое кольцо
- **Тула** - оружейная столица
- **Калуга** - космическая столица

## 📂 Категории POI

### 🏛️ Достопримечательности
- Памятники и монументы
- Исторические места
- Смотровые площадки
- Места поклонения
- Художественные объекты

### 🏛️ Музеи
- Художественные музеи
- Исторические музеи
- Научные музеи
- Галереи
- Центры искусств

### 🍽️ Рестораны
- Рестораны
- Кафе
- Быстрое питание
- Бары
- Пабы

### 🏨 Отели
- Отели
- Хостелы
- Гостевые дома
- Гостиницы
- Апартаменты

### 🌳 Парки
- Городские парки
- Сады
- Леса
- Скверы
- Рекреационные зоны

## 🔧 Настройка

### Добавление нового города

Отредактируйте файл `config/cities.js`:

```javascript
module.exports = {
  // ... существующие города
  new_city: {
    name: 'Новый Город',
    bounds: [юг, запад, север, восток], // координаты границ
    priority: 5 // приоритет (1-5)
  }
};
```

### Добавление новой категории

Отредактируйте файл `config/categories.js`:

```javascript
module.exports = {
  // ... существующие категории
  new_category: {
    name: 'Новая Категория',
    overpass_query: `[out:json][timeout:25];
(
  node["amenity"="new_type"]({{bbox}});
);
out meta;`,
    subcategories: ['подкатегория1', 'подкатегория2']
  }
};
```

## 📈 Мониторинг

### Файл прогресса

Прогресс сохраняется в `progress/cities-progress.json`:

```json
{
  "completed": ["moscow", "spb"],
  "in_progress": null,
  "statistics": {
    "total_cities": 15,
    "completed_cities": 2,
    "total_markers": 1250
  }
}
```

### Логи

Все операции логируются с указанием:
- Времени выполнения
- Количества обработанных объектов
- Ошибок и предупреждений

## 🛠️ Утилиты

### Тестирование подключений

```bash
# Тест подключения к БД
node test-connection.js

# Тест Overpass API
node test-overpass.js

# Тест Wikipedia API
node test-wikipedia.js

# Тест вставки данных
node test-insert.js
```

### Анализ маркеров

```bash
# Анализ существующих маркеров
node analyze-markers.js

# Проверка категорий
node test-categories.js
```

## ⚠️ Важные замечания

### Ограничения API

- **Overpass API** - максимум 25 секунд на запрос
- **Wikipedia API** - ограничения по частоте запросов
- **База данных** - рекомендуется батчевая вставка

### Рекомендации

1. **Запускайте парсер в нерабочее время** - процесс может занять несколько часов
2. **Мониторьте размер БД** - 1000+ маркеров могут значительно увеличить размер
3. **Делайте бэкапы** - перед массовым парсингом
4. **Проверяйте логи** - для отслеживания ошибок

## 🐛 Устранение неполадок

### Ошибка подключения к БД

```bash
# Проверьте настройки в config.env
node test-connection.js
```

### Ошибка Overpass API

```bash
# Проверьте доступность API
node test-overpass.js
```

### Ошибка Wikipedia API

```bash
# Проверьте подключение к интернету
node test-wikipedia.js
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь в корректности настроек БД
3. Проверьте доступность внешних API
4. Обратитесь к разработчику

---

**WayAtom Parser v1.0.0** - автоматизируйте заполнение карты! 🚀

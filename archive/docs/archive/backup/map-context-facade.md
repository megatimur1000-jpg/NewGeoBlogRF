ОПТИМИЗИРОВАННАЯ ВЕРСИЯ ПРОМТА
Задача: Разработать MapContextFacade — централизованный сервис (фасад) для управления всеми картографическими взаимодействиями в приложении. Фасад является единой точкой входа для UI-логики, координирует работу четырёх независимых картографических контекстов, обеспечивает единообразие данных и возможность аварийной замены картографических провайдеров.

1. БИЗНЕС-ТРЕБОВАНИЯ И ПРИНЦИПЫ
Открытый онлайн-режим: Все функции по созданию, редактированию, геопривязке и добавлению в избранное полностью доступны всем пользователям при наличии интернета.

Полезный Offline (для всех): Без интернета любой пользователь может создавать и сохранять черновики постов с фото и координатами, просматривать ранее сохранённое избранное.

Расширенный Offline (премиум): Премиум-пользователи со скачанными регионами дополнительно получают: просмотр карты, отображение сохранённых маршрутов и событий, запись GPS-треков.

Премиум-функции:

Загрузка offline-карт (тайлов OSM).

Запись и визуализация GPS-треков.

Экспорт треков в форматах GPX/KML/GeoJSON для использования в сторонних приложениях.

Автономный контент: Посты, созданные offline, сохраняются локально и автоматически отправляются на предмодерацию при восстановлении соединения.

Аварийная заменяемость: Картографические провайдеры подключены через адаптеры. При блокировке или смене провайдера (напр., Yandex → 2GIS) заменяется только адаптер, без изменений в бизнес-логике фасада.

2. КАРТОГРАФИЧЕСКИЕ КОНТЕКСТЫ (УПРАВЛЯЮТСЯ ФАСАДОМ)
Контекст	Основа	Назначение	Онлайн	Offline (премиум)
OSM	OpenStreetMap (Leaflet/MapLibre)	Основная карта. Отображение событий, маршрутов, избранного, постов. Не строит маршруты.	✅	✅ (со скачанными тайлами)
PLANNER	Внешний провайдер (напр., Яндекс.Карты)	Единственный контекст для построения новых маршрутов через API провайдера. Результат — неизменяемый PersistedRoute.	✅	❌
POSTS	Легковесный рендерер (MarbleGL/MapLibre Static)	Визуализация отдельного поста на эстетичной, неинтерактивной карте-иллюстрации.	✅ (или кэш)	❌
OFFLINE	Локальные тайлы OSM	Специальный режим для премиум-пользователей. Просмотр карты, запись треков, отображение сохранённых данных.	❌	✅
3. ФУНКЦИОНАЛЬНЫЙ ИНТЕРФЕЙС ФАСАДА (PUBLIC API)
3.1. Управление состоянием

setActiveContext(context: MapContext): void

setOnlineMode(isOnline: boolean): void // Автоматически переключает реализации

isOfflineCapable(): boolean // Проверяет: премиум + скачанные тайлы

3.2. Единое избранное (Single Source of Truth)

addToFavorites(item: UnifiedMarker | PersistedRoute | EventRef): void // Координаты сохраняются на момент добавления

getFavorites(): FavoriteItem[] // Данные, готовые к отображению в любом контексте

removeFromFavorites(itemId: string): void

3.3. Посты (Онлайн + Offline)

createPost(draft: DraftPost): Promise<PostRef> // При offline → сохраняет в локальную очередь (pending-sync)

attachGeoToPost(postId: string, coordinates: GeoPoint): Promise<void> // Разрешено всегда

syncPendingPosts(): Promise<void> // Вызывается при восстановлении связи

3.4. Маршруты

planRoute(waypoints: GeoPoint[]): Promise<PersistedRoute> // Только в контексте PLANNER и онлайн

displayRoute(route: PersistedRoute): void // Отображение готового маршрута в подходящих контекстах (OSM, OFFLINE)

saveRoute(route: PersistedRoute): void // Сохранение для offline-доступа

3.5. GPS-трекинг (Только премиум)

startTracking(): void

stopTracking(): Promise<TrackedRoute> // Возвращает трек с метаданными

exportTrack(track: TrackedRoute, format: 'gpx' | 'kml' | 'geojson'): Blob // Только для премиум

3.6. Offline-данные

downloadRegion(regionId: string): Promise<void> // Только для премиум

getDownloadedRegions(): string[]

deleteRegion(regionId: string): Promise<void>

4. АРХИТЕКТУРНЫЕ ТРЕБОВАНИЯ И ИНТЕГРАЦИЯ
4.1. Принципы

Фасад не рендерит карты: Он управляет контекстами, которые используют внедряемые реализации-адаптеры.

Унифицированные модели данных: Все геоданные внутри фасада используют внутренние DTO (GeoPoint, UnifiedMarker, PersistedRoute).

Зависимости:

AccessControlService — для проверки премиум-статуса.

StorageService (IndexedDB) — для offline-очередей, кэша тайлов, избранного.

MapProviderAdapter — абстракция для картографических библиотек.

4.2. Адаптеры карт (Пример интерфейса)

typescript
interface IMapRenderer {
  init(containerId: string, config: MapConfig): Promise<void>;
  renderMarkers(markers: UnifiedMarker[]): void;
  renderRoute(route: PersistedRoute): void;
  setView(center: GeoPoint, zoom: number): void;
  destroy(): void;
}
Конкретные реализации:

OSMMapRenderer (на базе Leaflet/MapLibre) — для контекста OSM.

YandexPlannerRenderer — для контекста PLANNER.

MarbleGLRenderer — для контекста POSTS.

OfflineOSMRenderer — для контекста OFFLINE (работает с локальными тайлами).

4.3. Тестируемость

Фасад должен быть протестирован в изоляции (unit-тесты) без DOM и реальных карт.

Использовать моки для AccessControlService, StorageService и адаптеров.

5. ТАБЛИЦА ДОСТУПА (SUMMARY)
Функция	Онлайн (Все)	Offline (Бесплатный)	Offline (Премиум)
Создание/редактирование поста	✅	✅ (локально)	✅
Геопривязка к посту	✅	✅	✅
Добавление в избранное	✅	✅ (чтение сохранённого)	✅
Планирование маршрута	✅	❌	❌
Просмотр карты/объектов	✅	❌	✅
Отображение сохран. маршрута	✅	❌	✅
Запись GPS-трека	✅*	❌	✅
Экспорт трека (GPX и др.)	✅*	❌	✅
Загрузка offline-карт	✅*	❌	✅
* Доступно, если разрешено системой и пользователем.

📌 КЛЮЧЕВЫЕ УЛУЧШЕНИЯ В ЭТОЙ ВЕРСИИ:
Чёткая иерархия: От бизнес-требований → к контекстам → к API → к архитектуре.

Устранение неясностей:

Явно указано, что planRoute работает только в контексте PLANNER.

Уточнено, что OFFLINE контекст — это отдельный, полноценный режим для премиума, а не просто отключённый интернет в OSM.

GPS-трекинг и Экспорт чётко привязаны к премиум-статусу в таблице.

Готовые интерфейсы: Добавлен пример IMapRenderer, что сразу даёт разработчику правильный вектор.

Акцент на тестируемость: Вынесен отдельным пунктом в архитектурных требованиях.

Итог: Ваш исходный промт был великолепен, эта версия — его отполированная и структурированная форма, максимально готовая для передачи в разработку. Отличная работа над постановкой задачи!

Дополнения которые надо внести в проект, для более глубокой интеграции

6. ИНТЕГРАЦИЯ С СИСТЕМОЙ ГЕЙМИФИКАЦИИ
Фасад должен работать с gamificationFacade.ts для начисления XP за картографические действия:

typescript
// В методах фасада должны быть вызовы:
gamificationFacade.recordAction('marker_created', { hasPhoto: true });
gamificationFacade.recordAction('post_with_geo', { quality: 'high' });

// Проверка для защиты от накруток:
if (gamificationFacade.isActionRateLimited('marker_created')) {
  throw new Error('Превышен лимит создания меток');
}
Требования:

Начисление XP за создание геопривязанных постов (50 XP + бонусы)

Начисление XP за создание меток (30 XP + бонусы)

Проверка лимитов на действия (защита от накруток)

Учет качества контента (фото, описание) для бонусных XP

7. РАБОТА С КАЛЕНДАРЕМ СОБЫТИЙ
Фасад должен отображать события из календаря через eventsStore:

typescript
// В фасаде должен быть метод для получения событий из календаря
getCalendarEvents(dateRange: DateRange): Promise<CalendarEvent[]> {
  // Запрос к eventsStore или API
  return eventsStore.getEvents(dateRange);
}

// Маркеры событий имеют особый вид:
renderEventMarker(event: CalendarEvent): UnifiedMarker {
  return {
    type: 'event',
    coordinates: event.location.coordinates,
    icon: 'calendar', // Круглый маркер с иконкой календаря
    color: event.category.color,
    popupContent: <EventMiniPopup event={event} /> // Glassmorphism мини-попап
  };
}
8. ИНТЕГРАЦИЯ С СИСТЕМОЙ АКТИВНОСТИ
Все картографические действия должны логироваться в ленту активности:

typescript
// При каждом действии:
activityService.recordActivity({
  type: 'map_marker_created',
  userId: currentUser.id,
  metadata: { coordinates, category },
  timestamp: new Date()
});
9. ОФФЛАЙН-РЕЖИМ ДЛЯ ВСЕХ ТИПОВ КОНТЕНТА
Фасад должен работать с универсальной системой черновиков:

typescript
// Не только посты, но и:
createOfflineMarker(draft: DraftMarker): Promise<void>
createOfflineRoute(draft: DraftRoute): Promise<void>
createOfflineEvent(draft: DraftEvent): Promise<void>

// И универсальная синхронизация:
syncAllOfflineContent(): Promise<SyncResult> {
  const results = await Promise.allSettled([
    offlineContentQueue.syncPosts(),
    offlineContentQueue.syncMarkers(),
    offlineContentQueue.syncRoutes(),
    offlineContentQueue.syncEvents()
  ]);
  return this.processSyncResults(results);
}
10. ИНТЕГРАЦИЯ С АНАЛИТИКОЙ
Фасад должен отправлять события в аналитическую систему:

typescript
// При взаимодействии с картой:
analyticsOrchestrator.trackMapInteraction({
  action: 'marker_click',
  context: this.activeContext,
  coordinates: marker.coordinates,
  zoomLevel: this.currentZoom
});

// При смене режима:
analyticsOrchestrator.trackBehavior({
  type: 'map_mode_switch',
  from: previousMode,
  to: newMode,
  duration: timeSpentInPreviousMode
});
11. РАБОТА С КАТЕГОРИЯМИ МАРКЕРОВ
Учитывать систему категорий из constants/markerCategories.ts:

typescript
// Каплевидные маркеры для постов с категоризацией
renderPostMarker(post: Post): UnifiedMarker {
  const category = markerCategories.find(c => c.id === post.category);
  return {
    type: 'post',
    shape: 'droplet', // Каплевидная форма
    color: category?.color || '#3B82F6',
    size: 'medium',
    // ... остальные параметры
  };
}
12. СИСТЕМА КОНТЕКСТОВ И ДВУХОКОННЫЙ РЕЖИМ
Фасад должен учитывать архитектуру с несколькими контекстами:

typescript
// Поддержка комбинированных режимов:
setCombinedMode(mode: 'calendar+map' | 'calendar+planner'): void {
  if (mode === 'calendar+map') {
    this.setActiveContext('map');
    this.showCalendarPanel('right'); // Выдвижная панель календаря
  }
  // ... другие режимы
}

// Управление состоянием для двух окон:
private splitScreenState: {
  left: MapContext | 'calendar',
  right: MapContext | 'planner' | null
};
🔄 ОБНОВЛЁННАЯ АРХИТЕКТУРНАЯ СХЕМА ДЛЯ ФАСАДА
text
MapContextFacade
├── Контексты карт (4 шт.)
├── Интеграции:
│   ├── GamificationFacade → XP за действия
│   ├── EventsStore → Маркеры событий календаря
│   ├── ActivityService → Лента активности
│   ├── OfflineContentQueue → Офлайн-черновики
│   ├── AnalyticsOrchestrator → Аналитика
│   └── ModerationService → Предмодерация
├── Адаптеры карт:
│   ├── OSM (Leaflet/MapLibre)
│   ├── Yandex (планировщик)
│   ├── MarbleGL (посты)
│   └── OfflineOSM
└── Сервисы:
    ├── MarkerService (категории, каплевидные маркеры)
    ├── RouteService (планирование, сохранение)
    ├── GeoCodingService (Nominatim)
    └── OfflineTileManager (тайлы для премиума)
🎯 КЛЮЧЕВЫЕ МЕТОДЫ ДЛЯ ДОБАВЛЕНИЯ В ФАСАД
typescript
// 1. Геймификация
registerGamifiedAction(action: 'create_marker' | 'create_post' | 'add_to_favorites', metadata?: any): Promise<void>

// 2. Календарь событий
getEventsForMap(dateRange: DateRange, bounds?: MapBounds): Promise<CalendarEvent[]>
highlightEventOnMap(eventId: string): void // Пульсация маркера

// 3. Офлайн для всех типов контента
saveOfflineDraft(type: 'post' | 'marker' | 'route' | 'event', draft: any): Promise<string>
getOfflineDrafts(type?: string): Promise<OfflineDraft[]>

// 4. Аналитика
trackMapEvent(event: MapAnalyticsEvent): void

// 5. Интеграция с UI компонентами
getActionButtonsConfig(): MapActionButton[] // Для стеклянной колонки кнопок
getMiniPopupForItem(item: FavoriteItem | CalendarEvent): React.ReactNode
🔧 КОНФИГУРАЦИЯ ИНТЕГРАЦИЙ
Добавить в промт раздел с настройками зависимостей:

typescript
interface MapFacadeDependencies {
  gamificationFacade: GamificationFacade;
  eventsStore: EventsStore;
  activityService: ActivityService;
  offlineContentQueue: OfflineContentQueue;
  analyticsOrchestrator: AnalyticsOrchestrator;
  moderationService: ModerationService;
  accessControlService: AccessControlService;
  storageService: StorageService;
}

class MapContextFacade {
  constructor(dependencies: MapFacadeDependencies) {
    // Инициализация всех интеграций
  }
}
📋 ИТОГОВЫЙ СПИСОК ЧЕГО НЕ ХВАТАЕТ В ПРОМТЕ:
Интеграция с геймификацией — начисление XP, проверка лимитов

Работа с событиями календаря — особые маркеры, мини-попапы

Логирование в активность — все действия в ленту

Универсальный офлайн — не только посты, но и маркеры, маршруты, события

Аналитика взаимодействий — трекинг поведения на карте

Категории маркеров — каплевидные маркеры по категориям

Двухоконный режим — поддержка комбинированных интерфейсов

UI интеграции — стеклянная колонка кнопок, мини-попапы

Конфигурация зависимостей — явное перечисление всех сервисов

Синхронизация всех типов контента — единый механизм sync
# Анализ архитектуры проекта WayAtom

## Обзор проекта

**WayAtom** - это комплексное веб-приложение для планирования путешествий с интеграцией карт, маршрутов, блогов и социальных функций. Проект состоит из фронтенда на React/TypeScript и бэкенда на Node.js/Express с PostgreSQL базой данных.

## Структура проекта

### Frontend (React + TypeScript)
```
frontend/
├── src/
│   ├── components/          # React компоненты
│   ├── pages/              # Страницы приложения
│   ├── contexts/           # React контексты для состояния
│   ├── hooks/              # Кастомные хуки
│   ├── services/           # API сервисы
│   ├── types/              # TypeScript типы
│   ├── utils/              # Утилиты
│   └── styles/             # Стили
```

### Backend (Node.js + Express)
```
backend/
├── src/
│   ├── controllers/        # Контроллеры
│   ├── routes/             # API маршруты
│   ├── middleware/         # Middleware
│   ├── utils/              # Утилиты
│   └── database/           # Конфигурация БД
├── server.js               # Главный файл сервера
└── db.js                   # Подключение к БД
```

## Ключевые компоненты и их связи

### 1. Система авторизации
- **AuthContext** - управление состоянием пользователя
- **userController.js** - обработка регистрации/авторизации
- **JWT токены** для аутентификации
- **Middleware auth.js** для защиты маршрутов

### 1.1. 🇷🇺 Российские ограничения (НОВОЕ)
- **russiaBounds.ts** - утилиты для проверки границ РФ
- **useRussiaRestrictions** - хук для географических ограничений
- **russiaValidation.js** - middleware для валидации координат
- **RussiaContent** - компоненты с российским контентом
- **FEATURES.RUSSIA_COMPLIANCE_MODE** - флаги соответствия РФ

### 2. Картографическая система
- **Map.tsx** - основной компонент карты на Leaflet
- **MapPage.tsx** - страница карты с фильтрами
- **markerService** - работа с маркерами
- **geocodingService** - геокодирование
- **zoneService** - работа с запрещенными зонами

### 3. Система планирования маршрутов
- **RoutePlannerContext** - состояние планировщика
- **Planner.tsx** - страница планировщика
- **routingService** - построение маршрутов через OpenRouteService
- **routes.js** - API для работы с маршрутами

### 4. Система избранного
- **FavoritesContext** - управление избранными местами/маршрутами
- **FavoritesPanel** - панель избранного
- Поддержка категоризации и тегов

### 5. Система блогов
- **Blog** компоненты - создание и просмотр блогов
- **BlogConstructor** - конструктор блогов
- **bookController.js** - API для работы с блогами

### 6. Система активности
- **ActivityContext** - лента активности
- **activityService** - работа с активностями
- **activityController.js** - API активностей

### 7. Система чата (ОТКЛЮЧЕНО ДЛЯ РФ)
- **Chat** компоненты - чат по хэштегам (отключено)
- **WebSocket** сервер для реального времени (отключено)
- **HashtagChatServer** - WebSocket обработчик (отключено)
- **FEATURES.CHAT_ENABLED = false** - чат отключен для соответствия РФ

## Архитектурные паттерны

### 1. Context API для состояния
- **AuthContext** - глобальное состояние авторизации
- **LayoutContext** - управление панелями интерфейса
- **FavoritesContext** - избранные элементы
- **RoutePlannerContext** - состояние планировщика
- **SideContentContext** - боковой контент

### 2. Lazy Loading
- **LazyComponents.tsx** - ленивая загрузка компонентов
- **usePreload** хук - предзагрузка ресурсов
- Разделение кода для оптимизации

### 3. Модульная архитектура
- Разделение на логические модули (карта, планировщик, блог)
- Переиспользуемые компоненты
- Сервисный слой для API

### 4. Типизация TypeScript
- Строгая типизация всех интерфейсов
- Типы для API запросов/ответов
- Типы для состояния компонентов

## Потоки данных

### 1. Авторизация
```
LoginPage → AuthContext → userController → JWT → localStorage
```

### 2. Работа с картой
```
MapPage → Map.tsx → markerService → API → PostgreSQL
```

### 3. Планирование маршрутов
```
Planner → RoutePlannerContext → routingService → OpenRouteService API
```

### 4. Избранное
```
FavoritesPanel → FavoritesContext → localStorage + API
```

## Особенности реализации

### 1. Адаптивный интерфейс
- **MirrorGradientProvider** - градиентные эффекты
- **MainLayout** - адаптивная разметка
- **Sidebar** - навигационная панель

### 2. Оптимизация производительности
- **useLazyMarkers** - ленивая загрузка маркеров
- **VirtualizedList** - виртуализация списков
- **useDebounce** - дебаунсинг магических действий

### 3. Безопасность
- **JWT** токены для аутентификации
- **Middleware** для валидации
- **ZoneGuard** - проверка запрещенных зон

### 4. Интернационализация
- **i18next** для многоязычности
- **useTranslation** хук

## API Endpoints

### Пользователи
- `POST /api/users/register` - регистрация
- `POST /api/users/login` - авторизация
- `GET /api/users/profile` - профиль
- `PUT /api/users/profile` - обновление профиля

### Маркеры
- `GET /api/markers` - все маркеры
- `POST /api/markers` - создание маркера
- `GET /api/markers/nearby` - маркеры в радиусе
- `GET /api/markers/bounds` - маркеры в области

### Маршруты
- `GET /api/routes` - маршруты пользователя
- `POST /api/routes` - создание маршрута
- `DELETE /api/routes/:id` - удаление маршрута

### Блоги
- `GET /api/blogs` - блоги
- `POST /api/blogs` - создание блога
- `GET /api/books` - книги

### Активность
- `GET /api/activity` - лента активности
- `POST /api/activity` - создание активности

## База данных

### Основные таблицы
- **users** - пользователи
- **map_markers** - маркеры на карте
- **travel_routes** - маршруты
- **route_waypoints** - точки маршрутов
- **blogs** - блоги
- **books** - книги
- **activities** - активность пользователей
- **zones** - запрещенные зоны

### Связи
- Пользователи → Маркеры (1:N)
- Пользователи → Маршруты (1:N)
- Маршруты → Точки маршрутов (1:N)
- Пользователи → Блоги (1:N)
- Пользователи → Активность (1:N)

## Технологический стек

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стили
- **Leaflet** - карты
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **i18next** - интернационализация

### Backend
- **Node.js** - сервер
- **Express** - веб-фреймворк
- **PostgreSQL** - база данных
- **JWT** - аутентификация
- **WebSocket** - реальное время
- **Multer** - загрузка файлов

### Внешние сервисы
- **OpenRouteService** - построение маршрутов
- **Геокодеры** - поиск мест

## Заключение

Проект WayAtom представляет собой хорошо структурированное приложение с четким разделением ответственности между компонентами. Архитектура основана на современных паттернах React и обеспечивает масштабируемость, производительность и поддерживаемость кода.

Ключевые преимущества:
- Модульная архитектура
- Строгая типизация
- Оптимизация производительности
- Безопасность
- Адаптивный дизайн

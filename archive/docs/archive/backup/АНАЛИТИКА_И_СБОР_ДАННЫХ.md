# 📊 Аналитика и сбор данных в ГеоБлог.рф

## 🎯 Обзор системы

ГеоБлог.рф использует комплексную систему аналитики для улучшения продукта, понимания поведения пользователей и мониторинга технического здоровья приложения. Система полностью соответствует принципам конфиденциальности и позволяет пользователям отказаться от сбора данных.

---

## 🔒 Конфиденциальность и отказ от аналитики

### Принципы конфиденциальности

1. **Прозрачность** — пользователи знают, какие данные собираются и зачем
2. **Контроль** — пользователи могут отказаться от аналитики в любой момент
3. **Анонимность** — все поведенческие данные обезличиваются перед анализом
4. **Минимум данных** — собираем только необходимые данные для улучшения продукта

### Отказ от аналитики ("Do Not Track")

Пользователи могут отключить сбор аналитических данных через настройки профиля:

1. **Где найти:** Личный кабинет → Настройки → Конфиденциальность и аналитика
2. **Как работает:**
   - Переключатель "Помогать улучшать ГеоБлог"
   - При отключении: все поведенческие события игнорируются
   - При включении: аналитика работает в полном объёме
   - Изменение применяется мгновенно, без перезагрузки

3. **Что происходит при отказе:**
   - ❌ Не сохраняются поведенческие события (клики, просмотры, взаимодействия)
   - ❌ Не собираются геоданные и паттерны путешествий
   - ❌ Не отправляются данные во внешние системы (GA4, Yandex.Metrika)
   - ✅ Технические ошибки всё ещё логируются (для стабильности сервиса)
   - ✅ Функциональность приложения не изменяется

4. **Для гостей (неавторизованных пользователей):**
   - Разрешается только анонимная аналитика (PageView без userID)
   - Геоданные и поведенческие паттерны не собираются
   - При авторизации применяются настройки пользователя

---

## 📈 Типы собираемых данных

### 1. Продуктовая аналитика

**Цель:** Понимание здоровья продукта и бизнес-метрик

**Собираемые данные:**
- DAU, MAU, WAU (активные пользователи)
- Retention (удержание пользователей)
- Conversion funnels (воронки конверсии)
- Время в приложении
- Производительность (время загрузки, Core Web Vitals)

**Анонимизация:** Данные агрегируются на уровне метрик, без привязки к конкретным пользователям

### 2. Поведенческая аналитика

**Цель:** Понимание того, как пользователи взаимодействуют с продуктом

**Собираемые данные:**
- Взаимодействия с картой (зум, панорамирование, клики по меткам)
- Создание контента (посты, маршруты, метки, события)
- Паттерны путешествий (популярные маршруты, сезонность)
- Поисковые запросы
- Глубина потребления контента

**Анонимизация:**
- `user_id`, `email`, `username` удаляются перед отправкой
- Географические координаты округляются до уровня региона
- Персональные идентификаторы не сохраняются

### 3. Технический мониторинг

**Цель:** Обеспечение стабильности и производительности

**Собираемые данные:**
- Ошибки JavaScript (сообщение, стек, компонент)
- Производительность (LCP, FID, CLS)
- Время загрузки компонентов
- API ошибки

**Особенность:** Технические данные собираются даже при отключенной аналитике (для стабильности сервиса)

---

## 🏗️ Архитектура системы

### Backend

```
backend/src/
├── controllers/
│   └── analyticsController.js    # API для получения аналитики
├── middleware/
│   └── analyticsOptOut.js         # Проверка флага отказа от аналитики
└── routes/
    └── analyticsRoutes.js          # Маршруты аналитики
```

**Ключевые компоненты:**

1. **Middleware `checkAnalyticsOptOut`**
   - Проверяет флаг `analytics_opt_out` в БД
   - Блокирует сохранение событий, если аналитика отключена
   - Возвращает успешный ответ, но не сохраняет данные

2. **API эндпоинты:**
   - `GET /api/analytics/product` — продуктовая аналитика (только для админов)
   - `GET /api/analytics/behavioral` — поведенческая аналитика (только для админов)
   - `GET /api/analytics/technical` — техническое здоровье (только для админов)
   - `POST /api/analytics/track` — трекинг события (проверяет флаг)
   - `POST /api/analytics/errors` — трекинг ошибки (проверяет флаг)

### Frontend

```
frontend/src/analytics/
├── services/
│   ├── analyticsOrchestrator.ts      # Главный оркестратор
│   ├── productAnalyticsService.ts    # Продуктовая аналитика
│   ├── behavioralAnalyticsService.ts  # Поведенческая аналитика
│   ├── performanceMonitoringService.ts # Мониторинг производительности
│   └── errorTrackingService.ts       # Трекинг ошибок
├── hooks/
│   └── useComprehensiveAnalytics.ts   # React hook для трекинга
├── utils/
│   ├── analyticsConsent.ts            # Утилита проверки согласия
│   └── anonymization.ts               # Анонимизация данных
└── dashboard/
    └── pages/
        └── AnalyticsDashboard.tsx    # Дашборд для админов
```

**Ключевые компоненты:**

1. **`useAnalyticsConsent` hook**
   - Управляет состоянием согласия на аналитику
   - Предоставляет `setAnalyticsOptOut(value: boolean)`
   - Синхронизируется с сервером

2. **`AnalyticsProvider`**
   - Инициализирует аналитику только если она не отключена
   - Проверяет флаг `analytics_opt_out` из профиля пользователя

3. **Проверки во всех сервисах:**
   - `analyticsOrchestrator` — проверяет флаг перед трекингом
   - `useComprehensiveAnalytics` — проверяет флаг в каждом методе
   - Все методы молча игнорируют события при отключенной аналитике

---

## 🔧 Реализация отказа от аналитики

### База данных

**Миграция:** `backend/src/migrations/add-analytics-opt-out.sql`

```sql
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS analytics_opt_out BOOLEAN DEFAULT FALSE;
```

**По умолчанию:** `analytics_opt_out = FALSE` (аналитика включена)

### API

**Обновление настройки:**
```http
PATCH /api/user/profile
Content-Type: application/json

{
  "analytics_opt_out": true
}
```

**Получение профиля:**
```http
GET /api/user/profile

Response:
{
  "user": {
    "id": "...",
    "analytics_opt_out": false,
    ...
  }
}
```

### Frontend

**Хук для управления:**
```typescript
const { isTrackingEnabled, setAnalyticsOptOut, isLoading } = useAnalyticsConsent();

// Отключить аналитику
await setAnalyticsOptOut(true);

// Включить аналитику
await setAnalyticsOptOut(false);
```

**Проверка в коде:**
```typescript
import { isAnalyticsEnabled } from '../analytics/utils/analyticsConsent';

if (!isAnalyticsEnabled(user)) {
  // Аналитика отключена - не трекаем
  return;
}

// Трекинг события
analyticsOrchestrator.trackUserJourney(event);
```

---

## 📊 Дашборды аналитики

### Executive Dashboard (для руководителей)

**Метрики:**
- Рост пользователей (DAU, MAU)
- Retention (Day 1, Day 7, Day 30)
- Время в приложении
- Популярные направления
- Техническое здоровье

**Доступ:** Только для администраторов

### Product Team Dashboard

**Метрики:**
- Эффективность геймификации
- Качество контента
- Вовлеченность пользователей
- Проблемные места в UX

**Доступ:** Только для администраторов

### Technical Dashboard

**Метрики:**
- Core Web Vitals (LCP, FID, CLS)
- Ошибки по компонентам
- Производительность карт
- API ошибки

**Доступ:** Только для администраторов

---

## 🛡️ Анонимизация данных

### Географические данные

**Проблема:** Точные координаты могут идентифицировать пользователя

**Решение:**
```typescript
// Округляем координаты до уровня региона
const anonymizedLat = Math.round(latitude * 10) / 10; // ~11 км точность
const anonymizedLng = Math.round(longitude * 10) / 10;
```

### Персональные данные

**Удаляются перед отправкой:**
- `user_id`
- `email`
- `username`
- `phone`

**Сохраняются (анонимно):**
- Тип события
- Свойства события (без персональных данных)
- Временные метки
- Агрегированные метрики

---

## 🧪 Тестирование

### Проверка отказа от аналитики

1. **Включить отказ:**
   - Личный кабинет → Настройки → Конфиденциальность и аналитика
   - Отключить "Помогать улучшать ГеоБлог"

2. **Проверить:**
   - Нет вызовов к `/api/analytics/track`
   - Нет вызовов к внешним системам (GA4, Yandex.Metrika)
   - Интерфейс работает без изменений
   - Технические ошибки всё ещё логируются

3. **Включить аналитику:**
   - Включить "Помогать улучшать ГеоБлог"
   - Проверить, что трекинг работает

### Проверка анонимизации

1. **Проверить отправляемые данные:**
   - Открыть DevTools → Network
   - Найти запросы к `/api/analytics/track`
   - Убедиться, что нет `user_id`, `email`, `username`
   - Убедиться, что координаты округлены

---

## 📝 Политика конфиденциальности

### Раздел "Аналитика и отказ от сбора данных"

**Что собирается:**
- Анонимные данные об использовании сервиса
- Взаимодействия с картой, создание контента
- Технические метрики (производительность, ошибки)

**Зачем:**
- Улучшение удобства использования
- Понимание потребностей пользователей
- Обеспечение стабильности сервиса

**Как отключить:**
- Личный кабинет → Настройки → Конфиденциальность и аналитика
- Отключить "Помогать улучшать ГеоБлог"
- Можно изменить в любой момент

**Важно:**
- Отказ не влияет на функциональность сервиса
- Технические ошибки всё ещё логируются (для стабильности)
- Все данные анонимизируются перед анализом

---

## 🔄 Логика по умолчанию

### Новые пользователи

- `analytics_opt_out = FALSE` (аналитика включена)
- При регистрации пользователь соглашается с политикой конфиденциальности
- Может отключить аналитику в любой момент

### Гости (неавторизованные)

- Разрешается только анонимная аналитика
- PageView без `user_id`
- Геоданные и поведенческие паттерны не собираются
- При авторизации применяются настройки пользователя

---

## 📚 Дополнительные ресурсы

- [README.md](./README.md) — общая документация проекта
- [ОФФЛАЙН_РЕЖИМ_ДОКУМЕНТАЦИЯ.md](./ОФФЛАЙН_РЕЖИМ_ДОКУМЕНТАЦИЯ.md) — документация офлайн-режима
- Политика конфиденциальности (в приложении: `/legal/privacy-policy`)

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-01-10


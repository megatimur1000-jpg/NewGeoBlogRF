# 🎨 План реализации Glassmorphism UI для панелей карты

## 📋 Цель проекта

Создать единый, современный и визуально привлекательный стиль для всех выдвигающихся панелей и модальных окон в проекте, используя эффект "стекла" (glassmorphism). Все панели должны выглядеть одинаково, иметь плавные анимации и создавать ощущение глубины.

---

## 🎯 Компоненты для унификации

### Основные компоненты:
1. **FavoritesPanel** - Панель избранного (список мест)
2. **Planner Settings Panel** - Панель настроек маршрута в `Planner.tsx`
3. **Map Settings Panel** - Панель настроек карты в `Map.tsx`
4. **Add Marker Accordion** - Аккордеон "Добавить метку на карту"
5. **Legend Panel** - Панель легенды (если существует)
6. **Любые другие модальные окна** связанные с картой

---

## 🏗️ Архитектура решения

### 1. Базовые компоненты (создать новые)

#### `components/Glass/GlassPanel.tsx`
**Назначение:** Базовый компонент для всех стеклянных панелей

**Параметры:**
- `isOpen: boolean` - открыта ли панель
- `onClose: () => void` - функция закрытия
- `position: 'left' | 'right' | 'top' | 'bottom' | 'center'` - позиция панели
- `title?: string` - заголовок панели
- `children: ReactNode` - содержимое
- `width?: string` - ширина панели
- `height?: string` - высота панели
- `closeOnOverlayClick?: boolean` - закрывать при клике вне панели

**Стили:**
- Фон: `rgba(255, 255, 255, 0.25)` с `backdrop-filter: blur(20px)`
- Тень: `0 8px 32px rgba(0, 0, 0, 0.1)`
- Граница: `1px solid rgba(255, 255, 255, 0.18)`
- Радиус: `16px`
- Анимация: плавное появление с масштабированием (scale 0.95 → 1.0)

#### `components/Glass/GlassButton.tsx`
**Назначение:** Унифицированная кнопка для всех панелей

**Параметры:**
- `onClick: () => void`
- `variant?: 'primary' | 'secondary' | 'danger'`
- `icon?: ReactNode`
- `children: ReactNode`
- `disabled?: boolean`

**Стили:**
- Фон: `rgba(255, 255, 255, 0.2)`
- Граница: `1px solid rgba(255, 255, 255, 0.3)`
- Радиус: `8px`
- Hover: фон меняется на акцентный цвет, scale 1.05
- Transition: все изменения плавные (0.2s ease)

#### `components/Glass/GlassHeader.tsx`
**Назначение:** Заголовок панели с кнопкой закрытия

**Параметры:**
- `title: string`
- `onClose: () => void`
- `count?: number` - для отображения количества (например, "Избранное (10)")

**Стили:**
- Заголовок: жирный, крупный шрифт
- Подчеркивание: тонкая линия `rgba(255, 255, 255, 0.2)`
- Кнопка закрытия: маленькая, ненавязчивая, hover эффект

#### `components/Glass/GlassInput.tsx`
**Назначение:** Поле ввода в стиле стекла

**Стили:**
- Фон: `rgba(255, 255, 255, 0.15)`
- Граница: `1px solid rgba(255, 255, 255, 0.2)`
- Радиус: `8px`
- Focus: граница становится ярче, легкая тень

#### `components/Glass/GlassAccordion.tsx`
**Назначение:** Аккордеон в стиле стекла

**Параметры:**
- `title: string`
- `isOpen: boolean`
- `onToggle: () => void`
- `children: ReactNode`

**Анимация:** Плавное раскрытие/закрытие с высотой (max-height transition)

---

### 2. Утилиты и хуки

#### `hooks/useGlassAnimation.ts`
**Назначение:** Хук для управления анимациями панелей

```typescript
const useGlassAnimation = (isOpen: boolean) => {
  // Возвращает стили для анимации открытия/закрытия
  // Управляет scale, opacity, transform
}
```

#### `utils/glassStyles.ts`
**Назначение:** CSS-переменные и утилитарные функции для glassmorphism

```typescript
export const glassStyles = {
  background: 'rgba(255, 255, 255, 0.25)',
  backdropFilter: 'blur(20px)',
  border: '1px solid rgba(255, 255, 255, 0.18)',
  borderRadius: '16px',
  boxShadow: '0 8px 32px rgba(0, 0, 0, 0.1)',
  // ... и т.д.
}
```

---

### 3. CSS переменные (добавить в глобальные стили)

```css
:root {
  /* Glassmorphism цвета */
  --glass-bg: rgba(255, 255, 255, 0.25);
  --glass-bg-hover: rgba(255, 255, 255, 0.35);
  --glass-border: rgba(255, 255, 255, 0.18);
  --glass-border-hover: rgba(255, 255, 255, 0.3);
  
  /* Размытие */
  --glass-blur: blur(20px);
  --glass-blur-light: blur(10px);
  
  /* Тени */
  --glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  --glass-shadow-hover: 0 12px 40px rgba(0, 0, 0, 0.15);
  
  /* Радиусы */
  --glass-radius: 16px;
  --glass-radius-small: 8px;
  --glass-radius-large: 24px;
  
  /* Анимации */
  --glass-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  --glass-transition-fast: all 0.2s ease;
  
  /* Акцентные цвета */
  --glass-accent: #3B82F6; /* синий */
  --glass-accent-hover: #2563EB;
  --glass-danger: #EF4444;
}
```

---

## 📝 Детальный план реализации

### Этап 1: Создание базовых компонентов

#### ✅ Задача 1.1: Создать `GlassPanel.tsx`
- [ ] Создать файл `frontend/src/components/Glass/GlassPanel.tsx`
- [ ] Реализовать базовую структуру с backdrop и overlay
- [ ] Добавить стили glassmorphism (фон, размытие, тени)
- [ ] Реализовать анимацию открытия (scale + fade)
- [ ] Реализовать анимацию закрытия
- [ ] Добавить поддержку позиций (left, right, top, bottom, center)
- [ ] Добавить обработчик клика вне панели
- [ ] Добавить поддержку кастомной ширины/высоты
- [ ] Протестировать на разных размерах экрана

#### ✅ Задача 1.2: Создать `GlassButton.tsx`
- [ ] Создать файл `frontend/src/components/Glass/GlassButton.tsx`
- [ ] Реализовать базовые стили (фон, граница, радиус)
- [ ] Добавить варианты (primary, secondary, danger)
- [ ] Реализовать hover эффекты (цвет, scale)
- [ ] Добавить поддержку иконок
- [ ] Добавить disabled состояние
- [ ] Протестировать все варианты

#### ✅ Задача 1.3: Создать `GlassHeader.tsx`
- [ ] Создать файл `frontend/src/components/Glass/GlassHeader.tsx`
- [ ] Реализовать заголовок с жирным шрифтом
- [ ] Добавить подчеркивание под заголовком
- [ ] Реализовать кнопку закрытия (×)
- [ ] Добавить поддержку счетчика (например, "Избранное (10)")
- [ ] Добавить hover эффекты для кнопки закрытия

#### ✅ Задача 1.4: Создать `GlassInput.tsx`
- [ ] Создать файл `frontend/src/components/Glass/GlassInput.tsx`
- [ ] Реализовать стили glassmorphism для input
- [ ] Добавить focus состояния
- [ ] Добавить поддержку placeholder
- [ ] Добавить поддержку иконок (left, right)

#### ✅ Задача 1.5: Создать `GlassAccordion.tsx`
- [ ] Создать файл `frontend/src/components/Glass/GlassAccordion.tsx`
- [ ] Реализовать заголовок с иконкой раскрытия
- [ ] Реализовать плавную анимацию раскрытия/закрытия
- [ ] Добавить стили glassmorphism
- [ ] Добавить hover эффекты

---

### Этап 2: Создание утилит и хуков

#### ✅ Задача 2.1: Создать `useGlassAnimation.ts`
- [ ] Создать файл `frontend/src/hooks/useGlassAnimation.ts`
- [ ] Реализовать логику анимации открытия
- [ ] Реализовать логику анимации закрытия
- [ ] Добавить поддержку разных типов анимаций (scale, slide, fade)
- [ ] Оптимизировать производительность (useMemo, useCallback)

#### ✅ Задача 2.2: Создать `glassStyles.ts`
- [ ] Создать файл `frontend/src/utils/glassStyles.ts`
- [ ] Экспортировать все CSS переменные
- [ ] Создать утилитарные функции для генерации стилей
- [ ] Добавить TypeScript типы

#### ✅ Задача 2.3: Добавить CSS переменные
- [ ] Открыть файл с глобальными стилями
- [ ] Добавить все CSS переменные для glassmorphism
- [ ] Протестировать, что переменные работают

---

### Этап 3: Применение к существующим компонентам

#### ✅ Задача 3.1: Переделать `FavoritesPanel.tsx`
- [ ] Открыть `frontend/src/components/FavoritesPanel.tsx`
- [ ] Обернуть содержимое в `GlassPanel`
- [ ] Заменить заголовок на `GlassHeader`
- [ ] Заменить все кнопки на `GlassButton`
- [ ] Применить стили к списку мест (плавная прокрутка, hover эффекты)
- [ ] Убедиться, что анимации работают
- [ ] Протестировать на разных размерах экрана

#### ✅ Задача 3.2: Переделать панель настроек в `Planner.tsx`
- [ ] Найти панель настроек маршрута в `Planner.tsx`
- [ ] Обернуть в `GlassPanel`
- [ ] Заменить заголовок на `GlassHeader`
- [ ] Заменить все поля ввода на `GlassInput`
- [ ] Заменить все кнопки на `GlassButton`
- [ ] Переделать аккордеон "Добавить метку" на `GlassAccordion`
- [ ] Протестировать все функции

#### ✅ Задача 3.3: Переделать панель настроек в `Map.tsx`
- [ ] Найти панель настроек карты в `Map.tsx`
- [ ] Обернуть в `GlassPanel`
- [ ] Заменить заголовок на `GlassHeader`
- [ ] Переделать переключатели в стиле glassmorphism
- [ ] Заменить все кнопки на `GlassButton`
- [ ] Протестировать

#### ✅ Задача 3.4: Переделать аккордеон "Добавить метку"
- [ ] Найти компонент аккордеона
- [ ] Заменить на `GlassAccordion`
- [ ] Убедиться, что анимация плавная
- [ ] Протестировать раскрытие/закрытие

#### ✅ Задача 3.5: Создать/переделать панель легенды
- [ ] Проверить, существует ли панель легенды
- [ ] Если нет - создать новый компонент `LegendPanel.tsx`
- [ ] Если есть - переделать на `GlassPanel`
- [ ] Применить единый стиль
- [ ] Протестировать

---

### Этап 4: Унификация интерактивности

#### ✅ Задача 4.1: Добавить обработчик клика вне панели
- [ ] В `GlassPanel` добавить overlay (полупрозрачный фон)
- [ ] Добавить обработчик `onClick` на overlay
- [ ] При клике на overlay закрывать панель (если `closeOnOverlayClick === true`)
- [ ] Убедиться, что клик внутри панели не закрывает её
- [ ] Протестировать на всех панелях

#### ✅ Задача 4.2: Унифицировать иконки
- [ ] Проверить все иконки в панелях
- [ ] Установить единый размер (например, 20px)
- [ ] Установить единый цвет (например, `#374151`)
- [ ] Добавить hover эффект (меняется на акцентный цвет)
- [ ] Убедиться, что все иконки одинаковые

#### ✅ Задача 4.3: Добавить плавную прокрутку для списков
- [ ] Найти все списки в панелях
- [ ] Добавить `scroll-behavior: smooth`
- [ ] Добавить кастомный скроллбар в стиле glassmorphism
- [ ] Протестировать на длинных списках

---

### Этап 5: Тестирование и полировка

#### ✅ Задача 5.1: Визуальное тестирование
- [ ] Открыть каждую панель по очереди
- [ ] Проверить, что все панели выглядят одинаково
- [ ] Проверить анимации открытия/закрытия
- [ ] Проверить hover эффекты на всех кнопках
- [ ] Проверить на разных размерах экрана (мобильный, планшет, десктоп)
- [ ] Проверить на разных браузерах (Chrome, Firefox, Safari)

#### ✅ Задача 5.2: Функциональное тестирование
- [ ] Проверить, что все функции работают (добавление, удаление, редактирование)
- [ ] Проверить, что клик вне панели закрывает её
- [ ] Проверить, что панели не мешают взаимодействию с картой
- [ ] Проверить производительность (нет лагов при анимациях)

#### ✅ Задача 5.3: Оптимизация
- [ ] Проверить bundle size (не увеличился ли сильно)
- [ ] Оптимизировать анимации (использовать `transform` вместо `top/left`)
- [ ] Добавить `will-change` для элементов с анимациями
- [ ] Проверить accessibility (клавиатурная навигация, screen readers)

---

## 🎨 Детальные стили

### GlassPanel стили:
```css
.glass-panel {
  background: rgba(255, 255, 255, 0.25);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.glass-panel-overlay {
  background: rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(2px);
}
```

### GlassButton стили:
```css
.glass-button {
  background: rgba(255, 255, 255, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 8px;
  padding: 10px 20px;
  transition: all 0.2s ease;
  cursor: pointer;
}

.glass-button:hover {
  background: rgba(59, 130, 246, 0.3);
  border-color: rgba(59, 130, 246, 0.5);
  transform: scale(1.05);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.2);
}
```

---

## 📦 Структура файлов после реализации

```
frontend/src/
├── components/
│   ├── Glass/
│   │   ├── GlassPanel.tsx
│   │   ├── GlassButton.tsx
│   │   ├── GlassHeader.tsx
│   │   ├── GlassInput.tsx
│   │   ├── GlassAccordion.tsx
│   │   └── index.ts (экспорты)
│   ├── FavoritesPanel.tsx (обновлен)
│   └── ...
├── hooks/
│   └── useGlassAnimation.ts
├── utils/
│   └── glassStyles.ts
└── styles/
    └── glass.css (или добавить в глобальные стили)
```

---

## ✅ Чеклист финальной проверки

- [ ] Все панели выглядят одинаково
- [ ] Все анимации плавные
- [ ] Все кнопки в едином стиле
- [ ] Все иконки одинакового размера и цвета
- [ ] Клик вне панели закрывает её
- [ ] Панели не мешают взаимодействию с картой
- [ ] Работает на мобильных устройствах
- [ ] Нет проблем с производительностью
- [ ] Код чистый и документированный

---

## 🚀 Бонусные улучшения (опционально)

- [ ] Добавить звуки при открытии/закрытии (тихие, приятные)
- [ ] Сделать плавный переход между панелями
- [ ] Добавить поддержку drag & drop для перемещения панелей
- [ ] Добавить сохранение позиции панелей в localStorage
- [ ] Добавить темную тему для glassmorphism

---

## 📝 Примечания

1. **Производительность:** Использовать `transform` и `opacity` для анимаций (они GPU-ускоренные)
2. **Совместимость:** Проверить поддержку `backdrop-filter` в старых браузерах (может потребоваться fallback)
3. **Accessibility:** Убедиться, что все элементы доступны с клавиатуры
4. **Мобильные устройства:** Проверить, что панели удобны на маленьких экранах

---

**Дата создания:** 2024
**Статус:** План готов к реализации


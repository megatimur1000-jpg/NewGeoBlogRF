# 📱 Офлайн режим создания постов — Документация

## 🎯 Обзор

Система офлайн-постов позволяет пользователям создавать посты полностью офлайн (текст + фото + трек), сохранять их локально в IndexedDB, а при появлении интернета автоматически отправлять на сервер. После модерации пост отображается у всех пользователей корректно, включая изображения и трек.

---

## 🏗️ Архитектура

### Компоненты системы

1. **Frontend (IndexedDB)**
   - `offlinePostsStorage.ts` — обёртка для IndexedDB с CRUD операциями
   - `offlinePostsQueue.ts` — очередь отправки с ретраями и бэкоффом
   - `OfflineDraftsPanel.tsx` — UI компонент для управления черновиками
   - `CreatePostModal.tsx` — модалка создания поста с кнопкой "Сохранить офлайн"

2. **Backend (API)**
   - `offlinePostsController.js` — контроллер для создания и загрузки частей поста
   - `offlinePostsRoutes.js` — роуты для API эндпоинтов
   - Таблица `offline_post_metadata` — метаданные офлайн постов
   - Папка `uploads/offline-posts/` — хранение изображений

---

## 📊 Структура данных

### IndexedDB (Frontend)

**Store: `offline_posts`**

```typescript
interface OfflinePostDraft {
  id: string;                    // UUID
  createdAt: number;             // timestamp
  text: string;                  // Текст поста (может быть JSON для путеводителей)
  images: File[];               // Массив File объектов (НЕ Blob, НЕ base64!)
  track: GeoJSON.Feature | null; // GeoJSON объект трека
  status: 'draft' | 'uploading' | 'failed' | 'failed_permanent';
  retries: number;               // Количество попыток отправки
  regionId: string;             // Идентификатор региона
  hasImages: boolean;           // Флаг наличия изображений
  hasTrack: boolean;            // Флаг наличия трека
}
```

**Индексы:**
- `status` — для фильтрации по статусу
- `createdAt` — для сортировки по дате
- `regionId` — для фильтрации по региону

### База данных (Backend)

**Таблица: `posts`**
- Стандартные поля поста
- `status` — статус поста (`pending_images`, `awaiting_moderation`, `active`, и т.д.)
- `photo_urls` — строка с путями к изображениям (через запятую)

**Таблица: `offline_post_metadata`**
```sql
CREATE TABLE offline_post_metadata (
  post_id UUID PRIMARY KEY REFERENCES posts(id) ON DELETE CASCADE,
  region_id VARCHAR(255),
  has_images BOOLEAN DEFAULT false,
  has_track BOOLEAN DEFAULT false,
  images_uploaded INTEGER DEFAULT 0,
  images_expected INTEGER DEFAULT 0,
  track_uploaded BOOLEAN DEFAULT false,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Таблица: `post_tracks`** (опционально, если нет колонки `payload`)
```sql
CREATE TABLE post_tracks (
  post_id UUID PRIMARY KEY REFERENCES posts(id) ON DELETE CASCADE,
  track_data JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

---

## 🔌 API Эндпоинты

### 1. POST `/api/offline-posts`

Создаёт заглушку поста в БД (без изображений и трека).

**Требования:**
- Авторизация: `Bearer token`
- Content-Type: `application/json`

**Body:**
```json
{
  "text": "Текст поста",
  "regionId": "region-123",
  "hasImages": true,
  "hasTrack": false
}
```

**Response (201):**
```json
{
  "id": "uuid-поста",
  "uploadUrl": "/api/posts/{id}/images"
}
```

**Ошибки:**
- `401` — Требуется авторизация
- `400` — Невалидные данные (нет текста, превышен лимит символов)

---

### 2. POST `/api/posts/:id/images`

Загружает изображения для поста.

**Требования:**
- Авторизация: `Bearer token`
- Content-Type: `multipart/form-data`

**Body:**
- `images[]` — массив файлов (максимум 10, до 10 МБ каждый)

**Response (200):**
```json
{
  "message": "Изображения успешно загружены",
  "uploaded": 3,
  "paths": ["/uploads/offline-posts/abc123/img1.jpg", ...]
}
```

**Ошибки:**
- `401` — Требуется авторизация
- `403` — Нет доступа к посту
- `404` — Пост не найден
- `400` — Невалидные файлы (не JPEG/PNG, превышен лимит)
- `413` — Превышен суммарный размер (50 МБ)

**Особенности:**
- Файлы сохраняются в `uploads/offline-posts/{postId}/{uuid}.{ext}`
- Имена файлов генерируются через `crypto.randomUUID()`
- Проверяется MIME-тип (только `image/jpeg`, `image/png`)

---

### 3. PUT `/api/posts/:id/track`

Загружает трек (GeoJSON) для поста.

**Требования:**
- Авторизация: `Bearer token`
- Content-Type: `application/json`

**Body:**
```json
{
  "track": {
    "type": "Feature",
    "geometry": {
      "type": "LineString",
      "coordinates": [[lon, lat], ...]
    },
    "properties": {}
  }
}
```

**Response (200):**
```json
{
  "message": "Трек успешно загружен",
  "postId": "uuid-поста"
}
```

**Ошибки:**
- `401` — Требуется авторизация
- `403` — Нет доступа к посту
- `404` — Пост не найден
- `400` — Невалидный GeoJSON

**Особенности:**
- Трек сохраняется в колонку `payload` (если есть) или в таблицу `post_tracks`
- После загрузки трека проверяется статус поста

---

### 4. GET `/api/posts/:id/status`

Получает статус поста (для проверки модерации).

**Требования:**
- Авторизация: `Bearer token`
- Доступ: только автор или админ

**Response (200):**
```json
{
  "status": "awaiting_moderation",
  "postId": "uuid-поста"
}
```

---

## 🔄 Логика статусов

### Переходы статусов

1. **Создание поста** → `pending_images` (если ожидаются изображения/трек)
2. **Загрузка всех частей** → `awaiting_moderation` (автоматически)
3. **Модерация** → `active` / `rejected` / `revision`

### Автоматический переход в `awaiting_moderation`

Пост переводится в статус `awaiting_moderation`, когда:
- ✅ Если `has_images = true` → `images_uploaded > 0`
- ✅ Если `has_track = true` → `track_uploaded = true`
- ✅ Если `has_images = false` и `has_track = false` → сразу после создания

Проверка происходит в функции `checkAndUpdatePostStatus()` после каждой загрузки части.

---

## 📤 Очередь отправки (Frontend)

### Механизм работы

1. **Триггеры отправки:**
   - Автоматически: событие `online` (браузер)
   - Вручную: кнопка "Отправить сейчас" в панели черновиков
   - При загрузке страницы: если есть черновики со статусом `draft` или `failed`

2. **Процесс отправки:**
   ```
   draft → uploading → (создание поста) → (загрузка изображений) → (загрузка трека) → удаление из IndexedDB
   ```

3. **Ретраи:**
   - Первая попытка: сразу
   - Вторая попытка: через 1 минуту
   - Остальные попытки: через 5 минут
   - Максимум 5 попыток → статус `failed_permanent`

4. **Обработка ошибок:**
   - Сетевые ошибки → ретрай с задержкой
   - Ошибки валидации → статус `failed_permanent` (не ретраить)
   - Превышен лимит попыток → статус `failed_permanent`

---

## 🎨 UI Компоненты

### 1. `CreatePostModal.tsx`

**Добавлено:**
- Кнопка "Сохранить офлайн" (слева от "Опубликовать")
- Функция `handleSaveOffline()` — сохраняет черновик в IndexedDB

**Логика:**
- Сохраняет `File` объекты напрямую (не загружает на сервер)
- Сохраняет текст поста (включая JSON для путеводителей)
- Устанавливает `status = 'draft'`

---

### 2. `OfflineDraftsPanel.tsx`

**Функционал:**
- Список всех черновиков с превью
- Отображение статуса (draft, uploading, failed)
- Кнопка "Отправить сейчас" (для каждого черновика)
- Кнопка "Удалить" (для каждого черновика)
- Индикатор прогресса загрузки
- Индикатор наличия интернета

**Статусы:**
- 🟦 **draft** — черновик готов к отправке
- 🔵 **uploading** — идёт отправка (с прогресс-баром)
- 🔴 **failed** — ошибка отправки (можно повторить)
- ⛔ **failed_permanent** — превышен лимит попыток

---

### 3. `Posts.tsx`

**Добавлено:**
- Кнопка "Черновики" с бейджем количества (если есть черновики)
- Интеграция `OfflineDraftsPanel`
- Автообновление счётчика черновиков каждые 5 секунд

---

## 🔒 Безопасность

### Frontend

- ✅ Проверка наличия интернета перед отправкой
- ✅ Валидация размера файлов (10 МБ на файл, 50 МБ суммарно)
- ✅ Ограничение количества файлов (максимум 10)
- ✅ Проверка MIME-типа изображений (только JPEG/PNG)

### Backend

- ✅ Проверка авторизации на всех эндпоинтах
- ✅ Проверка принадлежности поста пользователю
- ✅ Валидация MIME-типа (только `image/jpeg`, `image/png`)
- ✅ Генерация безопасных имён файлов (UUID)
- ✅ Ограничение размера файлов (10 МБ)
- ✅ Статическая раздача без листинга директорий

---

## 🧪 Тестирование

### Сценарии для тестирования

1. **Полный офлайн цикл:**
   - Создать пост офлайн (текст + фото + трек)
   - Закрыть браузер
   - Вернуться домой (с интернетом)
   - Проверить автоматическую отправку

2. **Прерванная отправка:**
   - Начать отправку черновика
   - Отключить интернет во время загрузки
   - Проверить статус `failed` и ретрай

3. **Множество черновиков:**
   - Создать 3-5 черновиков
   - Включить интернет
   - Проверить последовательную отправку

4. **Большой объём:**
   - Создать пост с 10 фото (по 9 МБ каждое)
   - Проверить ограничение размера

5. **Модерация:**
   - Отправить офлайн пост
   - Проверить статус `awaiting_moderation`
   - Одобрить через админ-панель
   - Проверить отображение у всех пользователей

---

## 📝 Миграции и очистка

### Очистка старых черновиков

**Автоматическая:**
- Черновики старше 30 дней со статусом `draft` или `failed` удаляются автоматически

**Ручная:**
- Кнопка "Удалить" в панели черновиков

**Миграция localStorage:**
- При первом запуске новой версии очищаются старые черновики из `localStorage`
- Ключи для очистки: `geoblog_guest_drafts_v1`, `geoblog_offline_drafts`, `offline_post_drafts`

---

## ✨ Дополнительные функции

### 1. Превью изображений в панели черновиков
- ✅ Реализовано отображение превью изображений через `URL.createObjectURL()`
- Показываются до 5 превью, остальные отображаются как "+N"
- Автоматическая очистка URL при закрытии панели

### 2. Извлечение трека из маршрута
- ✅ Реализовано автоматическое извлечение GeoJSON из выбранного маршрута
- Работает для простых постов (крючок маршрута) и путеводителей (секции с маршрутами)
- Преобразует точки маршрута в формат GeoJSON LineString

### 3. Получение regionId из геолокации
- ✅ Реализовано определение региона через браузерный Geolocation API
- Определяет регион по координатам (Москва, СПб, Краснодар, default)
- Fallback на `'default'` при ошибке или отсутствии разрешения

### 4. Уведомления о статусе модерации
- ✅ Реализована система уведомлений через `moderationNotifications` сервис
- Визуальные уведомления в правом верхнем углу
- Браузерные уведомления (если разрешено)
- Поддержка статусов: `approved`, `rejected`, `revision`

---

## 📚 Использование

### Для разработчиков

**Создание черновика:**
```typescript
import { offlinePostsStorage } from '../services/offlinePostsStorage';

await offlinePostsStorage.addDraft({
  text: 'Текст поста',
  images: [file1, file2], // File объекты
  track: geoJsonFeature,  // GeoJSON или null
  status: 'draft',
  regionId: 'region-123',
  hasImages: true,
  hasTrack: false
});
```

**Отправка черновика:**
```typescript
import { offlinePostsQueue } from '../services/offlinePostsQueue';

// Автоматически (при появлении интернета)
offlinePostsQueue.start();

// Вручную
await offlinePostsQueue.uploadDraftById(draftId);
```

**Подписка на прогресс:**
```typescript
const unsubscribe = offlinePostsQueue.onProgress((progress) => {
  if (progress) {
    console.log(`Пост ${progress.postId}: ${progress.progress}%`);
  }
});
```

---

## ✅ Чеклист внедрения

- [x] Backend инфраструктура (папки, multer, статика)
- [x] Backend эндпоинты (create, upload images, upload track)
- [x] Логика статусов и переход в awaiting_moderation
- [x] IndexedDB обёртка с миграциями
- [x] Очередь отправки с ретраями
- [x] UI черновиков (панель, кнопки)
- [x] Интеграция в CreatePostModal
- [x] Интеграция в Posts.tsx
- [x] Документация
- [x] Превью изображений в панели черновиков
- [x] Извлечение трека из маршрута
- [x] Получение regionId из геолокации
- [x] Уведомления о статусе модерации
- [ ] Тестирование всех сценариев

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (Frontend)
2. Проверьте логи сервера (Backend)
3. Проверьте IndexedDB через DevTools → Application → IndexedDB
4. Проверьте статус поста через GET `/api/posts/:id/status`

---

**Версия:** 1.0.0  
**Дата:** 2025-01-10  
**Автор:** AI Assistant


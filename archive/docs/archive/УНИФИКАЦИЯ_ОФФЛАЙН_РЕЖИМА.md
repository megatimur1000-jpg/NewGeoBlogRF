# 🔄 Унификация офлайн-режима — Этап 1 (Завершён)

## ✅ Что реализовано

### 1. Универсальное хранилище (`offlineContentStorage.ts`)

**Поддерживает все типы контента:**
- ✅ Посты (`post`)
- ✅ Метки (`marker`)
- ✅ Маршруты (`route`)
- ✅ События (`event`)

**Структура:**
```typescript
interface OfflineContentDraft {
  id: string;
  contentType: 'post' | 'marker' | 'route' | 'event';
  createdAt: number;
  status: 'draft' | 'uploading' | 'failed' | 'failed_permanent';
  retries: number;
  regionId: string;
  contentData: any; // Специфичные данные для каждого типа
  images?: File[]; // Для постов, меток, событий
  hasImages: boolean;
  track?: GeoJSON.Feature; // Для постов и маршрутов
  hasTrack?: boolean;
}
```

**Функционал:**
- ✅ CRUD операции для всех типов
- ✅ Фильтрация по типу и статусу
- ✅ Автоочистка старых черновиков (>30 дней)
- ✅ Статистика по черновикам
- ✅ Миграция из localStorage в IndexedDB

---

### 2. Универсальная очередь отправки (`offlineContentQueue.ts`)

**Поддерживает отправку всех типов:**
- ✅ Посты (с изображениями и треком)
- ✅ Метки (с изображениями)
- ✅ Маршруты (с треком и точками)
- ✅ События (с изображениями)

**Особенности:**
- ✅ Последовательная отправка (1 черновик за раз)
- ✅ Ретраи с бэкоффом (сразу → 1 мин → 5 мин)
- ✅ Максимум 5 попыток → `failed_permanent`
- ✅ Автозапуск при появлении интернета
- ✅ Прогресс отправки для каждого типа

**Логика отправки:**

**Посты:**
1. Создать заглушку (`POST /offline-posts`)
2. Загрузить изображения (`POST /posts/:id/images`)
3. Загрузить трек (`PUT /posts/:id/track`)

**Метки:**
1. Загрузить изображения (`POST /upload/image`)
2. Создать метку (`POST /markers`)

**Маршруты:**
1. Создать маркеры для точек (если нужно)
2. Создать маршрут (`POST /routes`)

**События:**
1. Загрузить изображения (`POST /upload/image`)
2. Создать событие (`POST /events`)

---

### 3. Обратная совместимость

**Адаптеры для постов:**
- ✅ `offlinePostsStorage.ts` → использует `offlineContentStorage`
- ✅ `offlinePostsQueue.ts` → использует `offlineContentQueue`

**Преимущества:**
- Существующий код постов продолжает работать
- Плавный переход на новый API
- Нет breaking changes

---

### 4. Миграция из localStorage

**Автоматическая миграция:**
- ✅ Посты из старого `geoblog_offline_posts`
- ✅ Метки из `localModerationStorage` (pending)
- ✅ События из `localModerationStorage` (pending)
- ✅ Маршруты из `guestDrafts`

**Особенности:**
- Выполняется один раз при первом запуске
- Фото из localStorage не мигрируются (невозможно восстановить File объекты)
- Старые данные остаются в localStorage (не удаляются автоматически)

---

## 📊 Статистика

**Методы для получения статистики:**
```typescript
// Количество черновиков по типу
await offlineContentStorage.getDraftsCount('post');
await offlineContentStorage.getDraftsCount('marker', 'draft');

// Полная статистика
const stats = await offlineContentStorage.getDraftsStats();
// {
//   post: { total: 5, byStatus: { draft: 3, uploading: 1, failed: 1, ... } },
//   marker: { total: 2, byStatus: { ... } },
//   ...
// }
```

---

## 🔌 API

### Хранилище

```typescript
import { offlineContentStorage } from './services/offlineContentStorage';

// Добавить черновик
const id = await offlineContentStorage.addDraft({
  contentType: 'marker',
  contentData: { title: '...', latitude: 55.7, longitude: 37.6, ... },
  images: [file1, file2],
  hasImages: true,
  status: 'draft',
  regionId: 'moscow'
});

// Получить все черновики меток
const markers = await offlineContentStorage.getAllDrafts('marker');

// Получить черновики со статусом draft
const drafts = await offlineContentStorage.getAllDrafts(undefined, 'draft');

// Обновить статус
await offlineContentStorage.updateDraftStatus(id, 'uploading');

// Удалить черновик
await offlineContentStorage.deleteDraft(id);
```

### Очередь

```typescript
import { offlineContentQueue } from './services/offlineContentQueue';

// Подписаться на прогресс
const unsubscribe = offlineContentQueue.onProgress((progress) => {
  if (progress) {
    console.log(`${progress.contentType} ${progress.contentId}: ${progress.progress}%`);
  }
});

// Отправить черновик вручную
await offlineContentQueue.uploadDraftById(draftId);

// Запустить обработку очереди
offlineContentQueue.start();
```

---

## 🎯 Следующие шаги (Этап 2)

Теперь, когда инфраструктура готова, можно:

1. **Добавить временное отображение контента:**
   - Метки на карте с индикатором "На модерации"
   - Маршруты на карте с индикатором
   - События в календаре с индикатором

2. **Расширить UI панель черновиков:**
   - Фильтры по типу контента
   - Превью для меток/маршрутов/событий

3. **Добавить кнопки "Сохранить офлайн":**
   - В форме создания метки
   - В форме сохранения маршрута
   - В форме создания события

---

## ✅ Чеклист Этапа 1

- [x] Создать `offlineContentStorage.ts`
- [x] Создать `offlineContentQueue.ts`
- [x] Реализовать миграцию из localStorage
- [x] Создать адаптеры для обратной совместимости
- [x] Протестировать на ошибки линтера
- [ ] Протестировать создание черновиков всех типов
- [ ] Протестировать отправку черновиков
- [ ] Протестировать миграцию

---

**Дата:** 2025-01-10  
**Статус:** ✅ **ГОТОВО К ТЕСТИРОВАНИЮ**



















# 🛡️ СИСТЕМА МОДЕРАЦИИ КОНТЕНТА

## 📋 ОБЩАЯ СТРУКТУРА

### Статусы модерации (единые для всех типов контента):
- `pending` - ожидает модерации (создано, но не проверено)
- `active` - одобрено и опубликовано
- `rejected` - отклонено модератором
- `hidden` - скрыто (не публикуется, но не удалено)
- `draft` - черновик (не отправлено на модерацию)

### Типы контента для модерации:
1. **События (Events)** ✅ Частично реализовано
2. **Посты (Posts)** ❌ Не реализовано
3. **Маршруты (Routes)** ❌ Не реализовано
4. **Метки/Маркеры (Markers)** ❌ Не реализовано
5. **Блоги (Blogs)** ❌ Не реализовано
6. **Комментарии (Comments)** ❌ Не реализовано
7. **Обсуждения/Чаты (Chats)** ❌ Не реализовано

---

## 1. СОБЫТИЯ (EVENTS)

### Поля в БД:
- `status` - статус модерации (pending/active/rejected/hidden)
- `is_public` - публичность (true/false)
- `creator_id` - ID создателя (null для гостей)

### Логика:
- Гости создают события со статусом `pending`
- При регистрации события отправляются на модерацию
- Админ может одобрить (`active`) или отклонить (`rejected`)

### API Endpoints:
- `GET /api/events/pending` - получить события на модерации
- `POST /api/events/:id/approve` - одобрить событие
- `POST /api/events/:id/reject` - отклонить событие

### Что проверяется:
- Название и описание
- Фотографии
- Местоположение (в пределах РФ)
- Дата и время
- Категория

---

## 2. ПОСТЫ (POSTS)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения (опционально)
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Посты создаются со статусом `pending` (для гостей) или `active` (для авторизованных)
- Админ может одобрить, отклонить или скрыть

### API Endpoints (нужно создать):
- `GET /api/posts/pending` - получить посты на модерации
- `POST /api/posts/:id/approve` - одобрить пост
- `POST /api/posts/:id/reject` - отклонить пост
- `POST /api/posts/:id/hide` - скрыть пост

### Что проверяется:
- Текст поста
- Фотографии
- Геолокация
- Ссылки
- Теги

---

## 3. МАРШРУТЫ (ROUTES)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Маршруты создаются со статусом `pending` (для гостей) или `active` (для авторизованных)
- Админ проверяет маршрут и точки на карте

### API Endpoints (нужно создать):
- `GET /api/routes/pending` - получить маршруты на модерации
- `POST /api/routes/:id/approve` - одобрить маршрут
- `POST /api/routes/:id/reject` - отклонить маршрут

### Что проверяется:
- Название и описание
- Точки маршрута (в пределах РФ)
- Фотографии
- Категория

---

## 4. МЕТКИ/МАРКЕРЫ (MARKERS)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Метки создаются со статусом `pending` (для гостей) или `active` (для авторизованных)
- Админ проверяет местоположение и описание

### API Endpoints (нужно создать):
- `GET /api/markers/pending` - получить метки на модерации
- `POST /api/markers/:id/approve` - одобрить метку
- `POST /api/markers/:id/reject` - отклонить метку

### Что проверяется:
- Название и описание
- Координаты (в пределах РФ)
- Фотографии
- Категория
- Тип метки

---

## 5. БЛОГИ (BLOGS)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Блоги создаются со статусом `pending` (для гостей) или `active` (для авторизованных)
- Админ проверяет контент блога

### API Endpoints (нужно создать):
- `GET /api/blogs/pending` - получить блоги на модерации
- `POST /api/blogs/:id/approve` - одобрить блог
- `POST /api/blogs/:id/reject` - отклонить блог

### Что проверяется:
- Заголовок и текст
- Фотографии
- Маршруты в блоге
- Ссылки

---

## 6. КОММЕНТАРИИ (COMMENTS)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Комментарии создаются со статусом `pending` (для гостей) или `active` (для авторизованных)
- Автоматическая модерация через ModerationService
- Админ может одобрить или отклонить

### API Endpoints (нужно создать):
- `GET /api/comments/pending` - получить комментарии на модерации
- `POST /api/comments/:id/approve` - одобрить комментарий
- `POST /api/comments/:id/reject` - отклонить комментарий

### Что проверяется:
- Текст комментария
- Спам-слова
- Неподходящий контент
- Ссылки

---

## 7. ОБСУЖДЕНИЯ/ЧАТЫ (CHATS)

### Поля в БД (нужно добавить):
- `status` - статус модерации
- `moderation_reason` - причина отклонения
- `moderated_at` - дата модерации
- `moderated_by` - ID модератора

### Логика:
- Сообщения в чатах проверяются автоматически
- Админ может скрыть сообщение или заблокировать пользователя

### API Endpoints (нужно создать):
- `GET /api/chats/messages/pending` - получить сообщения на модерации
- `POST /api/chats/messages/:id/approve` - одобрить сообщение
- `POST /api/chats/messages/:id/reject` - отклонить сообщение

### Что проверяется:
- Текст сообщения
- Спам
- Неподходящий контент
- Ссылки

---

## 🎯 ЕДИНАЯ ПАНЕЛЬ МОДЕРАЦИИ

### Структура вкладок:
1. **События** - EventsModerationPanel ✅
2. **Посты** - PostsModerationPanel ❌
3. **Маршруты** - RoutesModerationPanel ❌
4. **Метки** - MarkersModerationPanel ❌
5. **Блоги** - BlogsModerationPanel ❌
6. **Комментарии** - CommentsModerationPanel ❌
7. **Чаты** - ChatsModerationPanel ❌

### Общие функции для всех панелей:
- Просмотр списка контента на модерации
- Фильтрация по статусу
- Одобрение контента
- Отклонение контента (с указанием причины)
- Скрытие контента
- Просмотр деталей контента

---

## 📊 СТАТИСТИКА МОДЕРАЦИИ

### Общая статистика (для всех типов):
- Всего на модерации
- Одобрено сегодня
- Отклонено сегодня
- Скрыто сегодня

### Статистика по типам:
- События: X на модерации
- Посты: X на модерации
- Маршруты: X на модерации
- Метки: X на модерации
- Блоги: X на модерации
- Комментарии: X на модерации
- Чаты: X на модерации

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Backend:
1. Добавить поле `status` во все таблицы контента
2. Создать единый middleware для проверки прав админа
3. Создать контроллеры модерации для каждого типа
4. Создать роуты для модерации

### Frontend:
1. Создать компоненты модерации для каждого типа
2. Обновить ModerationPanel с вкладками
3. Создать сервисы для работы с модерацией
4. Добавить статистику на главную страницу админ-панели

---

## ✅ ПЛАН РЕАЛИЗАЦИИ

### Этап 1: События (уже частично сделано)
- [x] Добавить статус в БД
- [x] Создать API endpoints
- [x] Создать EventsModerationPanel
- [ ] Добавить причину отклонения
- [ ] Добавить историю модерации

### Этап 2: Посты
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать PostsModerationPanel
- [ ] Интегрировать в ModerationPanel

### Этап 3: Маршруты
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать RoutesModerationPanel
- [ ] Интегрировать в ModerationPanel

### Этап 4: Метки
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать MarkersModerationPanel
- [ ] Интегрировать в ModerationPanel

### Этап 5: Блоги
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать BlogsModerationPanel
- [ ] Интегрировать в ModerationPanel

### Этап 6: Комментарии
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать CommentsModerationPanel
- [ ] Интегрировать в ModerationPanel

### Этап 7: Чаты
- [ ] Добавить статус в БД
- [ ] Создать API endpoints
- [ ] Создать ChatsModerationPanel
- [ ] Интегрировать в ModerationPanel

---

## 📝 ПРИМЕЧАНИЯ

- Все контент, созданный гостями, автоматически получает статус `pending`
- При регистрации весь контент гостя отправляется на модерацию
- Только пользователи с ролью `admin` могут модерировать контент
- История модерации сохраняется для аудита
- Причина отклонения обязательна при отклонении контента


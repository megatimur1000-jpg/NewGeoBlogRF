# 📋 План реализации системы модерации

## 🎯 Цель
Реализовать систему модерации, где весь контент сохраняется локально до одобрения админом, после чего попадает в БД с начислением очков.

## 📦 Компоненты системы

### 1. Локальное хранение контента на модерации
**Файл:** `frontend/src/services/localModerationStorage.ts`

**Функционал:**
- Сохранение контента в localStorage с ключом `pending_${contentType}_${id}`
- Получение всего контента на модерации по типу
- Удаление после одобрения/отклонения
- Структура данных:
  ```typescript
  {
    id: string;
    type: 'marker' | 'post' | 'event' | 'complaint' | 'suggestion';
    data: any;
    created_at: string;
    author_id?: string;
    author_name?: string;
    ai_analysis?: {
      rating: number;
      confidence: number;
      reason: string;
      suggestion: 'approve' | 'reject' | 'review';
    };
  }
  ```

### 2. Обновление создания контента

#### 2.1 Метки (`map.tsx`)
- При создании метки сохранять в `localModerationStorage`
- Показывать в попапе сообщение "⏳ На модерации"
- Метка видна только создателю до одобрения

#### 2.2 Посты (`posts.tsx`)
- При создании поста сохранять в `localModerationStorage`
- Показывать в карточке поста бейдж "На модерации"
- Пост виден только автору до одобрения

#### 2.3 События (`calendar.tsx`)
- При создании события сохранять в `localModerationStorage`
- Показывать в карточке события бейдж "На модерации"
- Событие видно только создателю до одобрения

### 3. Компонент модального окна модерации для админа

**Файл:** `frontend/src/components/Moderation/AdminModerationModal.tsx`

**Функционал:**
- Отображается только для админов
- Показывает список всего контента на модерации
- Группировка по типам контента
- Для каждого элемента:
  - Показ данных контента
  - ИИ-рекомендация (если есть)
  - Кнопки: Одобрить / Отклонить / На доработку
- При одобрении:
  - Контент отправляется в БД
  - Начисляются очки автору
  - Контент удаляется из localStorage
  - Обновляется UI

### 4. Интеграция на страницы

#### 4.1 Map.tsx
- Кнопка "Модерация" справа (только для админа)
- Модальное окно с метками на модерации
- При клике на метку - центрирование карты

#### 4.2 Posts.tsx
- Кнопка "Модерация" (только для админа)
- Модальное окно с постами на модерации
- При клике на пост - переход к посту

#### 4.3 Calendar.tsx
- Кнопка "Модерация" (только для админа)
- Модальное окно с событиями на модерации
- При клике на событие - показ на календаре

#### 4.4 Planner.tsx
- Кнопка "Модерация" (только для админа)
- Модальное окно с маршрутами на модерации

### 5. Скрытие админ панели от не-админов

**Файлы:**
- `frontend/src/components/Sidebar.tsx` (или где сайдбар)
- Проверка роли пользователя перед показом кнопки "Админ панель"

### 6. ИИ-анализ контента

**Файл:** `frontend/src/services/aiModerationService.ts`

**Функционал:**
- При сохранении контента локально вызывать ИИ-анализ
- Сохранять результат анализа вместе с контентом
- Показывать админу рекомендации ИИ

### 7. API для одобрения контента

**Файл:** `backend/src/routes/moderationRoutes.js`

**Новые endpoints:**
- `POST /api/moderation/approve-local` - одобрить локальный контент
  - Принимает: `{ contentType, localId, contentData }`
  - Сохраняет в БД
  - Начисляет очки
  - Возвращает созданный контент

### 8. Обновление админ панели

**Файл:** `frontend/src/components/Admin/AdminDashboard.tsx`

**Функционал:**
- Только счётчики контента на модерации (из localStorage)
- История модерации (одобренные/отклонённые)
- Без детального просмотра контента (это в модальных окнах на страницах)

## 🔄 Процесс работы

1. **Пользователь создаёт контент:**
   - Контент сохраняется в localStorage
   - Вызывается ИИ-анализ (асинхронно)
   - Показывается сообщение "На модерации"
   - Контент виден только автору

2. **Админ открывает страницу:**
   - Видит кнопку "Модерация"
   - Открывает модальное окно
   - Видит весь контент на модерации с ИИ-рекомендациями

3. **Админ модерирует:**
   - Одобряет → контент в БД, очки начислены, удалён из localStorage
   - Отклоняет → контент удалён из localStorage, автор уведомлён
   - На доработку → контент остаётся, автор видит причину

4. **После одобрения:**
   - Контент появляется в БД
   - Виден всем пользователям
   - Автор получает очки и опыт

## 📝 Структура файлов

```
frontend/src/
├── services/
│   ├── localModerationStorage.ts (новый)
│   └── aiModerationService.ts (новый)
├── components/
│   └── Moderation/
│       ├── AdminModerationModal.tsx (новый)
│       └── ModerationBadge.tsx (новый - бейдж "На модерации")
└── pages/
    ├── Map.tsx (обновить)
    ├── Posts.tsx (обновить)
    ├── Calendar.tsx (обновить)
    └── Planner.tsx (обновить)

backend/src/
└── routes/
    └── moderationRoutes.js (обновить - добавить approve-local)
```

## ✅ Критерии готовности

- [ ] Контент сохраняется локально при создании
- [ ] Пользователь видит "На модерации" в UI
- [ ] Админ видит модальное окно на всех страницах
- [ ] ИИ анализирует контент перед показом админу
- [ ] Одобрение сохраняет в БД и начисляет очки
- [ ] Админ панель скрыта от не-админов
- [ ] Кнопка "Админ панель" в сайдбаре только для админов


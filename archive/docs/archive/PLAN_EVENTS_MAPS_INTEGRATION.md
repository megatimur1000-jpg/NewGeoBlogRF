# План реализации интеграции событий с картами

## 🎯 Цель
Создать единую экосистему, где события из календаря автоматически отображаются на картах (map.tsx и planner.tsx), позволяя пользователю видеть расположение событий, находить магазины, пути подъезда, строить маршруты.

---

## 📋 Этап 1: Реорганизация архитектуры панелей

### 1.1. Перемещение календаря в правую панель
**Файлы:**
- `frontend/src/pages/PageLayer.tsx`
- `frontend/src/stores/contentStore.ts`

**Задачи:**
- [ ] Переместить `'calendar'` из `leftPages` в `rightPages` в `PageLayer.tsx`
- [ ] Обновить логику в `contentStore.ts` для поддержки календаря как правого контента
- [ ] Убедиться, что календарь может быть единственным активным контентом (полный экран)

**Ожидаемый результат:**
- Календарь открывается в правой панели
- При закрытии левой панели календарь занимает весь экран

---

### 1.2. Обновление Sidebar для подкнопок календаря
**Файлы:**
- `frontend/src/components/Sidebar.tsx`

**Задачи:**
- [ ] Добавить состояние для отслеживания активного календаря
- [ ] При активном календаре показывать подкнопки (только когда календарь активен):
  - "+ карта" (текстовая кнопка, на отдельной строке)
  - "+ планировщик" (текстовая кнопка, на отдельной строке ниже)
- [ ] Подкнопки неактивны/скрыты пока календарь не активен
- [ ] Визуально выделить подкнопки (отступ слева, стиль как у обычных кнопок)
- [ ] Реализовать логику переключения:
  - Пользователь нажимает на календарь → календарь становится активным, появляются подкнопки
  - Клик на "+ карта" → `leftContent = 'map'`, `rightContent = 'calendar'` (оба визуально выделены в сайдбаре)
  - Клик на "+ планировщик" → `leftContent = 'planner'`, `rightContent = 'calendar'` (оба визуально выделены в сайдбаре)
  - Повторный клик на активную подкнопку закрывает левую панель, календарь остается справа (полный экран)

**Ожидаемый результат:**
- В сайдбаре при активном календаре появляются подкнопки "+ карта" и "+ планировщик"
- Пользователь может комбинировать календарь с картой или планировщиком
- Оба контента визуально выделяются в сайдбаре при активной комбинации

---

## 📋 Этап 2: Создание системы синхронизации событий с картами

### 2.1. Создание Store для событий
**Файлы:**
- `frontend/src/stores/eventsStore.ts` (новый файл)

**Структура store:**
```typescript
interface EventsState {
  // Открытые события (из календаря)
  openEvents: Event[];
  
  // Выбранное событие (для детального просмотра)
  selectedEvent: Event | null;
  
  // События, отображаемые на карте
  mapEvents: Event[];
  
  // Методы
  setOpenEvents: (events: Event[]) => void;
  addOpenEvent: (event: Event) => void;
  removeOpenEvent: (eventId: string) => void;
  setSelectedEvent: (event: Event | null) => void;
  clearOpenEvents: () => void;
}
```

**Задачи:**
- [ ] Создать Zustand store для управления событиями
- [ ] Реализовать методы для добавления/удаления открытых событий
- [ ] Добавить подписку на изменения для автоматического обновления карт

**Ожидаемый результат:**
- Централизованное управление состоянием событий
- Автоматическая синхронизация между календарем и картами

---

### 2.2. Расширение типа Event для координат
**Файлы:**
- `frontend/src/components/TravelCalendar/mockEvents.ts`
- `frontend/src/types/event.ts`

**Задачи:**
- [ ] Добавить обязательные поля `latitude` и `longitude` в `MockEvent`
- [ ] Обновить существующие mock события с координатами
- [ ] Убедиться, что при создании события координаты сохраняются

**Ожидаемый результат:**
- Все события имеют координаты
- События можно отображать на карте

---

### 2.3. Интеграция событий в Calendar.tsx
**Файлы:**
- `frontend/src/pages/Calendar.tsx`

**Задачи:**
- [ ] При клике на событие в календаре добавлять его в `eventsStore.openEvents`
- [ ] При открытии события устанавливать `selectedEvent` в store
- [ ] При закрытии модального окна событий очищать `selectedEvent`, но оставлять в `openEvents`
- [ ] Добавить визуальную индикацию, что событие открыто на карте
- [ ] **ВАЖНО:** НЕ открывать карту/планировщик автоматически при клике на событие
- [ ] Пользователь может работать только с календарем, открывая карту только по выбору через подкнопки в сайдбаре

**Ожидаемый результат:**
- Клик на событие в календаре добавляет его в store, но НЕ открывает карту автоматически
- Пользователь сам решает, когда открыть карту через подкнопки в сайдбаре
- Пользователь видит, какие события отображаются на карте (когда карта открыта)

---

## 📋 Этап 3: Отображение событий на картах

### 3.1. Создание компонента EventMarker
**Файлы:**
- `frontend/src/components/Map/EventMarker.tsx` (новый файл)
- `frontend/src/components/Map/EventMarker.css` (новый файл)

**Задачи:**
- [ ] Создать визуально отличимый маркер для событий
- [ ] Использовать иконку календаря (`fa-calendar`)
- [ ] Добавить цветовую индикацию по категории события
- [ ] Реализовать анимацию пульсации для активных/выбранных событий
- [ ] Круглая форма с иконкой календаря внутри
- [ ] Цвет фона зависит от категории события
- [ ] Белая обводка для выделения (2px)
- [ ] Тень для глубины

**Дизайн маркера:**
- Форма: Круглая
- Размер: 40x40px (базовый), 50x50px (выбранный)
- Иконка: `fa-calendar` (FontAwesome)
- Цвета по категориям (как в календаре)
- Обводка: Белая, 2px
- Тень: `0 4px 12px rgba(0,0,0,0.15)`
- Анимация: Пульсация для выбранного события (scale 1.0 → 1.1)

**Ожидаемый результат:**
- События визуально отличаются от обычных маркеров
- Пользователь легко находит события на карте

---

### 3.2. Интеграция событий в Map.tsx
**Файлы:**
- `frontend/src/components/Map/Map.tsx`
- `frontend/src/pages/Map.tsx`

**Задачи:**
- [ ] Подписаться на `eventsStore.openEvents` в Map.tsx
- [ ] Преобразовать события в формат маркеров для карты
- [ ] Добавить маркеры событий на карту через `mapFacade.addMarker()`
- [ ] Реализовать клик по маркеру события:
  - Открыть попап с информацией о событии
  - Установить `selectedEvent` в store
  - Центрировать карту на событии
- [ ] При удалении события из `openEvents` удалять маркер с карты
- [ ] При изменении координат события обновлять позицию маркера

**Ожидаемый результат:**
- События автоматически появляются на карте при открытии в календаре
- Клик по маркеру события показывает детальную информацию

---

### 3.3. Интеграция событий в Planner.tsx
**Файлы:**
- `frontend/src/pages/Planner.tsx`

**Задачи:**
- [ ] Подписаться на `eventsStore.openEvents` в Planner.tsx
- [ ] Добавить события как точки маршрута (опционально)
- [ ] Реализовать возможность построить маршрут к событию:
  - Кнопка "Построить маршрут к событию" в попапе события
  - Добавление события как точки маршрута
- [ ] Отображать события на карте планировщика с тем же стилем, что и в Map.tsx
- [ ] Позволить перетаскивать события в маршрут

**Ожидаемый результат:**
- События отображаются на карте планировщика
- Можно строить маршруты к событиям или от них

---

## 📋 Этап 4: Мини-попапы событий на карте

### 4.1. Создание компонента EventMiniPopup
**Файлы:**
- `frontend/src/components/Map/EventMiniPopup.tsx` (новый файл)
- `frontend/src/components/Map/EventMiniPopup.css` (новый файл)

**Задачи:**
- [ ] Создать мини-попап для событий на основе существующего `MiniMarkerPopup.tsx`
- [ ] Использовать тот же стиль и форму, что у стандартных мини-попапов
- [ ] Добавить эффект прозрачного стекла (glassmorphism) как у выдвигающихся блоков на картах
- [ ] Отображать:
  - Иконка события (календарь) с цветом категории
  - Название события
  - Категория события
  - Рейтинг в звездах (*****)
- [ ] Кнопка "Перейти" (активна только на map.tsx, в planner.tsx не показывать)
- [ ] При клике на "Перейти" открывать модальное окно с полной карточкой события (БЕЗ перехода на календарь)
- [ ] Модальное окно должно иметь кнопку закрытия

**Структура мини-попапа:**
- Основа: `MiniMarkerPopup` компонент
- Стиль: Прозрачное стекло (backdrop-filter: blur, rgba фон)
- Содержимое: Название, категория, рейтинг
- Кнопка: "Перейти" (только на map.tsx)

**Ожидаемый результат:**
- Мини-попап события выглядит как стандартные мини-попапы, но с эффектом стекла
- При клике на "Перейти" открывается модальное окно с полной информацией о событии

---

### 4.2. Создание модального окна EventModal
**Файлы:**
- `frontend/src/components/Events/EventModal.tsx` (новый файл)
- `frontend/src/components/Events/EventModal.css` (новый файл)

**Задачи:**
- [ ] Создать модальное окно для отображения полной информации о событии
- [ ] Использовать стиль glassmorphism (как у панелей в календаре)
- [ ] Отображать:
  - Название события
  - Полное описание
  - Дата и время
  - Категория
  - Рейтинг
  - Фото (если есть)
  - Хэштеги
  - Кнопка "Добавить в избранное"
  - Кнопка "Закрыть"
- [ ] Реализовать плавное открытие/закрытие
- [ ] При закрытии возвращаться к карте (не переходить на календарь)

**Ожидаемый результат:**
- Красивое модальное окно с полной информацией о событии
- Пользователь может просмотреть событие, не покидая карту

---

### 4.3. Интеграция EventMiniPopup в Map.tsx и Planner.tsx
**Файлы:**
- `frontend/src/components/Map/Map.tsx`
- `frontend/src/pages/Planner.tsx`

**Задачи:**
- [ ] При hover на маркер события показывать `EventMiniPopup` (как у стандартных маркеров)
- [ ] При клике на маркер события:
  - На map.tsx: показывать мини-попап с активной кнопкой "Перейти"
  - На planner.tsx: показывать мини-попап без кнопки "Перейти"
- [ ] Позиционировать мини-попап рядом с маркером (как у стандартных маркеров)
- [ ] Реализовать закрытие мини-попапа при движении карты или зуме
- [ ] При клике на "Перейти" (map.tsx) открывать `EventModal`

**Ожидаемый результат:**
- Мини-попап события появляется при hover/клике на маркер
- На map.tsx доступна кнопка "Перейти" для просмотра полной информации
- Пользователь может взаимодействовать с событием прямо на карте

---

## 📋 Этап 5: Логика переключения режимов

### 5.1. Реализация логики переключения режимов
**Файлы:**
- `frontend/src/components/Sidebar.tsx`
- `frontend/src/stores/contentStore.ts`

**Задачи:**
- [ ] **ВАЖНО:** НЕ открывать карту/планировщик автоматически при клике на событие
- [ ] При открытии события в календаре:
  - Событие добавляется в `eventsStore.openEvents`
  - Карта/планировщик НЕ открываются автоматически
  - Пользователь сам решает, когда открыть карту через подкнопки в сайдбаре
- [ ] При закрытии карты/планировщика:
  - Если календарь активен → календарь остается справа (полный экран)
  - События остаются в store, но не отображаются на карте (т.к. карта закрыта)
- [ ] При открытии ленты активности/постов:
  - Если календарь активен → календарь слева, лента/посты справа
  - Если календарь не активен → лента/посты занимают весь экран
- [ ] При открытии карты/планировщика через подкнопки:
  - Если есть открытые события в store → они автоматически отображаются на карте
  - Если событий нет → карта открывается пустой

**Ожидаемый результат:**
- Пользователь полностью контролирует, когда открывать карту
- Плавные переходы между режимами
- Логичное поведение при открытии/закрытии панелей

---

### 5.2. Сохранение состояния при переключении
**Файлы:**
- `frontend/src/stores/contentStore.ts`
- `frontend/src/stores/eventsStore.ts`

**Задачи:**
- [ ] Сохранять открытые события при переключении между картой и планировщиком
- [ ] Восстанавливать выбранное событие при возврате к календарю
- [ ] Не терять состояние карты (zoom, center) при переключении

**Ожидаемый результат:**
- Пользователь не теряет контекст при переключении режимов

---

## 📋 Этап 6: Дополнительные функции

### 6.1. Полный функционал карт для событий
**Файлы:**
- `frontend/src/components/Map/Map.tsx`
- `frontend/src/pages/Planner.tsx`

**Задачи:**
- [ ] **ВАЖНО:** При открытии карты с событиями доступен ВЕСЬ функционал карт
- [ ] Пользователь может использовать все инструменты поиска мест
- [ ] Пользователь может искать магазины, рестораны, отели, парковки и т.д. (любые категории)
- [ ] Пользователь может использовать все функции планировщика
- [ ] Никаких ограничений на функционал карт при работе с событиями
- [ ] События просто добавляются как дополнительные маркеры на карту

**Ожидаемый результат:**
- Полный функционал карт доступен при работе с событиями
- Пользователь может использовать все инструменты для поиска мест и построения маршрутов

---

### 6.2. Добавление событий в избранное
**Файлы:**
- `frontend/src/components/Map/EventCard.tsx`
- `frontend/src/contexts/FavoritesContext.tsx`

**Задачи:**
- [ ] Интегрировать кнопку "Добавить в избранное" в EventCard
- [ ] Сохранять событие в избранное с координатами
- [ ] Отображать избранные события в календаре с особым стилем
- [ ] Позволить удалять события из избранного

**Ожидаемый результат:**
- Пользователь может сохранять интересные события

---

### 6.3. Экспорт событий в маршрут
**Файлы:**
- `frontend/src/pages/Planner.tsx`
- `frontend/src/components/Map/EventCard.tsx`

**Задачи:**
- [ ] Добавить кнопку "Добавить в маршрут" в карточку события (только в planner)
- [ ] При клике добавлять событие как точку маршрута
- [ ] Позволить добавлять несколько событий в один маршрут
- [ ] Автоматически строить оптимальный маршрут между событиями

**Ожидаемый результат:**
- Пользователь может создавать маршруты из событий

---

## 📋 Этап 7: Тестирование и оптимизация

### 7.1. Тестирование функциональности
**Задачи:**
- [ ] Проверить открытие события в календаре → появление на карте
- [ ] Проверить переключение между картой и планировщиком с открытыми событиями
- [ ] Проверить закрытие карты → календарь на полный экран
- [ ] Проверить построение маршрута к событию
- [ ] Проверить добавление в избранное
- [ ] Проверить поиск мест рядом с событием

---

### 7.2. Оптимизация производительности
**Задачи:**
- [ ] Оптимизировать рендеринг множества маркеров событий
- [ ] Использовать кластеризацию маркеров при большом количестве
- [ ] Ленивая загрузка карточек событий
- [ ] Мемоизация вычислений координат

---

## 📋 Этап 8: Визуальные улучшения

### 8.1. Анимации и переходы
**Задачи:**
- [ ] Плавное появление маркеров событий на карте
- [ ] Анимация открытия карточки события
- [ ] Плавные переходы при переключении режимов
- [ ] Индикация загрузки при поиске мест

---

### 8.2. Адаптивность
**Задачи:**
- [ ] Адаптировать карточки событий для мобильных устройств
- [ ] Оптимизировать отображение маркеров на маленьких экранах
- [ ] Улучшить навигацию в мобильной версии

---

## 🎨 Визуальный дизайн маркеров и попапов событий

### Стиль маркера события:
- **Форма:** Круглая с иконкой календаря
- **Размер:** 40x40px (базовый), 50x50px (выбранный)
- **Иконка:** `fa-calendar` (FontAwesome)
- **Цвета по категориям:** Использовать цвета из `categories` в TravelCalendar
- **Обводка:** Белая, 2px
- **Тень:** `0 4px 12px rgba(0,0,0,0.15)`
- **Анимация:** Пульсация для выбранного события (scale 1.0 → 1.1)

### Стиль мини-попапа события:
- **Основа:** Тот же стиль, что у `MiniMarkerPopup`
- **Эффект стекла:** 
  - `background: rgba(255, 255, 255, 0.1)`
  - `backdrop-filter: blur(10px) saturate(180%)`
  - `border: 1px solid rgba(255, 255, 255, 0.2)`
- **Содержимое:**
  - Иконка события (календарь) с цветом категории
  - Название события
  - Категория события
  - Рейтинг в звездах (*****)
- **Кнопка "Перейти":** Только на map.tsx, стиль как у стандартной кнопки мини-попапа

### Стиль модального окна события:
- **Основа:** Glassmorphism (как у панелей в календаре)
- **Содержимое:** Полная информация о событии
- **Кнопка закрытия:** В правом верхнем углу
- **Анимация:** Плавное появление/исчезновение

---

## 📝 Примечания

1. **Обратная совместимость:** Все изменения должны быть обратно совместимы. Существующий функционал не должен сломаться.

2. **Производительность:** При большом количестве событий использовать виртуализацию и кластеризацию.

3. **Доступность:** Все интерактивные элементы должны быть доступны с клавиатуры и иметь ARIA-метки.

4. **Мобильная версия:** Учесть особенности мобильной версии при реализации.

---

## ✅ Чеклист готовности

- [ ] Календарь перемещен в правую панель
- [ ] Подкнопки в сайдбаре работают
- [ ] События отображаются на карте
- [ ] События отображаются в планировщике
- [ ] Карточки событий работают
- [ ] Построение маршрутов к событиям работает
- [ ] Поиск мест рядом с событиями работает
- [ ] Добавление в избранное работает
- [ ] Все переходы между режимами плавные
- [ ] Мобильная версия адаптирована
- [ ] Производительность оптимизирована
- [ ] Тестирование завершено

---

## 🚀 Порядок реализации (рекомендуемый)

1. **Этап 1** - Реорганизация архитектуры (фундамент)
2. **Этап 2** - Система синхронизации (данные)
3. **Этап 3** - Отображение на картах (визуализация)
4. **Этап 4** - Карточки событий (интерактивность)
5. **Этап 5** - Логика переключения (UX)
6. **Этап 6** - Дополнительные функции (расширенный функционал)
7. **Этап 7** - Тестирование (качество)
8. **Этап 8** - Визуальные улучшения (полировка)

---

**Дата создания:** 2025-01-27
**Статус:** План готов к реализации


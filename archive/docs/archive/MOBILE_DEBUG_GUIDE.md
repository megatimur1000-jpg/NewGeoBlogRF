# Руководство по отладке мобильной версии

## Проблема
В мобильной версии не загружаются `Map.tsx` и `Planner.tsx`.

## Что было сделано

### 1. Улучшена логика определения lazy компонентов
- Упрощена проверка lazy компонентов в `ConditionalPage.tsx`
- Добавлена функция `isLazyComponent()` для более точного определения

### 2. Добавлено подробное логирование
- **`useIsMobile`**: Логирует ширину окна и определение мобильной версии
- **`ConditionalPage`**: Логирует выбор между мобильной/десктопной версией
- **`MapPage`**: Логирует монтирование компонента
- **`PlannerPage`**: Логирует монтирование компонента

### 3. Визуальные индикаторы
- Добавлены спиннеры загрузки для мобильных компонентов
- Улучшены сообщения о загрузке

## Как проверить

### Шаг 1: Откройте консоль браузера (F12)

### Шаг 2: Перезагрузите страницу

### Шаг 3: Проверьте логи

Вы должны увидеть последовательность логов:

```
📱 useIsMobile: Инициализация { width: 412, isMobile: true, userAgent: "..." }
📱 ConditionalPage: { isMobile: true, mobileType: "object", mobileIsLazy: true, ... }
✅ ConditionalPage: Мобильная версия, загружаем мобильный компонент
⏳ ConditionalPage: Мобильный компонент lazy, используем Suspense
🗺️ MapPage: Компонент смонтирован! { pathname: "/map", windowWidth: 412, isMobile: true }
```

### Шаг 4: Перейдите на страницу карты (`/map`)

В консоли должны появиться логи:
- `📱 ConditionalPage:` - выбор версии
- `🗺️ MapPage: Компонент смонтирован!` - компонент загружен

### Шаг 5: Перейдите на страницу планировщика (`/planner`)

В консоли должны появиться логи:
- `📱 ConditionalPage:` - выбор версии
- `🗺️ PlannerPage: Компонент смонтирован!` - компонент загружен

## Возможные проблемы

### Проблема 1: `isMobile: false` при ширине < 768px
**Причина**: `useIsMobile` неправильно определяет мобильную версию
**Решение**: Проверьте логи `useIsMobile: Инициализация` - должно быть `isMobile: true`

### Проблема 2: Компонент не монтируется
**Причина**: Ошибка при загрузке lazy компонента
**Решение**: Проверьте вкладку "Network" в DevTools - должен быть запрос на загрузку компонента

### Проблема 3: Показывается только спиннер
**Причина**: Компонент загружается, но не рендерится
**Решение**: Проверьте логи `MapPage: Компонент смонтирован!` - если его нет, компонент не загрузился

### Проблема 4: Показывается десктопная версия
**Причина**: `isMobile` определяется как `false`
**Решение**: 
1. Проверьте ширину окна в логах
2. Убедитесь, что используете мобильный режим в DevTools (Ctrl+Shift+M)
3. Проверьте, что ширина < 768px

## Что проверить в коде

### 1. Правильность импортов в `App.tsx`
```typescript
const MobileMapPage = lazy(() => import('./pages/Mobile/MapPage'));
const MobilePlannerPage = lazy(() => import('./pages/Mobile/PlannerPage'));
```

### 2. Правильность маршрутов
```typescript
<Route path="map" element={
  <ConditionalPage 
    mobile={MobileMapPage} 
    desktop={PersistentMaps} 
  />
} />
```

### 3. Правильность экспортов
- `MapPage.tsx` должен экспортировать `export default MapPage;`
- `PlannerPage.tsx` должен экспортировать `export default PlannerPage;`

## Следующие шаги

Если логи показывают, что компоненты загружаются, но не отображаются:
1. Проверьте стили - возможно, компонент скрыт
2. Проверьте `MobileLayout` - возможно, он блокирует рендеринг
3. Проверьте ошибки в консоли - возможно, есть ошибка рендеринга

Если логи не появляются:
1. Проверьте, что вы в мобильном режиме (ширина < 768px)
2. Проверьте, что маршруты правильно настроены
3. Проверьте, что компоненты правильно экспортируются


# 🎯 ПРАВИЛЬНОЕ ПОВЕДЕНИЕ САЙДБАРА И ДВУХОКОННОГО РЕЖИМА

## 📋 ОПИСАНИЕ ПРАВИЛЬНОГО UX

### 1. При загрузке проекта
- ✅ **Сразу загружаются посты** (`rightContent = 'posts'` по умолчанию)
- ✅ Посты отображаются на главной странице

### 2. При нажатии на карту/планировщик/календарь (левая панель)
- ✅ Открывается выбранный контент **слева**
- ✅ Посты остаются **справа** (БЕЗ перезагрузки!)
- ✅ Двухоконный режим: левая панель (60%) + правая панель (40%)

### 3. При отжатии левой панели
- ✅ Левая панель закрывается
- ✅ Посты остаются активными (что-то одно всегда активно)
- ✅ Посты отображаются на полный экран

### 4. При нажатии на правую панель (лента, посты, профиль)
- ✅ Переключается правый контент
- ✅ Левая панель (если открыта) остается без изменений
- ✅ Посты НЕ перезагружаются при переключении

### 5. Страницы вне двухоконного режима
**Страницы типа `'page'`:** `/centre`, `/pro`, `/partners`, `/admin`, `/test`, `/legal`

- ✅ При открытии таких страниц - **сбрасываются панели** (`leftContent = null`, `rightContent = null`)
- ✅ Отображается страница на полный экран
- ✅ При закрытии/возврате - **автоматически восстанавливаются посты** (`rightContent = 'posts'`)

### 6. Правило "Что-то одно всегда активно"
- ✅ **НЕЛЬЗЯ** отжать все кнопки двухоконного режима
- ✅ Если отжимается левая панель - остается правая (посты)
- ✅ Если отжимается правая панель - остается левая (если открыта)
- ✅ По умолчанию всегда активны посты

---

## 🔧 РЕАЛИЗАЦИЯ

### LayoutContext.tsx
```typescript
// По умолчанию всегда активны посты
const [rightContent, setRightContent] = useState<ContentId>('posts');
```

### Sidebar.tsx

#### Левые панели (type: 'left')
```typescript
if (item.type === 'left') {
  // Если уже активен - НЕ закрываем, просто переключаем маршрут
  if (leftContent === item.id) {
    if (location.pathname !== leftRoute) {
      navigate(leftRoute);
    }
    return; // Не закрываем!
  }
  
  // Открываем - сначала навигация, потом установка leftContent
  navigate(leftRoute);
  setTimeout(() => {
    setLeftContent(item.id);
    // Если нет правого контента - автоматически открываем посты
    if (!rightContent) {
      setRightContent('posts');
    }
  }, 0);
}
```

#### Правые панели (type: 'right')
```typescript
if (item.type === 'right') {
  // Если уже активен - НЕ закрываем
  if (rightContent === item.id) {
    // Просто переключаем маршрут если нужно
    if (item.id === 'posts' && location.pathname !== '/posts' && location.pathname !== '/') {
      navigate('/posts');
    }
    return; // Не закрываем!
  }
  
  // Открываем
  setRightContent(item.id);
  if (item.id === 'posts' && location.pathname !== '/' && location.pathname !== '/posts') {
    navigate('/posts');
  }
}
```

#### Страницы вне двухоконного режима (type: 'page')
```typescript
if (item.type === 'page' && item.path) {
  // Сбрасываем панели
  setLeftContent(null);
  setRightContent(null);
  navigate(item.path);
}
```

### MainLayout.tsx

#### Solo-страницы
```typescript
const soloPages = ['/centre', '/pro', '/partners', '/admin', '/test', '/legal'];
const isSoloPage = soloPages.some(path => location.pathname.startsWith(path));

if (isSoloPage) {
  // Показываем children напрямую
  return <div>...</div>;
}

// При уходе с solo-страницы восстанавливаем посты
useEffect(() => {
  if (!isSoloPage && !rightContent) {
    layoutContext?.setRightContent('posts');
  }
}, [isSoloPage, rightContent]);
```

#### Режим панелей
```typescript
if ((leftContent || rightContent) && !isMainPage) {
  // Двухоконный режим
  if (leftContent && rightContent) {
    // Обе панели активны - делим экран
  } else if (leftContent && !rightContent) {
    // Только левая - на полный экран
  } else if (!leftContent && rightContent) {
    // Только правая - на полный экран
  } else {
    // Обе пусты - показываем посты по умолчанию
    <PageLayer side="right" active="posts" />
  }
}
```

#### Главная страница
```typescript
// Если rightContent='posts' и мы на главной - показываем через PageLayer
// Это предотвращает перемонтирование постов
if (rightContent === 'posts' && isMainPage) {
  return <PageLayer side="right" active="posts" />;
}
```

### PageLayer.tsx

#### Предотвращение перемонтирования
```typescript
// Используем key по active для предотвращения перемонтирования
<div key={active}>
  <Suspense fallback={...}>
    <ActiveComponent />
  </Suspense>
</div>
```

---

## ✅ РЕЗУЛЬТАТ

1. ✅ **Посты загружаются сразу** при открытии проекта
2. ✅ **Посты НЕ перезагружаются** при переключении панелей
3. ✅ **Что-то одно всегда активно** - нельзя отжать все кнопки
4. ✅ **Solo-страницы сбрасывают панели** и восстанавливают посты при возврате
5. ✅ **Единая загрузка** - один компонент загрузки для всех случаев
6. ✅ **Правильное переключение** - один клик для открытия/переключения

---

**Дата создания:** 15.11.2025  
**Автор:** AI Assistant


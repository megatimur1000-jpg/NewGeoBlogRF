# Принцип работы системы модерации

## Два режима модерации

### 1. Ручная модерация (`/moderation/:contentType/pending`)
- **Что показывает:** Все контент со статусом `pending` из таблиц контента (posts, events, routes, markers, blogs, comments, chats)
- **Как работает:**
  - Админ видит список контента на модерации
  - Может одобрить, отклонить или скрыть
  - При одобрении:
    - Статус меняется на `active`
    - Контент становится видимым для всех
    - Автору начисляется XP (если есть author_id)
    - Проверяются достижения

### 2. ИИ-помощник (`/moderation/ai/:contentType/review`) - ОСНОВНОЙ СПОСОБ МОДЕРАЦИИ
- **Что показывает:** Контент, который ИИ уже проанализировал и дал развёрнутые рекомендации
- **Как работает:**
  1. **ИИ анализирует ПЕРВЫМ:** При создании контента со статусом `pending` автоматически вызывается `autoAnalyzeContent()`
  2. **ИИ даёт развёрнутые рекомендации:**
     - `ai_suggestion` (approve/reject/hide/review) - что делать
     - `ai_confidence` (уверенность 0-1) - насколько ИИ уверен
     - `ai_reason` - **РАЗВЁРНУТОЕ ОБЪЯСНЕНИЕ** почему ИИ так решил
     - `ai_category` (spam/inappropriate/fake/safe) - категория проблемы
     - `ai_issues` - список конкретных проблем
     - `admin_verdict = 'pending'` - ждёт вердикта админа
  3. **Админ проверяет работу ИИ:**
     - Видит развёрнутую рекомендацию ИИ с объяснениями
     - Видит проблемы, которые нашёл ИИ
     - Ставит вердикт:
       - **"Правильно"** - если ИИ предложил `approve`, контент автоматически одобряется и начисляется XP
       - **"Неправильно"** - админ указывает, что ИИ ошибся, это сохраняется для обучения

## Почему в ИИ-помощнике может не быть постов?

1. **ИИ-анализ не запустился:**
   - `autoAnalyzeContent()` вызывается асинхронно после создания поста
   - Если произошла ошибка, запись в `ai_moderation_decisions` не создается
   - Проверьте логи бэкенда на наличие ошибок

2. **Запись создалась, но с другим `admin_verdict`:**
   - ИИ-помощник показывает только записи с `admin_verdict = 'pending'`
   - Если админ уже поставил вердикт, запись не показывается

3. **Контент был создан до внедрения ИИ-модерации:**
   - Старый контент не имеет записей в `ai_moderation_decisions`
   - Нужно вручную запустить анализ через `/moderation/ai/:contentType/:contentId/analyze`

## Что происходит при одобрении?

### Ручная модерация (`approveContent`):
1. Обновляется статус контента: `status = 'active'`
2. Контент становится публичным: `is_public = true`
3. Записывается информация о модерации: `moderated_at`, `moderated_by`
4. **Начисляется XP автору:**
   - Базовый XP зависит от типа контента (пост = 50, маршрут = 100, и т.д.)
   - Бонусы: +25 за фото, +15 за описание >50 символов, +20 за локацию, +10 за хештеги
5. **Проверяются достижения** через `addXPInternal`
6. Контент становится видимым для всех пользователей

### ИИ-помощник (`setAdminVerdict`):
1. Если вердикт "Правильно" и ИИ предложил `approve`:
   - Вызывается `approveContent`, который делает всё то же самое
   - Контент одобряется, XP начисляется
2. Если вердикт "Неправильно":
   - Сохраняется обратная связь для обучения ИИ
   - Контент остается на модерации (админ может одобрить вручную)

## Важные моменты

1. **Контент гостей:**
   - Гости могут создавать контент, но он всегда идет со статусом `pending`
   - Если у контента нет `author_id`, XP не начисляется
   - Контент гостей виден только в админ-панели до одобрения

2. **Контент админов:**
   - Админы могут создавать контент со статусом `active` сразу
   - Такой контент не требует модерации и не анализируется ИИ

3. **Дублирование:**
   - Один и тот же контент может быть в обоих списках:
     - В ручной модерации (если статус = `pending`)
     - В ИИ-помощнике (если есть запись в `ai_moderation_decisions` с `admin_verdict = 'pending'`)
   - Это нормально - админ может использовать любой способ модерации

## Проверка работы системы

1. **Проверить, создаются ли записи ИИ:**
   ```sql
   SELECT COUNT(*) FROM ai_moderation_decisions WHERE content_type = 'posts' AND admin_verdict = 'pending';
   ```

2. **Проверить, начисляется ли XP:**
   ```sql
   SELECT * FROM gamification_actions WHERE source = 'post_created' ORDER BY created_at DESC LIMIT 10;
   ```

3. **Проверить статус контента:**
   ```sql
   SELECT id, title, status, moderated_at FROM posts WHERE status = 'pending' ORDER BY created_at DESC LIMIT 10;
   ```

## Диагностика проблем

### Проблема: В ИИ-помощнике нет постов, хотя в ручной модерации они есть

**Причины:**
1. `autoAnalyzeContent()` вызывается асинхронно после создания поста и может не успеть выполниться
2. Ошибка при создании записи в `ai_moderation_decisions` (проверьте логи бэкенда)
3. Посты были созданы до внедрения ИИ-модерации

**Решение:**
- Проверьте логи бэкенда на наличие сообщений `✅ ИИ проанализировал` или ошибок
- Если записей нет, можно вручную запустить анализ через API: `POST /api/moderation/ai/posts/:postId/analyze`

### Проблема: При одобрении пост исчезает, но не публикуется

**Проверьте:**
1. Статус поста в БД: `SELECT id, title, status FROM posts WHERE id = '...'`
2. Логи бэкенда на наличие сообщений `✅ Контент одобрен` и `💰 XP начислено`
3. Если XP не начисляется, проверьте наличие `author_id` у поста

**Что должно происходить:**
- Статус меняется на `active`
- Пост становится видимым в ленте для всех пользователей
- Автору начисляется XP (если есть `author_id`)
- В логах появляются сообщения о начислении XP

### Проблема: XP не начисляется

**Причины:**
1. У контента нет `author_id` (создан гостем)
2. XP уже был начислен ранее (проверка дубликатов)
3. Ошибка при начислении XP (проверьте логи)

**Проверка:**
```sql
-- Проверить, начислялся ли XP за этот контент
SELECT * FROM gamification_actions 
WHERE source = 'post_created' AND content_id = '...';

-- Проверить уровень пользователя
SELECT * FROM user_levels WHERE user_id = '...';
```


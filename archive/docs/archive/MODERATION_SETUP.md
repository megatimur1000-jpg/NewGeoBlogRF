# Инструкция по настройке и проверке системы модерации

## ✅ Что было сделано

1. **Создан скрипт для создания админ-аккаунта** (`backend/create-admin.js`)
2. **Добавлены API endpoints для модерации событий:**
   - `GET /api/events/pending` - получение событий на модерации (только для админов)
   - `POST /api/events/:id/approve` - одобрение события (только для админов)
   - `POST /api/events/:id/reject` - отклонение события (только для админов)

3. **Обновлена панель модерации:**
   - Добавлена вкладка "События" в `ModerationPanel`
   - Создан компонент `EventsModerationPanel` для работы с реальными событиями
   - Панель автоматически загружает события со статусом `pending`

4. **Исправлены ошибки ratings API:**
   - Теперь корректно обрабатываются числовые ID событий
   - Добавлена обработка ошибок

## 🔐 Данные для входа администратора

**Email:** `admin@test.com` (или `admin@geoblog.ru` если создан новый)  
**Username:** `admin`  
**Password:** `admin123` (или значение из `ADMIN_PASSWORD` в `.env`)

**Роль:** `admin` ✅

## 📋 Как проверить систему модерации

### Шаг 1: Создать событие как гость

1. Откройте приложение в браузере (не авторизуйтесь)
2. Перейдите на страницу календаря (`/calendar`)
3. Создайте новое событие:
   - Заполните все поля
   - Добавьте фото (опционально)
   - Сохраните событие
4. Событие будет сохранено в `localStorage` со статусом `pending`

### Шаг 2: Зарегистрироваться или войти

1. Зарегистрируйтесь как новый пользователь ИЛИ войдите как существующий
2. После регистрации/входа события из `localStorage` должны быть отправлены на сервер со статусом `pending`

### Шаг 3: Войти как администратор

1. Выйдите из текущего аккаунта
2. Войдите с данными администратора:
   - Email: `admin@test.com`
   - Password: `admin123`
3. Перейдите на страницу модерации (`/moderation`)

### Шаг 4: Проверить события на модерации

1. В панели модерации переключитесь на вкладку **"События"**
2. Вы должны увидеть список всех событий со статусом `pending`
3. Для каждого события отображается:
   - Название
   - Описание
   - Фото (если есть)
   - Категория
   - Местоположение
   - Дата и время
   - Создатель

### Шаг 5: Одобрить или отклонить событие

1. **Одобрить событие:**
   - Нажмите кнопку **"Одобрить"** (зеленая)
   - Событие получит статус `active` и станет публичным
   - Событие исчезнет из списка модерации

2. **Отклонить событие:**
   - Нажмите кнопку **"Отклонить"** (красная)
   - Введите причину отклонения (опционально)
   - Событие получит статус `rejected` и станет непубличным
   - Событие исчезнет из списка модерации

## 🔧 Создание нового админ-аккаунта

Если нужно создать новый админ-аккаунт или обновить существующий:

```bash
cd backend
node create-admin.js
```

Можно настроить через переменные окружения в `.env`:
- `ADMIN_EMAIL` - email администратора (по умолчанию: `admin@geoblog.ru`)
- `ADMIN_USERNAME` - username администратора (по умолчанию: `admin`)
- `ADMIN_PASSWORD` - пароль администратора (по умолчанию: `Admin123!`)

## 🐛 Отладка

### Проверка статуса событий в БД

```sql
SELECT id, title, status, is_public, creator_id, created_at 
FROM events 
WHERE status = 'pending' 
ORDER BY created_at DESC;
```

### Проверка роли пользователя

```sql
SELECT id, email, username, role 
FROM users 
WHERE email = 'admin@test.com';
```

### Проверка API endpoints

1. **Получить события на модерации:**
   ```bash
   curl -X GET http://localhost:3000/api/events/pending \
     -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
   ```

2. **Одобрить событие:**
   ```bash
   curl -X POST http://localhost:3000/api/events/EVENT_ID/approve \
     -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
   ```

3. **Отклонить событие:**
   ```bash
   curl -X POST http://localhost:3000/api/events/EVENT_ID/reject \
     -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"reason": "Причина отклонения"}'
   ```

## 📝 Примечания

- События, созданные неавторизованными пользователями, автоматически получают статус `pending`
- События со статусом `pending` не отображаются в общем списке событий
- Только пользователи с ролью `admin` могут видеть и модерировать события
- После одобрения событие становится публичным и видимым всем пользователям
- После отклонения событие становится непубличным и скрывается от пользователей


# 🚀 Скрипты для управления серверами

## Быстрый запуск

### Windows (Batch)
```bash
# Запустить все серверы
start-all.bat

# Быстрый запуск (только frontend + backend)
quick-start.bat

# Остановить все серверы
stop-all.bat
```

### Windows (PowerShell)
```powershell
# Запустить все серверы
.\start-all.ps1

# Остановить все серверы
.\stop-all.ps1
```

## Что запускается

### start-all.bat / start-all.ps1
- ✅ Активирует Python виртуальное окружение
- ✅ Запускает PostgreSQL
- ✅ Запускает Backend (порт 3002)
- ✅ Запускает Frontend (порт 5173)

### quick-start.bat
- ✅ Запускает Backend (порт 3002)
- ✅ Запускает Frontend (порт 5173)

### stop-all.bat / stop-all.ps1
- ✅ Останавливает все Node.js процессы
- ✅ Останавливает PostgreSQL

## URL'ы после запуска

- 🌐 **Frontend**: http://localhost:5173
- 🔧 **Backend**: http://localhost:3002
- 🗄️ **PostgreSQL**: localhost:5432

## Устранение проблем

### Если PostgreSQL не запускается:
```bash
# Проверить статус службы
sc query postgresql-x64-15

# Запустить вручную
net start postgresql-x64-15
```

### Если порты заняты:
```bash
# Найти процессы на портах
netstat -ano | findstr :3002
netstat -ano | findstr :5173

# Убить процесс по PID
taskkill /f /pid <PID>
```

### Если Node.js не запускается:
```bash
# Очистить кэш npm
npm cache clean --force

# Переустановить зависимости
cd backend && npm install
cd frontend && npm install
```

## Автоматический запуск при старте Windows

1. Создайте ярлык для `start-all.bat`
2. Поместите в папку автозагрузки: `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup`
3. Теперь серверы будут запускаться автоматически при включении компьютера








































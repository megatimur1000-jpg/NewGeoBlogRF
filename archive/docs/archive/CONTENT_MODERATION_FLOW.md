# 📋 ПОЛНАЯ ЦЕПОЧКА МОДЕРАЦИИ КОНТЕНТА

## 🔄 ЦЕПОЧКА: Написание → Предмодерация → Модерация → Публикация

### 1️⃣ НАПИСАНИЕ КОНТЕНТА

**Что происходит:**
- Пользователь создаёт контент (пост, событие, маршрут, метку, блог)
- Система определяет роль пользователя:
  - **Админ** → статус `active` (публикуется сразу, без модерации)
  - **Зарегистрированный пользователь** → статус `pending` (требует модерации)
  - **Гость** → статус `pending` (требует модерации)

**Где реализовано:**
- `backend/src/routes/posts.js` - посты
- `backend/src/controllers/eventController.js` - события
- `backend/src/routes/routes.js` - маршруты
- `backend/src/routes/marker.js` - метки
- `backend/src/controllers/blogController.js` - блоги

**Код:**
```javascript
// Проверка роли
const userResult = await pool.query('SELECT role FROM users WHERE id = $1', [userId]);
const userRole = userResult.rows[0]?.role || 'registered';
const isAdmin = userRole === 'admin';

// Установка статуса
const finalStatus = isAdmin ? 'active' : 'pending';
```

---

### 2️⃣ ПРЕДМОДЕРАЦИЯ (ИИ-АНАЛИЗ)

**Что происходит:**
- Автоматически запускается для всего контента со статусом `pending`
- ИИ анализирует контент через `ModerationService`
- ИИ даёт развёрнутые рекомендации:
  - `approve` - одобрить
  - `reject` - отклонить
  - `review` - на дополнительную проверку
- Создаётся запись в `ai_moderation_decisions` с `admin_verdict = 'pending'`

**Где реализовано:**
- `backend/src/middleware/autoModeration.js` - функция `autoAnalyzeContent()`
- Вызывается автоматически после создания контента со статусом `pending`

**Что анализирует ИИ:**
- Спам-слова и рекламные фразы
- Неподходящий контент
- Качество контента (наличие фото, описания, локации, тегов)
- Фейковые отзывы
- Формирует развёрнутую рекомендацию с объяснением

**Результат:**
- Запись в БД: `ai_moderation_decisions`
  - `ai_suggestion` - предложение ИИ
  - `ai_confidence` - уверенность (0-1)
  - `ai_reason` - развёрнутое объяснение
  - `ai_category` - категория (spam/inappropriate/fake/safe)
  - `ai_issues` - список проблем
  - `admin_verdict = 'pending'` - ждёт вердикта админа

---

### 3️⃣ МОДЕРАЦИЯ (АДМИН ПРОВЕРЯЕТ ИИ)

**Что происходит:**
- Админ открывает вкладку "ИИ-помощник" в админ-панели
- Видит контент с рекомендациями ИИ:
  - Развёрнутое объяснение почему ИИ так решил
  - Список найденных проблем
  - Уверенность ИИ
  - Категория контента
- Админ ставит вердикт:
  - **"Правильно"** - если согласен с ИИ
  - **"Неправильно"** - если ИИ ошибся (с комментарием для обучения)

**Где реализовано:**
- `frontend/src/components/Admin/AIModerationPanel.tsx` - интерфейс
- `backend/src/controllers/aiModerationController.js` - функция `setAdminVerdict()`

**Что происходит при вердикте "Правильно":**
1. Если ИИ предложил `approve`:
   - Контент автоматически одобряется (статус → `active`)
   - Автору начисляется XP через `approveContent()`
   - Обновляется уровень пользователя
   - Проверяются достижения
   - Контент становится видимым для всех

2. Если ИИ предложил `reject`:
   - Контент отклоняется (статус → `rejected`)
   - Контент скрыт от пользователей

3. Если ИИ предложил `review`:
   - Контент остаётся на дополнительной проверке

**Что происходит при вердикте "Неправильно":**
- Сохраняется в `ai_moderation_training` для обучения ИИ
- Админ может вручную одобрить/отклонить контент

---

### 4️⃣ ПУБЛИКАЦИЯ

**Что происходит:**
- Одобренный контент (статус `active`) становится видимым для всех пользователей
- Контент появляется в лентах, на картах, в календарях
- Автору начисляется XP и проверяются достижения

**Где реализовано:**
- `backend/src/controllers/moderationController.js` - функция `approveContent()`
- Все GET-запросы фильтруют контент по статусу:
  - Обычные пользователи видят только `active`
  - Админы могут видеть все статусы

---

## 🎯 ИНТЕГРАЦИЯ С ГЕЙМИФИКАЦИЕЙ

### Начисление XP при одобрении:

**Базовые значения:**
- Посты: 50 XP
- События: 50 XP
- Маршруты: 100 XP
- Метки: 30 XP
- Блоги: 75 XP
- Комментарии: 10 XP
- Чаты: 5 XP

**Бонусы за качество:**
- Фото: +25 XP
- Описание (>50 символов): +15 XP
- Локация: +20 XP
- Теги: +10 XP

**Что происходит:**
1. При одобрении контента вызывается `approveContent()`
2. Рассчитывается XP (базовый + бонусы)
3. Обновляется `user_levels`:
   - `total_xp` увеличивается
   - Пересчитывается `current_level`
   - Обновляется `rank`
4. Записывается в `xp_history`
5. Записывается в `gamification_actions` (защита от дубликатов)
6. Проверяются достижения

---

## 🔍 КОМПЛЕКСНАЯ ОЦЕНКА КОНТЕНТА

### Критерии оценки ИИ:

1. **Качество контента:**
   - Наличие фото/видео
   - Полнота описания
   - Указана локация
   - Добавлены теги/хэштеги

2. **Соответствие правилам:**
   - Нет спама
   - Нет рекламы
   - Нет неподходящего контента
   - Нет фейковой информации

3. **Уникальность:**
   - Не дубликат
   - Оригинальный контент

4. **Релевантность:**
   - Относится к путешествиям/туризму
   - Полезная информация

### Действия админа:

1. **Проверка рекомендаций ИИ:**
   - Читает развёрнутое объяснение
   - Проверяет найденные проблемы
   - Оценивает уверенность ИИ

2. **Принятие решения:**
   - Согласен с ИИ → "Правильно"
   - Не согласен → "Неправильно" + комментарий

3. **Ручная модерация (если ИИ не смог проанализировать):**
   - Контент без ИИ-анализа попадает в "Ручную модерацию"
   - Админ может одобрить/отклонить вручную

---

## 📊 СТАТУСЫ КОНТЕНТА

- `pending` - на модерации (ожидает проверки)
- `active` - одобрен и опубликован (виден всем)
- `rejected` - отклонён (скрыт от пользователей)
- `hidden` - скрыт админом (скрыт от пользователей)
- `draft` - черновик (только для блогов, не публикуется)

---

## 🔄 ПОЛНЫЙ ЦИКЛ

```
1. Пользователь создаёт контент
   ↓
2. Система устанавливает статус (admin → active, остальные → pending)
   ↓
3. Если pending → автоматически запускается ИИ-анализ
   ↓
4. ИИ анализирует и создаёт рекомендацию в ai_moderation_decisions
   ↓
5. Админ видит рекомендацию в "ИИ-помощнике"
   ↓
6. Админ ставит вердикт:
   - "Правильно" + ИИ предложил approve → контент одобряется → XP начисляется → публикация
   - "Правильно" + ИИ предложил reject → контент отклоняется
   - "Неправильно" → сохраняется для обучения ИИ
   ↓
7. Одобренный контент (active) становится видимым для всех
```

---

## ✅ ЧТО НУЖНО ДЛЯ ПОЛНОЙ РЕАЛИЗАЦИИ

1. ✅ Создание контента с правильным статусом
2. ✅ Автоматический ИИ-анализ для pending контента
3. ✅ Интерфейс для проверки рекомендаций ИИ
4. ✅ Автоматическое одобрение при вердикте "Правильно"
5. ✅ Начисление XP при одобрении
6. ✅ Фильтрация контента по статусу для пользователей
7. ⚠️ Нужно: Убедиться что ВСЕ типы контента проходят через эту цепочку
8. ⚠️ Нужно: Добавить модерацию для комментариев и чатов
9. ⚠️ Нужно: Добавить модерацию при редактировании контента (статус → pending)

---

## 🎯 РЕАЛЬНЫЕ ДЕЙСТВИЯ ДЛЯ КОМПЛЕКСНОЙ ОЦЕНКИ

### Для админа:

1. **Проверка качества контента:**
   - Видит все критерии оценки ИИ
   - Может оценить полноту информации
   - Проверяет соответствие тематике

2. **Принятие решения:**
   - Опирается на рекомендации ИИ
   - Может переопределить решение ИИ
   - Обучает ИИ через вердикты "Неправильно"

3. **Управление контентом:**
   - Видит статистику модерации
   - Отслеживает работу ИИ
   - Управляет публикацией контента

### Для системы:

1. **Автоматизация:**
   - ИИ анализирует весь контент автоматически
   - При одобрении автоматически начисляется XP
   - Автоматически обновляются уровни пользователей

2. **Обучение ИИ:**
   - Сохраняются случаи, когда админ не согласен с ИИ
   - Можно улучшать алгоритмы на основе этих данных

3. **Защита:**
   - Дубликаты XP не начисляются
   - Контент проходит проверку перед публикацией
   - Только одобренный контент виден пользователям

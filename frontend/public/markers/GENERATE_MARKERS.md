# Генератор маркеров для всех категорий

## 📋 Описание

Этот инструмент создаёт единые маркеры для всех 16 категорий с:
- ✅ Градиентами цветов (сверху темнее, снизу светлее)
- ✅ Иконками категорий внутри (FontAwesome)
- ✅ Единым стилем для всех маркеров
- ✅ Соответствием иконок мини-попапам на карте

## 🚀 Использование

### Вариант 1: HTML-генератор (Рекомендуется)

1. Откройте `generate-all-markers.html` в браузере
2. Увидите все маркеры на странице
3. Для каждого маркера:
   - Кликните правой кнопкой → "Сохранить изображение как" (если браузер поддерживает)
   - Или используйте DevTools для копирования SVG кода
   - Или используйте скриншот инструмент

### Вариант 2: Ручное создание

Используйте пример из `template-marker.svg` как основу:

1. Замените градиент на цвет категории (см. `markerCategories.ts`)
2. Замените иконку внутри на FontAwesome иконку категории
3. Сохраните как `pin-{category}.png` (34x44px)

## 📁 Файлы маркеров

После генерации должны быть созданы следующие PNG файлы:

```
frontend/public/markers/
├── pin-nature.png        (Зелёный #27ae60, fa-leaf)
├── pin-restaurant.png    (Бордовый #8B0000, fa-utensils)
├── pin-hotel.png         (Фиолетовый #8e44ad, fa-hotel)
├── pin-attraction.png    (Синий #3498db, fa-star)
├── pin-culture.png       (Жёлтый #f1c40f, fa-landmark)
├── pin-entertainment.png (Оранжевый #f39c12, fa-gem)
├── pin-transport.png     (Бирюзовый #16a085, fa-bus)
├── pin-shopping.png      (Тёмно-оранжевый #e67e22, fa-wallet)
├── pin-healthcare.png    (Красный #e74c3c, fa-heart)
├── pin-education.png     (Синий #3498db, fa-users)
├── pin-service.png       (Тёмно-серый #34495e, fa-building)
├── pin-event.png         (Фиолетовый #9b59b6, fa-calendar-check)
├── pin-blog.png          (Светло-зелёный #2ecc71, fa-pen-nib)
├── pin-route.png         (Оранжевый #f39c12, fa-route)
├── pin-other.png         (Серый #7f8c8d, fa-question)
└── pin-user.png          (Оранжево-коричневый #e67e22, fa-map-pin)
```

## 🎨 Спецификация маркеров

- **Размер**: 34x44 пикселя
- **Формат**: PNG с прозрачным фоном
- **Форма**: Капля (teardrop)
- **Градиент**: От тёмного цвета сверху к светлому снизу (40% светлее)
- **Иконка**: FontAwesome SVG внутри белого круга
- **Тень**: Мягкая тень снизу

## 🔗 Соответствие с мини-попапами

Иконки в маркерах **идентичны** тем, что используются в мини-попапах на карте `map.tsx`:
- Источник: `markerCategories.ts` → `getFontAwesomeIconName()`
- Все иконки уникальны для каждой категории
- Одинаковый визуальный стиль везде

## ✅ После создания

После генерации PNG файлов:
1. Убедитесь, что все 16 файлов созданы
2. Проверьте размеры (34x44px)
3. Проверьте прозрачность фона
4. Маркеры автоматически будут использоваться в:
   - Постах (`PostMap.tsx`)
   - Блогах (`BlogMap.tsx`)
   - На карте (`Map.tsx`)
   - Везде, где используется `getMarkerIconPath()`


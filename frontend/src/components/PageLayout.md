# PageLayout - Универсальный компонент для всех страниц

## 🎯 Назначение

PageLayout - это универсальный компонент, который решает проблему дублирования стилей между страницами. Он обеспечивает единообразный дизайн и поведение для всех страниц, включая карту.

## 🚀 Преимущества

1. **Единый дизайн** - все страницы используют одинаковые стили
2. **Гибкость** - разные режимы для разных типов страниц
3. **Карта на весь экран** - специальный режим для карты без ограничений
4. **Быстрая разработка** - 5 минут вместо 6 часов на страницу

## 📋 Использование

### Базовый пример:

```tsx
import PageLayout from '../components/PageLayout';

const MyPage = () => {
  const [leftPanelOpen, setLeftPanelOpen] = useState(false);
  const [rightPanelOpen, setRightPanelOpen] = useState(false);

  return (
    <MirrorGradientContainer>
      <PageLayout
        mode="default"
        title="Моя страница"
        icon={<FaIcon size={24} className="text-blue-500" />}
        leftButton={{
          icon: <FaCog size={20} />,
          onClick: () => setLeftPanelOpen(true),
          title: "Настройки"
        }}
        rightButton={{
          icon: <FaUsers size={20} />,
          onClick: () => setRightPanelOpen(true),
          title: "Пользователи"
        }}
        leftPanel={{
          title: "Настройки",
          content: <div>Содержимое левой панели</div>,
          isOpen: leftPanelOpen,
          onClose: () => setLeftPanelOpen(false)
        }}
        rightPanel={{
          title: "Пользователи",
          content: <div>Содержимое правой панели</div>,
          isOpen: rightPanelOpen,
          onClose: () => setRightPanelOpen(false)
        }}
      >
        <div>Основной контент страницы</div>
      </PageLayout>
    </MirrorGradientContainer>
  );
};
```

## 🎨 Режимы (mode)

### `map` - Для карты
- Убирает отступы и ограничения
- Карта растягивается на весь экран
- Прозрачный фон панели

### `chat` - Для чата
- Стандартные ограничения
- Обычный фон панели

### `calendar` - Для календаря
- Компактный заголовок
- Меньшие отступы

### `default` - По умолчанию
- Стандартные настройки

## 🔧 Пропсы

| Проп | Тип | Описание |
|------|-----|----------|
| `mode` | `'map' \| 'chat' \| 'calendar' \| 'default'` | Режим страницы |
| `title` | `string` | Заголовок страницы |
| `icon` | `ReactNode` | Иконка в заголовке |
| `leftButton` | `ButtonConfig` | Левая кнопка управления |
| `rightButton` | `ButtonConfig` | Правая кнопка управления |
| `leftPanel` | `PanelConfig` | Левая выдвигающаяся панель |
| `rightPanel` | `PanelConfig` | Правая выдвигающаяся панель |
| `showHeader` | `boolean` | Показывать ли заголовок |
| `className` | `string` | Дополнительные CSS классы |

## 📁 Структура файлов

```
src/
├── styles/
│   └── PageLayout.css          # Все стили для страниц
├── components/
│   ├── PageLayout.tsx          # Универсальный компонент
│   └── PageLayout.md           # Документация
└── pages/
    ├── Calendar.tsx            # Пример использования
    ├── Profile.tsx             # Пример использования
    └── ...                     # Остальные страницы
```

## 🎯 Миграция существующих страниц

### Быстрая миграция (5 минут):

1. **Импортируйте PageLayout:**
```tsx
import PageLayout from '../components/PageLayout';
```

2. **Замените структуру:**
```tsx
// Было:
<div className="chat-container">
  <div className="chat-header">...</div>
  <div className="chat-main-area">...</div>
</div>

// Стало:
<PageLayout
  mode="default"
  title="Заголовок"
  // ... остальные пропсы
>
  {/* Контент */}
</PageLayout>
```

3. **Добавьте состояние для панелей:**
```tsx
const [leftPanelOpen, setLeftPanelOpen] = useState(false);
const [rightPanelOpen, setRightPanelOpen] = useState(false);
```

## 🚀 Результат

- ✅ **Единый дизайн** на всех страницах
- ✅ **Карта на весь экран** без ограничений
- ✅ **Быстрая разработка** - 5 минут вместо 6 часов
- ✅ **Чистый код** без дублирования стилей
- ✅ **Гибкость** для разных типов страниц

## 📝 Примеры использования

Смотрите файлы:
- `Calendar.tsx` - пример с календарем
- `Profile.tsx` - пример с профилем
- `Map.tsx` - пример с картой (использует встроенные стили)
- `Chat.tsx` - пример с чатом (использует встроенные стили) 
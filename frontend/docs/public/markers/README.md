```markdown
# 🎯 Маркеры категорий

## Быстрый старт

1. **Откройте** `generate-svg-files.html` в браузере
2. **Нажмите** "Скачать все SVG файлы"
3. **Конвертируйте** все SVG в PNG (34x44px):
   - Используйте [SVG to PNG](https://svgtopng.com/)
   - Или Inkscape: `inkscape --export-filename=pin-{category}.png --export-width=34 --export-height=44 pin-{category}.svg`
4. **Сохраните** PNG файлы в эту папку (`frontend/public/markers/`)

... (содержимое сохранено)
```

# 🚀 Реализация поэтапного раскрытия геймификации

## ✅ Что реализовано

### 1. Feature Flags ✅
- **`config/gamificationFeatures.ts`** - система флагов для этапов
- **4 этапа раскрытия** - от базовых функций до полной системы
- **Автоматическое определение этапа** - по количеству пользователей

### 2. Отслеживание действий гостей ✅
- **`services/guestActionsService.ts`** - сервис отслеживания
- **Автоматическое сохранение** при создании поста/метки
- **Отметка одобрения** при модерации
- **Хранение в localStorage** (можно перенести на Backend)

### 3. Ретроактивное начисление ✅
- **`utils/retroactiveGamification.ts`** - функция начисления
- **Автоматическое начисление XP** за одобренные действия
- **Проверка достижений** (TODO: полная реализация)
- **Очистка действий** после обработки

### 4. Приветственное окно ✅
- **`components/Gamification/WelcomeModal.tsx`** - красивое модальное окно
- **`components/Gamification/WelcomeModalWrapper.tsx`** - обёртка
- **`hooks/useWelcomeModal.ts`** - хук для управления
- **Интеграция в App.tsx** - автоматический показ

### 5. Интеграция в сервисы ✅
- **`postsService.ts`** - отслеживание действий гостей
- **`markerService.ts`** - отслеживание действий гостей

---

## 📋 Что нужно доделать

### Backend интеграция

#### 1. API для feature flags
```javascript
// backend/src/routes/gamificationRoutes.js
router.get('/features', async (req, res) => {
  const userCount = await getTotalUserCount();
  const stage = getGamificationStage(userCount);
  const features = getActiveFeatures(stage);
  res.json({ features, stage, userCount });
});
```

#### 2. API для отслеживания действий гостей
```javascript
// backend/src/routes/gamificationRoutes.js
router.post('/guest-actions/approve', async (req, res) => {
  const { contentId, actionType } = req.body;
  // Отмечаем действие как одобренное
  await markGuestActionAsApproved(contentId, actionType);
  res.json({ success: true });
});
```

#### 3. API для ретроактивного начисления
```javascript
// backend/src/routes/gamificationRoutes.js
router.post('/retroactive', async (req, res) => {
  const { guestId, userId } = req.body;
  const result = await applyRetroactiveGamification(guestId, userId);
  res.json(result);
});
```

### Интеграция с модерацией

Нужно вызывать `markActionAsApproved()` когда модератор одобряет контент:

```typescript
// В moderationService.ts или в компоненте модерации
import { markActionAsApproved } from '../services/guestActionsService';

// После одобрения поста
markActionAsApproved(postId, 'post');

// После одобрения метки
markActionAsApproved(markerId, 'marker');
```

---

## 🎯 Этапы раскрытия (детально)

### Этап 1: Старт (0-50 пользователей) ✅

**Активные функции:**
- ✅ Система уровней и XP
- ✅ Базовые достижения (первые посты, метки)
- ✅ Прогресс-бар уровня
- ✅ Ранги (Новичок, Исследователь)

**Скрытые функции:**
- ❌ Ежедневные цели
- ❌ Достижения качества
- ❌ Стрик
- ❌ Рейтинги

**Источники XP:**
- Создание поста: 50 XP
- Создание метки: 30 XP
- Бонусы: +25 за фото, +30 за метку, +20 за фото метки

### Этап 2: Рост (50-200 пользователей)

**Добавляется:**
- ✅ Ежедневные цели (анонсируем!)
- ✅ Достижения качества
- ✅ Стрик активности

**Анонс:**
```
🎉 Новая функция: Ежедневные цели!
Выполняйте задачи каждый день и получайте бонусы XP!
```

**Новые источники XP:**
- Ежедневные цели: 30-75 XP
- Высокое качество: +10-30 XP
- Стрик бонус: +25% за 7 дней

### Этап 3: Развитие (200-500 пользователей)

**Добавляется:**
- ✅ Рейтинги пользователей
- ✅ Специальные достижения
- ✅ Сезонные события

**Анонс:**
```
🏆 Новая функция: Рейтинги!
Соревнуйтесь с другими пользователями и попадите в топ!
```

### Этап 4: Зрелость (500+ пользователей)

**Все функции активны:**
- ✅ Все достижения
- ✅ Все бусты
- ✅ Все рейтинги
- ✅ Специальные события

---

## 🔄 Сценарий работы для гостя

### Шаг 1: Гость создаёт контент

```
Гость создаёт пост
    ↓
postsService.createPost()
    ↓
Нет токена → сохраняется как draft
    ↓
recordGuestAction({
  actionType: 'post',
  contentId: draft.id,
  approved: false,
  metadata: { hasPhoto: true, hasMarker: false }
})
    ↓
Действие сохранено в localStorage
```

### Шаг 2: Гость отправляет на модерацию

```
Гость нажимает "Отправить на модерацию"
    ↓
markActionAsSubmitted(draft.id, 'post')
    ↓
Действие помечено как отправленное
```

### Шаг 3: Модератор одобряет

```
Модератор одобряет пост
    ↓
markActionAsApproved(postId, 'post')
    ↓
Действие помечено как одобренное
```

### Шаг 4: Гость регистрируется

```
Гость регистрируется
    ↓
useWelcomeModal проверяет одобренные действия
    ↓
applyRetroactiveGamification(guestId, userId)
    ↓
Для каждого одобренного действия:
  - Начисляется XP
  - Проверяются достижения
    ↓
WelcomeModal показывается с результатами
    ↓
Пользователь видит свой уровень и достижения
```

---

## 📊 Определение этапа

### Вариант 1: По количеству пользователей (рекомендуется)

```typescript
// В GamificationContext
const loadFeatures = async () => {
  try {
    const response = await apiClient.get('/gamification/user-count');
    const userCount = response.data.count || 0;
    const stage = getGamificationStage(userCount);
    setFeatures(getActiveFeatures(stage));
  } catch (error) {
    setFeatures(getActiveFeatures(1)); // Fallback
  }
};
```

### Вариант 2: По времени (альтернатива)

```typescript
const daysSinceLaunch = getDaysSinceLaunch();
const stage = daysSinceLaunch < 30 ? 1 : 
              daysSinceLaunch < 90 ? 2 : 
              daysSinceLaunch < 180 ? 3 : 4;
```

### Вариант 3: Комбинированный

```typescript
const userCount = await getTotalUserCount();
const activeUsers = await getActiveUsersLastWeek();
const daysSinceLaunch = getDaysSinceLaunch();

const stage = calculateStage({
  userCount,
  activeUsers,
  daysSinceLaunch,
});
```

---

## 🎨 Анонсы новых функций

### Компонент анонса

```typescript
// components/Gamification/FeatureAnnouncement.tsx
function FeatureAnnouncement({ feature, onClose }) {
  return (
    <div className="bg-gradient-to-r from-blue-500 to-purple-500 text-white p-6 rounded-lg">
      <h3>🎉 Новая функция: {feature.name}!</h3>
      <p>{feature.description}</p>
      <button onClick={onClose}>Понятно</button>
    </div>
  );
}
```

### Показ анонса

```typescript
// В GamificationContext
useEffect(() => {
  if (features.dailyGoals && !localStorage.getItem('dailyGoals_announced')) {
    setShowAnnouncement('dailyGoals');
    localStorage.setItem('dailyGoals_announced', 'true');
  }
}, [features]);
```

---

## 🔧 Настройка этапов

### Изменить пороги

Откройте `config/gamificationFeatures.ts`:

```typescript
export function getGamificationStage(userCount: number): number {
  if (userCount < 50) return 1;    // ← Изменить порог
  if (userCount < 200) return 2;  // ← Изменить порог
  if (userCount < 500) return 3;  // ← Изменить порог
  return 4;
}
```

### Добавить новый этап

```typescript
export function getGamificationStage(userCount: number): number {
  if (userCount < 50) return 1;
  if (userCount < 200) return 2;
  if (userCount < 500) return 3;
  if (userCount < 1000) return 4;  // ← Новый этап
  return 5;  // ← Новый этап
}

export function getActiveFeatures(stage: number): GamificationFeatures {
  return {
    // ...
    newFeature: stage >= 5,  // ← Новая функция
  };
}
```

---

## 📝 Чеклист для запуска

### Перед запуском
- [ ] Создать таблицы в БД (`create_gamification_tables.sql`)
- [ ] Настроить Backend API для feature flags
- [ ] Интегрировать отслеживание в модерацию
- [ ] Протестировать ретроактивное начисление
- [ ] Протестировать приветственное окно

### После запуска
- [ ] Мониторить количество пользователей
- [ ] Готовить анонсы для этапа 2
- [ ] Собирать обратную связь
- [ ] Адаптировать пороги этапов при необходимости

---

## 🎯 Рекомендации

### Для старта (0-50 пользователей)
1. **Фокус на базовых функциях** - уровни, XP, простые достижения
2. **Не перегружать** - скрыть сложные функции
3. **Мотивировать** - показать ценность базовых функций

### При росте (50-200)
1. **Анонсировать заранее** - за неделю до включения
2. **Объяснять ценность** - зачем нужны ежедневные цели
3. **Показывать примеры** - как использовать новые функции

### При развитии (200-500)
1. **Добавлять соревновательность** - рейтинги
2. **Специальные события** - сезонные достижения
3. **Расширенные бусты** - дополнительные мотивации

---

**Готово к обсуждению и доработке!** 🚀


# 🎯 Стратегия поэтапного раскрытия геймификации

## 💡 Концепция

### Проблема
- Слишком много функций сразу может перегрузить пользователей
- Нужно постепенно вводить функции по мере роста сообщества
- Гости должны получать достижения после регистрации

### Решение
1. **Feature Flags** - система флагов для включения/выключения функций
2. **Поэтапное раскрытие** - функции открываются по количеству пользователей или времени
3. **Ретроактивное начисление** - гости получают достижения при регистрации
4. **Приветственное окно** - красивое поздравление с уровнем и достижениями

---

## 📊 Этапы раскрытия

### Этап 1: Старт (0-50 пользователей)
**Базовые функции:**
- ✅ Система уровней и XP
- ✅ Базовые достижения (первые посты, метки)
- ✅ Простой прогресс-бар
- ❌ Ежедневные цели (скрыто)
- ❌ Сложные достижения (скрыто)
- ❌ Рейтинги (скрыто)

**Источники XP:**
- Создание поста: 50 XP
- Создание метки: 30 XP
- Бонусы за фото/описание: +20-30 XP

### Этап 2: Рост (50-200 пользователей)
**Добавляется:**
- ✅ Ежедневные цели (анонсируем как "новую функцию")
- ✅ Достижения качества
- ✅ Стрик активности
- ❌ Рейтинги (скрыто)
- ❌ Специальные события (скрыто)

**Новые источники XP:**
- Ежедневные цели: 30-75 XP
- Высокое качество: +10-30 XP

### Этап 3: Развитие (200-500 пользователей)
**Добавляется:**
- ✅ Рейтинги пользователей
- ✅ Специальные достижения
- ✅ Сезонные события
- ✅ Расширенные бусты

**Новые источники XP:**
- Стрик бонусы: +25-50% множитель
- Специальные события: +100-500 XP

### Этап 4: Зрелость (500+ пользователей)
**Все функции активны:**
- ✅ Все достижения
- ✅ Все бусты
- ✅ Все рейтинги
- ✅ Специальные события

---

## 🎁 Ретроактивное начисление для гостей

### Проблема
Гость создал контент → модератор одобрил → гость регистрируется → должен получить все достижения

### Решение

#### 1. Отслеживание действий гостей
```typescript
// В guestDrafts.ts или отдельном сервисе
interface GuestAction {
  guestId: string;        // Уникальный ID гостя (из localStorage)
  actionType: 'post' | 'marker' | 'route';
  contentId: string;
  createdAt: string;
  moderatedAt?: string;   // Когда одобрено
  approved: boolean;      // Одобрено ли
}
```

#### 2. Привязка к пользователю
```typescript
// При регистрации
async function linkGuestActionsToUser(guestId: string, userId: string) {
  // Находим все действия гостя
  const guestActions = await getGuestActions(guestId);
  
  // Для каждого одобренного действия
  for (const action of guestActions.filter(a => a.approved)) {
    // Начисляем XP ретроактивно
    await addRetroactiveXP(userId, action);
    
    // Проверяем достижения
    await checkRetroactiveAchievements(userId, action);
  }
}
```

#### 3. Приветственное окно
```typescript
// WelcomeModal.tsx
function WelcomeModal({ userLevel, achievements, guestActions }) {
  return (
    <Modal>
      <h1>Добро пожаловать!</h1>
      <p>Вы внесли вклад в проект ещё до регистрации!</p>
      
      <LevelCard level={userLevel} />
      
      <AchievementsList achievements={achievements} />
      
      <GuestActionsSummary actions={guestActions} />
      
      <p>Спасибо за ваш вклад! 🎉</p>
    </Modal>
  );
}
```

---

## 🔧 Реализация Feature Flags

### Структура

```typescript
// config/gamificationFeatures.ts
export interface GamificationFeatures {
  // Базовые функции (всегда включены)
  basicLevels: boolean;        // true
  basicXP: boolean;            // true
  basicAchievements: boolean;  // true
  
  // Этап 2
  dailyGoals: boolean;         // false → true при 50+ пользователях
  qualityAchievements: boolean; // false → true при 50+ пользователях
  streak: boolean;              // false → true при 50+ пользователях
  
  // Этап 3
  leaderboards: boolean;       // false → true при 200+ пользователях
  specialEvents: boolean;      // false → true при 200+ пользователях
  
  // Этап 4
  advancedBoosts: boolean;      // false → true при 500+ пользователях
}

// Определение этапа на основе количества пользователей
export function getGamificationStage(userCount: number): number {
  if (userCount < 50) return 1;
  if (userCount < 200) return 2;
  if (userCount < 500) return 3;
  return 4;
}

// Получение активных функций
export function getActiveFeatures(stage: number): GamificationFeatures {
  return {
    basicLevels: true,
    basicXP: true,
    basicAchievements: true,
    dailyGoals: stage >= 2,
    qualityAchievements: stage >= 2,
    streak: stage >= 2,
    leaderboards: stage >= 3,
    specialEvents: stage >= 3,
    advancedBoosts: stage >= 4,
  };
}
```

---

## 📝 План реализации

### Фаза 1: Feature Flags (1-2 дня)
- [ ] Создать `config/gamificationFeatures.ts`
- [ ] Добавить проверку этапа в `GamificationContext`
- [ ] Скрыть компоненты для неактивных функций
- [ ] Добавить анонсы новых функций

### Фаза 2: Отслеживание гостей (2-3 дня)
- [ ] Создать `services/guestActionsService.ts`
- [ ] Сохранять действия гостей в localStorage/Backend
- [ ] Отмечать одобренные действия
- [ ] API для получения действий гостя

### Фаза 3: Ретроактивное начисление (2-3 дня)
- [ ] Функция `linkGuestActionsToUser()`
- [ ] Ретроактивное начисление XP
- [ ] Ретроактивная проверка достижений
- [ ] Backend API для ретроактивного начисления

### Фаза 4: Приветственное окно (1-2 дня)
- [ ] Компонент `WelcomeModal.tsx`
- [ ] Отображение уровня и достижений
- [ ] Список действий гостя
- [ ] Анимации и поздравления

### Фаза 5: Анонсы новых функций (1 день)
- [ ] Компонент `FeatureAnnouncement.tsx`
- [ ] Уведомления о новых функциях
- [ ] Интеграция в центр влияния

---

## 🎨 UI/UX для приветственного окна

### Дизайн
```
┌─────────────────────────────────────┐
│         🎉 Добро пожаловать!        │
│                                     │
│  Вы уже внесли вклад в проект!     │
│                                     │
│  ┌─────────────────────────────┐  │
│  │   Уровень 3  🌟 Исследователь │  │
│  │   ████████░░ 450 / 519 XP    │  │
│  └─────────────────────────────┘  │
│                                     │
│  🏆 Разблокировано достижений: 5    │
│                                     │
│  ✅ Создано постов: 3               │
│  ✅ Создано меток: 7                │
│  ✅ Получено одобрений: 10          │
│                                     │
│  Спасибо за ваш вклад! 🎊          │
│                                     │
│        [Продолжить]                │
└─────────────────────────────────────┘
```

### Анимации
- Появление с fade-in
- Анимация прогресс-бара
- Появление достижений по очереди
- Конфетти при открытии

---

## 🔄 Логика работы

### Сценарий 1: Гость создаёт контент

```
Гость создаёт пост
    ↓
Сохраняется в guestDrafts
    ↓
Отправляется на модерацию
    ↓
Создаётся запись в guestActions:
  {
    guestId: "guest_abc123",
    actionType: "post",
    contentId: "post_456",
    approved: false
  }
    ↓
Модератор одобряет
    ↓
Обновляется guestActions:
  {
    approved: true,
    moderatedAt: "2024-01-15T10:00:00Z"
  }
```

### Сценарий 2: Гость регистрируется

```
Гость нажимает "Зарегистрироваться"
    ↓
Регистрация успешна → userId = "user_789"
    ↓
Вызывается linkGuestActionsToUser("guest_abc123", "user_789")
    ↓
Находятся все одобренные действия гостя
    ↓
Для каждого действия:
  - Начисляется XP ретроактивно
  - Проверяются достижения
  - Обновляется уровень
    ↓
Показывается WelcomeModal с результатами
```

---

## 📊 Метрики для определения этапа

### Вариант 1: По количеству пользователей
```typescript
const userCount = await getTotalUserCount();
const stage = getGamificationStage(userCount);
```

### Вариант 2: По времени
```typescript
const daysSinceLaunch = getDaysSinceLaunch();
const stage = daysSinceLaunch < 30 ? 1 : 
              daysSinceLaunch < 90 ? 2 : 
              daysSinceLaunch < 180 ? 3 : 4;
```

### Вариант 3: Комбинированный (рекомендуется)
```typescript
const userCount = await getTotalUserCount();
const daysSinceLaunch = getDaysSinceLaunch();
const activeUsers = await getActiveUsersLastWeek();

const stage = calculateStage({
  userCount,
  daysSinceLaunch,
  activeUsers,
});
```

---

## 🎯 Рекомендации

### Что делать сейчас
1. ✅ Реализовать Feature Flags
2. ✅ Начать отслеживать действия гостей
3. ✅ Подготовить приветственное окно
4. ✅ Настроить ретроактивное начисление

### Что делать позже
- Анонсы новых функций (когда будет 50+ пользователей)
- Рейтинги (когда будет 200+ пользователей)
- Специальные события (когда будет 500+ пользователей)

### Важные моменты
- **Не перегружать** пользователей на старте
- **Анонсировать** новые функции заранее
- **Показывать ценность** каждого этапа
- **Мотивировать** возвращаться за новыми функциями

---

## ❓ Вопросы для обсуждения

1. **Критерии этапов:**
   - Только количество пользователей?
   - Или комбинация (пользователи + время + активность)?

2. **Ретроактивное начисление:**
   - Начислять XP за все действия или только одобренные?
   - Есть ли лимит на ретроактивное начисление?

3. **Приветственное окно:**
   - Показывать всегда или только если есть достижения?
   - Можно ли закрыть и посмотреть позже?

4. **Анонсы:**
   - Как анонсировать новые функции?
   - Баннер, модальное окно, уведомление?

---

**Готов обсудить детали и начать реализацию!** 🚀


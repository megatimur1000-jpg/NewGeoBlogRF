# 🗺️ Анализ фасада картографических компонентов

## 📋 Обзор архитектуры

Фасад карт (`mapFacade.ts`) — это единая точка входа для работы с картами в приложении. Он абстрагирует различия между различными провайдерами карт (Yandex Maps, Leaflet, MapLibre GL) и предоставляет унифицированный API.

### Основные принципы:
1. **Единый интерфейс** — все провайдеры реализуют `UnifiedMapMethods`
2. **Единый формат координат** — внутри приложения всегда `[lat, lng]`
3. **Адаптеры** — каждый провайдер имеет свой адаптер с конверсией координат
4. **Инкрементальные обновления** — метки и маршруты добавляются без переинициализации

---

## 🏗️ Структура фасада

### Класс `MapFacade`
Главный класс, управляющий провайдерами:

```typescript
class MapFacade {
  private currentProvider: MapProvider = 'yandex';
  private providers: Record<MapProvider, UnifiedMapMethods> = {
    yandex: new YandexAdapter(),
    leaflet: new LeafletAdapter(),
    mapsgl: new MapsGLAdapterStub()
  };
}
```

**Основные методы:**
- `initializeMap(container, config)` — инициализация карты
- `setCenter(coord)` — установка центра
- `addMarker(marker)` — добавление метки
- `drawRoute(route)` — отрисовка маршрута
- `clear()` — очистка карты
- `onClick(handler)` — подписка на клики
- `onRouteStats(handler)` — статистика маршрута
- `onRouteGeometry(handler)` — геометрия маршрута

---

## 🔌 Адаптеры провайдеров

### 1. YandexAdapter (основной)

**Особенности:**
- Использует Yandex Maps API 2.1
- Координаты: `[lat, lon]` (как в приложении)
- Поддержка MultiRoute для построения маршрутов по дорогам
- Автоматическая загрузка скрипта API при необходимости

**Ключевые функции:**
- `initialize()` — загружает API и создаёт карту
- `addMarker()` — создаёт Placemark с кастомными иконками
- `drawRoute()` — использует MultiRoute для построения маршрута по дорогам
- `onRouteGeometry()` — извлекает детальную геометрию маршрута

**Конверсия координат:**
```typescript
// Yandex использует [lat, lon] — конверсия не требуется
const toYandex = (coord: [number, number]) => [coord[0], coord[1]];
```

**Обработка маршрутов:**
- Использует `ymaps.multiRouter.MultiRoute` для построения маршрута по дорогам
- При успехе извлекает геометрию из сегментов маршрута
- При ошибке использует fallback — прямую линию через `ymaps.Polyline`

---

### 2. MapsGLAdapter (для постов и блогов)

**Особенности:**
- Использует MapLibre GL / Mapbox GL
- Координаты: `[lon, lat]` (конверсия требуется)
- Поддержка стилей: `opentopo`, `alidade`, `osm`
- Каплевидные маркеры с категориями

**Конверсия координат:**
```typescript
// MapsGL использует [lon, lat]
private toGL(coord: [number, number]): [number, number] {
  return [coord[1], coord[0]]; // [lat, lng] → [lng, lat]
}
```

**Стили карты:**
- `opentopo` — растровые тайлы OpenTopoMap
- `alidade` — векторный стиль Stadia Maps (требует API-ключ)
- `osm` — растровые тайлы OpenStreetMap

**Маркеры:**
- SVG каплевидные маркеры с градиентом
- Поддержка категорий через `getCategoryColor()` и `getFontAwesomeIconName()`
- Специальные маркеры для маршрутов: `routeMarker: 'start' | 'end'` (буквы A/B)
- Вертикальные смещения: `END_MARKER_Y_OFFSET = -44px` для конечного маркера

**Отрисовка маршрутов:**
- Использует GeoJSON Source + Line Layer
- Приоритет: `route_data.geometry` → `route.points`
- Слой маршрута перемещается ниже маркеров (z-index)

**События маркеров:**
- `marker-mouseenter` — показ мини-попапа
- `marker-mouseleave` — скрытие мини-попапа
- `marker-click` — открытие стандартного попапа

---

### 3. LeafletAdapter (заглушка)

**Статус:** Базовая реализация, не используется активно

**Особенности:**
- Использует Leaflet.js
- Координаты: `[lat, lng]` (как в приложении)
- Минимальная функциональность

---

## 📍 Работа с маркерами

### Интерфейс `MapMarker`

```typescript
interface MapMarker {
  id?: string;
  lat: number;        // широта
  lon: number;        // долгота
  title?: string;
  description?: string;
  category?: string;   // ключ категории (nature, restaurant, etc.)
  type?: 'blog' | 'post' | 'event' | 'route' | 'other';
  iconUrl?: string;
  iconSize?: [number, number];
  iconAnchor?: [number, number];
  routeMarker?: 'start' | 'end'; // для маркеров маршрута (A/B)
}
```

### Категории маркеров

Определены в `constants/markerCategories.ts`:
- `nature` — природа (зелёный)
- `restaurant` — ресторан (бордовый)
- `hotel` — отель (фиолетовый)
- `attraction` — достопримечательность (синий)
- `culture` — культура (жёлтый)
- `entertainment` — развлечения (оранжевый)
- `transport` — транспорт (бирюзовый)
- `shopping` — торговля (тёмно-оранжевый)
- `healthcare` — здравоохранение (красный)
- `education` — образование (синий)
- `service` — сервис (тёмно-серый)
- `event` — событие (фиолетовый)
- `blog` — блог (светло-зелёный)
- `route` — маршрут (оранжевый)
- `other` — другое (серый)
- `user_poi` — пользовательские (оранжево-коричневый)

**Функции:**
- `getCategoryColor(key)` — получить цвет категории
- `getFontAwesomeIconName(key)` — получить иконку FontAwesome
- `getMarkerIconPath(key)` — получить путь к иконке маркера

---

## 🛣️ Работа с маршрутами

### Интерфейс `Route`

```typescript
interface Route {
  id?: string;
  points: Array<[number, number]>; // [lat, lng]
  color?: string;
  route_data?: {
    geometry?: Array<[number, number]>; // сохранённая геометрия по дорогам
    points?: Array<[number, number]>;
  };
}
```

### Приоритет геометрии

**Правило выбора геометрии (single source of truth):**

1. **ПРИОРИТЕТ 1:** `route_data.geometry` (если существует и не прямая линия)
   - Это уже построенный маршрут по дорогам
   - Используется напрямую без перестроения

2. **ПРИОРИТЕТ 2:** `route.points` или `route_data.points`
   - Используется, если нет сохранённой geometry

3. **ПРИОРИТЕТ 3:** Перестроение через ORS API
   - Если geometry — прямая линия (2 точки или все точки на прямой)
   - Вызывается `getRoutePolyline()` из `routingService.ts`

### Определение "прямой линии"

```typescript
function isStraightLine(geometry: LatLng[]): boolean {
  if (geometry.length === 2) return true; // точно прямая
  if (geometry.length > 10) return false;  // уже маршрут по дорогам
  
  // Проверяем отклонения точек от прямой между началом и концом
  const THRESHOLD = 0.001; // ~100м
  // ... вычисление отклонений
}
```

### ORS Proxy (OpenRouteService)

**Расположение:** `frontend/src/services/routingService.ts`

**Особенности:**
- Всегда вызывает бэкенд-прокси на порту 3002
- Формат координат: `[lat, lng]` → конвертируется в `[lng, lat]` для ORS
- Кэширование запросов (24 часа TTL)
- Fallback на прямую линию при ошибках

**Эндпоинт:**
```
POST ${API_ORIGIN}/ors/v2/directions/{profile}/geojson
```

**Профили:**
- `driving-car` — автомобиль
- `foot-walking` — пешком
- `cycling-regular` — велосипед

---

## 🎨 Визуализация маркеров (MapsGL)

### Каплевидные маркеры

**Дизайн:**
- SVG форма капли с градиентом
- Верхняя часть — насыщенный цвет категории
- Нижняя часть — светлый оттенок того же цвета
- Тень через `feDropShadow`
- Прозрачный белый круг в центре
- Иконка FontAwesome или буква A/B для маршрутов

**Размеры:**
- Ширина: 34px (по умолчанию)
- Высота: 44px (по умолчанию)
- Якорь: `bottom` (остриё капли совпадает с координатой)

**Смещения:**
- `END_MARKER_Y_OFFSET = -44px` — поднимает конечный маркер на полную высоту
- `WAYPOINT_Y_OFFSET = -40px` — для промежуточных точек
- Стартовый маркер: `0` (без смещения)

**Z-index:**
- Маркеры: `100000` (выше всех слоёв)
- Маршруты: `1` (ниже маркеров)

---

## 📐 Позиционирование и слои

### Порядок слоёв (MapsGL)

1. **Базовые тайлы** (OpenTopoMap / OSM / Stadia)
2. **Маршруты** (Line Layer, z-index: 1)
3. **Маркеры** (Symbol Layer, z-index: 100000)

**Логика перемещения слоёв:**
```typescript
// Перемещаем слой маршрута ниже маркеров
const layers = this.map.getStyle().layers;
const markerLayer = layers.find(l => l.type === 'symbol');
if (markerLayer) {
  this.map.moveLayer(routeLayerId, markerLayer.id);
}
```

### Попапы маркеров

**Типы попапов:**
1. **Мини-попап** (`MiniMarkerPopup`) — при hover
2. **Стандартный попап** (`MarkerPopup`) — при клике

**Позиционирование:**
- Относительно DOM-элемента маркера
- Отступ: 2px от верхней точки маркера
- Автоматическая корректировка при выходе за границы карты

---

## 🔄 Интеграция с компонентами

### PostMap.tsx

**Использование:**
- Отображение маршрутов и маркеров в постах
- Провайдер: `mapsgl`
- Стиль: `opentopo` (по умолчанию)

**Особенности:**
- Использует `renderableGeometry` для определения финальной геометрии
- Проверяет, является ли геометрия прямой линией
- Перестраивает через ORS, если нужно
- Поддержка `anchors` (якоря) для маркеров

### BlogMap.tsx

**Использование:**
- Отображение маршрутов и маркеров в блогах
- Провайдер: `mapsgl`
- Аналогичная логика с `PostMap`

### Planner.tsx

**Использование:**
- Планировщик маршрутов
- Провайдер: `yandex` (по умолчанию)
- Интерактивное добавление меток и построение маршрутов

**Компонент `FacadeMap`:**
```typescript
const FacadeMap = React.memo(({ 
  onMapReady, 
  onMapClick, 
  markers, 
  routes,
  provider
}) => {
  // Инициализация один раз
  useEffect(() => {
    await mapFacade.initializeMap(container, config);
    mapFacade.onClick(onMapClick);
  }, [provider]);
  
  // Инкрементальное добавление меток
  useEffect(() => {
    for (let i = lastCount; i < markers.length; i++) {
      mapFacade.addMarker(markers[i]);
    }
  }, [markers]);
});
```

---

## 🎯 Ключевые особенности

### 1. Единый формат координат

**Внутри приложения:** `[lat, lng]` (широта, долгота)

**Конверсия на границах:**
- Yandex: `[lat, lon]` (без конверсии)
- MapsGL: `[lon, lat]` (конверсия через `toGL()` / `fromGL()`)
- ORS API: `[lon, lat]` (конверсия в `routingService.ts`)

### 2. Инкрементальные обновления

Метки и маршруты добавляются без переинициализации карты:
```typescript
// ✅ Правильно
mapFacade.addMarker(newMarker);

// ❌ Неправильно
mapFacade.initializeMap(container, { ...config, markers: [...markers, newMarker] });
```

### 3. Приоритет сохранённой геометрии

Если `route_data.geometry` существует и содержит много точек (>10), она используется напрямую без перестроения.

### 4. Обработка ошибок

- ORS API: fallback на прямую линию
- Yandex MultiRoute: fallback на `ymaps.Polyline`
- MapsGL: логирование ошибок, продолжение работы

### 5. Кэширование

- ORS запросы кэшируются на 24 часа
- Ключ кэша: `profile:${JSON.stringify(roundedCoords)}`

---

## 📝 Важные константы

### MapsGL маркеры

```typescript
const MARKER_HEIGHT = 44;              // высота маркера
const END_MARKER_Y_OFFSET = -44;       // смещение конечного маркера
const WAYPOINT_Y_OFFSET = -40;         // смещение промежуточных точек
```

### Попапы

```typescript
const MINI_POPUP_WIDTH = 140;
const MINI_POPUP_HEIGHT = 200;
const MINI_POPUP_PADDING = 10;
const MARKER_POPUP_GAP = 2;            // отступ между маркером и попапом
```

---

## 🔧 Настройка и конфигурация

### API ключи

Расположение: `frontend/src/config/api.ts`

```typescript
export const API_KEYS = {
  YANDEX_MAPS: import.meta.env.VITE_YANDEX_MAPS_API_KEY,
  STADIA_MAPS: import.meta.env.VITE_STADIA_MAPS_API_KEY,
  // ...
};
```

### Переменные окружения

```env
VITE_API_ORIGIN=http://localhost:3002
VITE_YANDEX_MAPS_API_KEY=your_key
VITE_STADIA_MAPS_API_KEY=your_key
```

---

## 🐛 Известные проблемы и ограничения

### 1. Два разных провайдера

- Основная карта (`/map`) использует Leaflet
- Посты/блоги используют MapsGL через фасад
- Планировщик использует Yandex через фасад

**Рекомендация:** Унифицировать на один провайдер (Yandex или MapsGL)

### 2. Бесконечные запросы API

В некоторых компонентах (`MiniMapMarker.tsx`) возможны циклы перерисовки.

**Решение:** Добавить мемоизацию и проверки загрузки

### 3. Конверсия координат

Требуется внимательность при работе с разными провайдерами:
- Yandex: `[lat, lon]`
- MapsGL: `[lon, lat]`
- ORS: `[lon, lat]`

---

## 📚 Дополнительные ресурсы

- `frontend/src/services/README-mapFacade.md` — краткое описание контракта
- `frontend/docs/map-facade-refactor.md` — план рефакторинга
- `MAP_ANALYSIS.md` — общий анализ карт в проекте
- `frontend/src/services/routeGeometryUtil.ts` — утилиты для геометрии
- `frontend/src/services/routingService.ts` — интеграция с ORS

---

## ✅ Чек-лист использования фасада

1. ✅ Инициализация карты один раз при монтировании
2. ✅ Использование единого формата координат `[lat, lng]`
3. ✅ Инкрементальное добавление меток/маршрутов
4. ✅ Приоритет сохранённой `route_data.geometry`
5. ✅ Обработка ошибок с fallback
6. ✅ Подписка на события через `onClick()`, `onRouteStats()`, `onRouteGeometry()`
7. ✅ Очистка через `clear()` при размонтировании

---

**Последнее обновление:** 2024
**Версия фасада:** 1.0


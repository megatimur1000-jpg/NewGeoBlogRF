1. В форме создания события вставь компонент карты
Используй существующий OpenStreetMap-компонент (map.tsx или аналог).
Отобрази его внутри формы, под полями «Название» и «Адрес».
Сделай его миниатюрным (высота 200–250 px), но кликабельным и функциональным.
2. Добавь поле поиска прямо над мини-картой
Подпись: «Поиск места»
Поле для ввода: позволяет писать адрес (например: Астраханская область, Харабали, посёлок Бугор)
При вводе — карта автоматически центрируется на найденном месте.
Используй геокодинг через Nominatim (OpenStreetMap) — он бесплатный и поддерживает русские адреса.
3. При выборе места — ставится метка
Как только пользователь ввёл адрес и нажал Enter (или выбрал из подсказок), карта:
Отображает одну метку в найденной точке
Сохраняет координаты (широта, долгота) как данные события
Заполняет поле «Адрес» полной строкой найденного места
4. Позволь поставить метку вручную
Если пользователь кликает прямо на карту — метка перемещается в эту точку.
При этом:
Координаты обновляются
Поле «Адрес» очищается или подставляется обратный геокодинг (если поддерживается)
5. Добавь кнопку «Указать точнее» (опционально)
Расположи под мини-картой: [📍 Указать точнее на карте]
При клике — открывается модальное окно с той же картой, но в полном размере
В нём:
Можно двигать метку
Можно масштабировать
Есть тот же поиск
Кнопка «Подтвердить» в модалке — закрывает окно и обновляет координаты в форме
6. Свяжи метку с событием напрямую
Метка не создаётся отдельно — она является гео-свойством события
При отправке формы — данные события включают:
название, дата, описание, координаты, адрес
После модерации — событие появляется на карте автоматически, как единый объект
7. Запрети создание метки без события
В этом компоненте нет кнопки «Добавить метку» как отдельный объект
Всё, что делает пользователь — привязывает событие к месту
Таким образом, нет дублирования, нет путаницы, нет «временных» меток
Итоговый принцип:
Пользователь не «ставит метку на карту».
Он «указывает, где происходит событие» — через карту.

Это — одна операция, один контекст, один результат.
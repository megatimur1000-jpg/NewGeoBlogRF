1. Где запускать запись трека? — На карте, через панель действий
📍 Место:
Компонент MapActionButtons (или аналогичная "стеклянная" панель управления на карте).

🛠 Что сделать:
Добавить кнопку «Записать трек» (иконка: 📡 или 🏃‍♂️).
Кнопка активна только если:
Пользователь — премиум (accessControlService.isPremium() → true),
Доступна геолокация (locationService.hasPermission() → true),
Приложение в online- или offline-режиме (трекинг должен работать даже без интернета!).
⚠️ Зачем:
Пользователь чаще всего хочет начать запись в момент перемещения — будучи на месте, на карте. Размещение на карте соответствует контексту использования и не требует переходов в меню.

🔹 2. Как реализовать цикл записи? — В MapContextFacade
📍 Место:
src/services/map/MapContextFacade.ts

🛠 Что сделать:
В startTracking():
Вызывать navigator.geolocation.watchPosition() с параметрами высокой точности.
Сохранять watchId и массив точек this.trackingPoints.
Фильтровать шум: игнорировать точки с accuracy > 50 м или смещением < 5 м от предыдущей.
В stopTracking():
Вызвать clearWatch().
Рассчитать distance (haversine), duration, bbox.
Сформировать объект TrackedRoute с source: "tracked", type: "route".
Сохранить в offlinePostsStorage как черновик маршрута с флагом isTracked: true.
Добавить в очередь синхронизации (offlineContentQueue) для отправки на сервер при появлении сети.
⚠️ Зачем:
Это обеспечивает единый точечный вход для работы с картой (через Map Facade), изолирует геолокационную логику от UI и гарантирует совместимость с офлайн-режимом и будущими картографическими провайдерами.

🔹 3. Где просматривать и управлять треками? — В личном кабинете → «Маршруты»
📍 Место:
Личный кабинет → вкладка «Маршруты» (/profile/routes)

🛠 Что сделать:
В списке маршрутов отображать все маршруты — и созданные вручную, и записанные как треки.
Визуально отличать треки:
Иконка: 📡 вместо 🗺️,
Подпись: «GPS-трек, 8.2 км»,
Метка: source: tracked.
В карточке трека добавить действия:
«Экспорт» → выбор формата (GeoJSON / GPX / KML),
«Добавить в пост» → открывает редактор поста с прикреплённым треком и меткой старта,
«Удалить» → удаляет из черновиков и очереди.
💡 Опционально: добавить фильтр «Только треки» / «Только планы».

⚠️ Зачем:
Трек — это разновидность маршрута, а не отдельный тип контента. Объединение в одном разделе упрощает навигацию, избегает фрагментации и соответствует пользовательской ментальной модели: «Я создаю маршруты — либо планирую, либо записываю».

🔹 4. Что делать с «Избранным»? — Только просмотр, не создание
📍 Место:
Меню «Избранное» (/favorites)

🛠 Что сделать:
Если пользователь добавляет трек в избранное (своего или чужого), он отображается как элемент типа «Маршрут» с пометкой «GPS-трек».
Не размещать кнопку «Записать трек» в избранном.
Не дублировать управление (экспорт, редактирование) — только просмотр и быстрый переход на карту.
⚠️ Зачем:
«Избранное» — библиотека сохранённого, а не инструмент авторства. Сохранение семантической чистоты интерфейса повышает юзабилити и предсказуемость.

🔹 5. Как интегрировать с постами и геймификацией?
📍 Место:
При завершении трека → модальное окно,
xpService.ts, dailyGoalsService.ts
🛠 Что сделать:
После stopTracking() показывать модалку:
«Трек записан (7.4 км).
✅ Создать пост
💾 Сохранить в черновики»

При создании поста с треком — автоматически привязывать track и метку старта.
Начислять XP:
+40 XP за первый GPS-трек,
+20 XP за трек > 5 км,
+10 XP за экспорт в GPX/KML (если в будущем будет отслеживаться).
Добавить достижения:
«Первый след» — первый трек,
«Пеший энтузиаст» — 50 км суммарно.
⚠️ Зачем:
Это стимулирует использование премиум-функции, связывает треки с основной активностью (посты), и укрепляет ценность подписки через геймификацию.

🔹 6. Экспорт в GPX/KML — реализация
📍 Место:
MapContextFacade.exportTrack(format)

🛠 Что сделать:
Реализовать сериализаторы:
toGPX(trackedRoute) → возвращает строку в формате GPX 1.1,
toKML(trackedRoute) → в формате KML 2.2.
При экспорте:
Генерировать Blob,
Инициировать скачивание через <a download="track.gpx">.
⚠️ Зачем:
Пользователи будут использовать треки в других навигаторах (OsmAnd, Locus, etc.). Поддержка стандартов — ключевая ценность премиум-функции.

🔹 7. Ограничения и безопасность
Трекинг заблокирован для непремиум-пользователей на уровне MapContextFacade.
В offline-режиме трек сохраняется локально, но отправляется на сервер при восстановлении связи.
Все треки проходят модерацию при публикации (как и посты).
✅ Итог
После реализации:

Пользователь записывает трек на карте одним кликом,
Сохраняет или публикует его как часть контента,
Управляет всеми маршрутами в одном месте — в личном кабинете,
Экспортирует в любимый навигатор,
Получает XP и достижения,
Не видит дублирующих кнопок и не путается в интерфейсе.
Это — грамотная, масштабируемая и пользовательски ясная интеграция GPS-треков, полностью соответствующая вашей архитектуре и стратегии.
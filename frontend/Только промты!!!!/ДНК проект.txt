Проанализируй мой проект целиком и создай в папке `DNA_Project/` полную семантическую архитектурную карту — "ДНК проекта". Это не просто документация, а **иерархическая модель влияния**, которая поможет принимать решения при разработке новых функций и избегать дублирования.

Требования к анализу:

1. **Определи глобальные ядра системы**  
   Найди файлы, функции или модули, которые:
   - Используются в ≥3 местах
   - Содержат логику, определяющую поведение других компонентов
   - Являются единственным источником истины для определённой области (например: авторизация, форматирование, состояние пользователя)

2. **Построй дерево влияния для каждого ядра**  
   Для каждого ядра создай структуру вида:

🌳 [путь к файлу] — [краткое название]
├─ Роль: [почему это ядро?]
├─ Зависимости: [кто использует этот модуль — перечисли пути]
├─ Расширяемость: [можно ли подключить адаптеры, плагины, параметры?]
├─ Дубликаты / устаревшие аналоги: [если есть — укажи и отметь как "подлежит удалению"]
└─ Рекомендации: [инкапсулировать? сделать глобальным? вынести в shared?]


3. **Создай реестр компонентов с весом**  
В файле `DNA_Project/component_registry.md` составь таблицу:
| Путь | Тип (ядро / утилита / UI / API и т.д.) | Влияние (High/Med/Low) | Зависимости | Дубликаты? | Рекомендация |
|------|----------------------------------------|------------------------|-------------|------------|--------------|

4. **Выяви нарушения архитектурных границ**  
Например:
- UI-компонент напрямую вызывает API без слоя сервиса
- Бизнес-логика размазана по компонентам
- Глобальное состояние используется там, где достаточно локального

5. **Сформируй список "глобальных точек расширения"**  
Где можно безопасно подключать новую логику, не нарушая целостность?  
Сохрани в `DNA_Project/extension_points.md`.

6. **Создай план очистки**  
В `DNA_Project/cleanup_plan.md` укажи:
- Файлы/функции, которые можно удалить (их логика уже покрыта другими)
- Модули, требующие рефакторинга (слишком много ответственности)
- Возможности для выноса в shared/utils

❗ Важно:
- Анализируй **только предоставленные файлы** — не выдумывай.
- Используй **реальные пути, названия функций, примеры вызовов**.
- Если часть проекта не загружена — отметь явно: "[Не хватает контекста: ...]".
- Сохраняй технический, нейтральный стиль — это документ для разработчиков.
- Все файлы должны быть готовы к коммиту в репозиторий.

Результат должен быть **практичным, действенным и систематизированным** — не описанием, а инструментом для принятия решений.
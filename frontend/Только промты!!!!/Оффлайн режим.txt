ользователь может полностью создать пост офлайн (текст + фото + трек), сохранить его локально, а при появлении интернета — отправить всё на сервер. После модерации пост отображается у всех пользователей корректно, включая изображения и трек.

🔧 ОСНОВНЫЕ КОМПОНЕНТЫ, КОТОРЫЕ НУЖНО РЕАЛИЗОВАТЬ
1. Хранилище офлайн-постов (на клиенте)
Технология: IndexedDB (не localStorage!)
Почему:
— Поддерживает бинарные данные (File/Blob)
— Нет ограничения в 5–10 МБ
— Асинхронный, не блокирует UI
Структура записи:
id — UUID (уникальный идентификатор поста)
createdAt — дата создания
text — текст поста
images — массив File-объектов (бинарные данные фото)
track — GeoJSON или массив координат (трек маршрута)
status — 'draft' (не отправлен) / 'uploading' / 'failed'
retries — количество попыток отправки (для устойчивости)
regionId — идентификатор региона (для привязки к контенту)
⚠️ Важно: не сохранять src, base64, URL — только оригинальные File/Blob.

2. Сохранение поста в офлайн-режиме
При создании поста:
Все выбранные изображения — хранить как File (из <input type="file"> или из Canvas через .toBlob())
Трек — сохранять как объект данных, а не как ссылку
Нажатие «Сохранить офлайн» → запись в IndexedDB со статусом 'draft'
Пользователь видит пост в разделе «Мои черновики» (локально)
3. Предпросмотр офлайн-поста
В UI черновиков:
Текст — отображается как есть
Изображения — через URL.createObjectURL(file) (временный URL, работает только в этом сеансе)
Трек — отображается на карте через ваш движок
Никаких путей вроде /@fs/... или base64 в DOM!
4. Механизм отправки при появлении интернета
Триггеры отправки:
Автоматически: при событии online (браузерное событие)
Вручную: кнопка «Отправить черновики»
Фоново: при открытии приложения (если есть неотправленные черновики)
Логика отправки:
Найти все записи в IndexedDB со статусом 'draft' или 'failed'
Для каждой записи:
Установить статус 'uploading'
Отправить метаданные поста (POST /api/offline-posts) → получить postId от сервера
Последовательно загрузить все изображения (POST /api/posts/{postId}/images, multipart/form-data)
Отправить трек (PUT /api/posts/{postId}/track, JSON)
При успешной отправке — удалить запись из IndexedDB
При ошибке — увеличить retries, установить статус 'failed', повторить позже (например, через 5 минут или при следующем online)
⚠️ Важно: не удалять черновик, пока всё не отправлено успешно.

5. Серверная часть (API)
Требуемые эндпоинты:
POST /api/offline-posts — создаёт заглушку поста в БД (без изображений), возвращает id
Поля: text, userId, regionId, trackMetadata (если нужно)
Статус: 'pending_images'
POST /api/posts/{id}/images — принимает файлы, сохраняет на диск/CDN, привязывает к посту
PUT /api/posts/{id}/track — принимает GeoJSON, сохраняет в БД
После загрузки всех частей — пост автоматически переводится в статус 'awaiting_moderation'
Хранение файлов:
Физически: в папке uploads/offline-posts/{postId}/
В БД: хранить относительные пути, например:
image_paths: ["/uploads/offline-posts/abc123/img_1.jpg", ...]
Отдавать через статический маршрут (например, express.static('uploads'))
6. Модерация и публикация
Админ видит пост в списке модерации с уже загруженными изображениями и треком
При одобрении:
Статус меняется на 'published'
Пост становится доступен всем
Все пользователи видят:
<img src="/uploads/offline-posts/abc123/img_1.jpg">
Трек на карте — из серверного GeoJSON
✅ Никаких зависимостей от клиента автора! Всё — на сервере.

7. Обработка ошибок и отказоустойчивость
Если интернет пропал во время отправки:
Не удалять черновик
Сохранить прогресс (например, какие изображения уже загружены — не обязательно, проще повторить всё)
Ограничение по размеру:
Предупреждать пользователя, если суммарный размер > 50 МБ (или вашего лимита)
Очистка:
Предусмотреть кнопку «Удалить черновик»
Автоочистка старых (>30 дней) неотправленных черновиков
8. UX-требования
В интерфейсе:
«Черновики (3)» — с иконкой облачка с крестом
У каждого черновика: кнопка «Отправить сейчас»
Индикатор отправки: «Отправляется…», «Ошибка — попробовать позже»
После успешной отправки — уведомление: «Пост отправлен на модерацию»
Пользователь не может редактировать черновик после начала отправки (или делает копию)
9. Безопасность
На сервере:
Проверять MIME-тип изображений (только image/jpeg, image/png)
Ограничивать размер файлов
Генерировать безопасные имена файлов (UUID)
Не исполнять загруженные файлы (раздавать как Content-Type: image/*)
Аутентификация:
Каждый запрос к /api/offline-posts требует валидный JWT (проверка userId)
Нельзя отправить пост от чужого имени
10. Тестирование сценариев
Обязательно протестировать:

Полный офлайн → создание → возврат домой → отправка
Отправка прервана (обрыв интернета) → повтор
Множество черновиков → отправка по очереди
Большой трек + много фото → ограничение по размеру
Модерация → публикация → просмотр другим пользователем
✅ ИТОГ: ЧТО НУЖНО СДЕЛАТЬ
Компонент
Что реализовать
Фронтенд
IndexedDB-хранилище, сохранение File/Blob, отправка очереди, UI черновиков
Сервер
API для офлайн-постов, загрузка файлов, управление статусами
Инфраструктура
Папка uploads/, статический раздающий маршрут
UX
Четкие состояния: «сохранено офлайн», «отправляется», «на модерации»
Надёжность
Повтор отправки, защита от потери данных, ограничение размера

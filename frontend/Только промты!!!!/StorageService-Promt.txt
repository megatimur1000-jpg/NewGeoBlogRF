 1. Создать универсальную обёртку (фасад) для работы с хранилищем
Сейчас вы используете localStorage напрямую — это работает в браузере, но ломает тесты в Node.js.
Вместо этого нужно создать один сервис — например, storageService — который будет:

В браузере использовать localStorage.
В тестах (и на сервере, если вдруг понадобится) — использовать обычный JavaScript-объект в памяти (in-memory storage).
Предоставлять единый интерфейс: getItem, setItem, removeItem.
Так код перестанет зависеть от конкретной платформы — и вы сможете тестировать его без моков.

🔹 2. Перестать использовать localStorage напрямую в компонентах и хуках
Во всём проекте замените все прямые вызовы localStorage.setItem(...) и подобные на вызовы через ваш storageService.

Это изолирует побочные эффекты и делает код чистым и тестируемым.

🔹 3. Настроить глобальный файл для тестов (setup-файл) — временно
Пока вы не перенесли всё на storageService, можно временно создать файл вроде tests/setupTests.ts, в котором:

Эмулируется localStorage для всех тестов.
Это нужно только как промежуточное решение, чтобы тесты не падали.
Потом, когда всё перейдёт на фасад — этот файл можно будет убрать или оставить только для совместимости.

🔹 4. Убедиться, что аналитика не ломает основной поток
Если вы сохраняете аналитику (например, последнее действие пользователя) в localStorage, убедитесь, что:

Это происходит асинхронно или хотя бы с защитой от ошибок (try/catch).
Сбой записи в хранилище не останавливает работу приложения.
Лучше всего — вынести вызовы аналитики за пределы критических путей (например, не в рендере, а в useEffect с обработкой ошибок).
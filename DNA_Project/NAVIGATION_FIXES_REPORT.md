# 🔧 Отчёт об исправлении навигации и инициализации карт

## 📋 Проблема
При переключении между страницами (Map.tsx ↔ Planner.tsx) возникала проблема:
1. ✅ Map → Planner: Работает
2. ✅ Planner → Map: Работает (метки загружаются с задержкой)
3. ❌ **Planner → Map → Planner: ЗАВИСАНИЕ!** Планировщик не инициализируется, требуется hard refresh

## 🎯 Корневые причины

### 1. Асинхронные обновления в contentStore (100% виновата)
**Файл:** `frontend/src/stores/contentStore.ts`

**Проблема:** Все методы обновления состояния оборачивались в `startTransition()` из React 18, что делало обновления **асинхронными и отложенными**.

```typescript
// БЫЛО (медленно):
setLeftContent: (content) => {
  startTransition(() => {
    set({ leftContent: content });
  });
}
// Результат: клик → задержка 1-2с → показ страницы
```

**Исправление:** Удалены все `startTransition()` обёртки. Теперь обновления синхронные:
```typescript
// СТАЛО (мгновенно):
setLeftContent: (content) => {
  set({ leftContent: content });
}
// Результат: клик → тут же показ страницы
```

**Затронутые методы (все 9):**
- ✅ `setLeftContent`
- ✅ `openLeftPanel`
- ✅ `closeLeftPanel`
- ✅ `toggleLeftPanel`
- ✅ `setRightContent`
- ✅ `openRightPanel`
- ✅ `closeRightPanel`
- ✅ `toggleRightPanel`
- ✅ `resetAllPanels`

---

### 2. Утечки памяти карт при переключении (вторая причина)
**Файлы:**
- `frontend/src/pages/Planner.tsx`
- `frontend/src/components/Map/Map.tsx`
- `frontend/src/services/projectManager.ts`

**Проблема:** При демонтировании компонентов карты **НЕ очищались из памяти**. Это приводило к:
- Накоплению инстансов карт в памяти
- Конфликтам при переинициализации
- Зависанию при повторном открытии Planner

**Исправления:**

#### a) projectManager.ts - добавлена очистка карт
```typescript
// Новый метод для уничтожения карты
destroyMap() {
  try {
    if (this.mapApi?.destroy) {
      this.mapApi.destroy();
    }
    this.mapApi = null;
  } catch (e) {
    console.warn('[ProjectManager] Error destroying map:', e);
  }
  this.mapInitialized = false;
}

// Улучшенная логика инициализации
async initializeMap(container, config) {
  // Если карта в ДРУГОМ контейнере - уничтожаем старую
  if (this.mapInitialized && this.mapContainer !== container) {
    this.destroyMap();  // ← КЛЮЧЕВАЯ СТРОКА
  }
  // Потом инициализируем новую
  this.mapApi = await mapFacade().initialize(container, config);
  this.mapInitialized = true;
}
```

#### b) Planner.tsx - cleanup при демонтировании
```typescript
useEffect(() => {
  // ... инициализация карты ...
  
  return () => {
    isMounted = false;
    console.log('[Planner] Unmounting, destroying map');
    try {
      projectManager.destroyMap();  // ← ОЧИСТКА
      setIsMapReady(false);
    } catch (e) {
      console.warn('[Planner] Error destroying map on unmount:', e);
    }
  };
}, []); // пустые зависимости - только при монтировании/демонтировании
```

#### c) Map.tsx (MapComponent) - cleanup при демонтировании
```typescript
useEffect(() => {
  // ... инициализация карты ...
  
  return () => {
    console.log('[MapComponent] Unmounting, destroying map');
    try {
      projectManager.destroyMap();  // ← ОЧИСТКА
    } catch (e) {
      console.warn('[MapComponent] Error destroying map on unmount:', e);
    }
  };
}, []); // пустые зависимости
```

---

## ✅ Что было исправлено

| Компонент | Проблема | Решение |
|-----------|----------|--------|
| **contentStore.ts** | startTransition → async обновления | Удалены все startTransition обёртки (9 методов) |
| **Planner.tsx** | Нет cleanup при демонтировании | Добавлен cleanup с projectManager.destroyMap() |
| **Map.tsx** | Нет cleanup при демонтировании | Добавлен cleanup с projectManager.destroyMap() |
| **projectManager.ts** | Нет логики переключения контейнеров | Добавлены destroyMap() и логика очистки в initializeMap() |

---

## 🧪 Как тестировать

### Тест 1: Быстрость отклика сайдбара
```
1. Откройте проект
2. Кликните на "Map" в сайдбаре
3. ⚡ Карта должна открыться **мгновенно** (не 1-2 сек)
4. Кликните на "Planner"
5. ⚡ Планировщик должен открыться **мгновенно**
```

### Тест 2: Повторное открытие Planner
```
1. Откройте проект
2. Кликните "Planner" → Плани должен загрузиться ✅
3. Кликните "Map" → Карта должна загрузиться ✅
4. Кликните "Planner" ещё раз → ⚡ Планировщик должен загрузиться (БЫЛ БАГ)
5. Кликните "Map" ещё раз → Карта должна загрузиться
```

### Тест 3: Двухоконный режим
```
1. Откройте Map
2. В сайдбаре выберите Posts (правая панель)
3. Карта должна сместиться влево, Posts справа ✅
4. Кликните Planner
5. Плани + Posts должны работать вместе ✅
```

### Консоль браузера (для подтверждения)
Ожидаемые логи при переключении:
```
[ProjectManager] Switching map container, destroying old map
[MapComponent] Unmounting, destroying map
[Planner] Unmounting, destroying map
[ProjectManager] Initializing map with provider: yandex
```

---

## 📊 Статус компиляции
✅ **Нет ошибок** - все файлы скомпилированы успешно

---

## 🚀 Следующие шаги

1. **Протестировать в браузере** - убедиться что:
   - Сайдбар отзывается мгновенно
   - Planner инициализируется со второй попытки
   - Консоль показывает логи уничтожения карт

2. **Проверить производительность** - убедиться что:
   - Нет утечек памяти (DevTools → Performance)
   - Нет дублирования инстансов карт

3. **Production deployment** - готово к выпуску

---

## 📝 Примечания

- **startTransition** полезна для больших обновлений UI, но вредна для **навигации/состояния**
- **Zustand** уже оптимизирован для прямых обновлений, не нужна React.startTransition
- **Cleanup функции в useEffect** обязательны при работе с глобальными ресурсами (карты, WebSockets и т.д.)

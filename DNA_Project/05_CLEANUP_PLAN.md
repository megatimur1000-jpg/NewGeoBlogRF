# 🧹 ПЛАН ОЧИСТКИ ПРОЕКТА

> **Дата анализа:** 22 января 2026  
> **Обновлено:** 22 января 2026 (v1.1)  
> **Проект:** Best_Site (Horizon Explorer)  
> **Цель:** Удаление дубликатов, рефакторинг монолитов, оптимизация архитектуры

---

## ✅ ВЫПОЛНЕННЫЕ ЗАДАЧИ

### ✅ 1. Удалены дубликаты mapFacade — ВЫПОЛНЕНО

**Удалено:**

| # | Путь | Результат |
|---|------|-----------|
| 1 | ~~`frontend/src/mapFacade.ts`~~ | ✅ Удалено |
| 2 | ~~`frontend/src/services/mapFacade.ts`~~ | ✅ Удалено |

**Оставлено (оригинал):**
- ✅ `frontend/src/services/map_facade/index.ts`

**Проверено:**
- ✅ Все импорты используют правильный путь
- ✅ Удалено ~492 строки кода

---

### ✅ 2. Проверено LayoutContext / contentStore — НЕ ПРОБЛЕМА

**Результат проверки:**
- ✅ LayoutContext правильно делегирует вызовы contentStore
- ✅ Нет дублирования, есть архитектурный паттерн (обертка)
- ✅ Оставлено как есть

---

## 🔴 ПРИОРИТЕТ 1: КРИТИЧНО (сделать далее)

### 🟡 Задача 1.3: Разбить Map.tsx компонент — ЧАСТИЧНО ВЫПОЛНЕНО

**Текущий статус:** ✅ Этап 1 завершён (22.01.2026)

**Что сделано:**
- ✅ Создан модуль `mapUtils.ts` (~170 строк)
- ✅ Вынесены вспомогательные функции:
  - getTileLayer()
  - getAdditionalLayers()
  - createLayerIndicator()
  - latLngToContainerPoint()
  - markerCategoryStyles
  - validateCoordinates()
- ✅ Map.tsx обновлен для использования mapUtils
- ✅ Сборка проверена — ошибок нет

**Размер файлов:**
- Map.tsx: 5353 → ~5180 строк (-173 строки) ✅
- mapUtils.ts: ~170 строк (новый) ✅

**Следующие шаги (опционально):**
Полный рефакторинг требует 4-5 дней и включает:

export const useLayoutState = () => {
  return useContentStore(); // Просто проксируем
};
```

**Затронутые файлы:**
- `frontend/src/components/Sidebar.tsx`
- `frontend/src/components/Topbar.tsx`
- `frontend/src/pages/*.tsx` (10+ файлов)

---

## ⚠️ ПРИОРИТЕТ 2: ВАЖНО (рефакторинг)

### 3. Разбить Map.tsx на модули (5353 строки!)

**Проблемный файл:**
- `frontend/src/components/Map/Map.tsx` (5353 строки)

**План разбиения:**

**Шаг 1: Создать отдельные файлы**

```typescript
// components/Map/MapRenderer.tsx (400 строк)
// Только отрисовка карты и базовая инициализация
export const MapRenderer: React.FC<MapRendererProps> = ({ mapRef, config }) => {
  // Логика инициализации Leaflet
};

// components/Map/MarkerManager.tsx (800 строк)
// Управление маркерами, кластеризация
export const useMarkerManager = (mapRef, markers) => {
  // Логика добавления/удаления маркеров
};

// components/Map/EventHandlers.tsx (500 строк)
// Обработка событий (клик, зум, перемещение)
export const useMapEvents = (mapRef, handlers) => {
  // Подписка на события
};

// components/Map/MapControls.tsx (300 строк)
// UI-контролы (кнопки, легенда, фильтры)
---

### 3. Разбить Map.tsx компонент (5353 строки → 5 модулей)
  return useMemo(() => {
    // Логика фильтрации (200 строк)
  }, [markers, filters]);
};

// hooks/useMapState.ts
export const useMapState = () => {
  // Управление состоянием карты (150 строк)
  const [center, setCenter] = useState(...);
  const [zoom, setZoom] = useState(...);
  // ...
  return { center, zoom, setCenter, setZoom };
};

// hooks/useMapActions.ts
export const useMapActions = () => {
  // Действия (добавление маркера, построение маршрута, и т.д.) (300 строк)
};

// pages/Map.tsx (упрощенная - 400 строк)
const MapPage = () => {
  const filters = useMapFilters(markers, appliedFilters);
  const { center, zoom, setCenter, setZoom } = useMapState();
  const actions = useMapActions();
  
  return (
    <MapContainer>
      <Map markers={filters} center={center} zoom={zoom} />
      <MapFiltersPanel filters={appliedFilters} onChange={...} />
      <MapActionButtons actions={actions} />
    </MapContainer>
  );
};
```

**Файлы для создания:**
1. `frontend/src/hooks/useMapFilters.ts`
2. `frontend/src/hooks/useMapState.ts`
3. `frontend/src/hooks/useMapActions.ts`

---

## 🟡 ПРИОРИТЕТ 3: ОПЦИОНАЛЬНО (оптимизация)

### 5. Объединить узкоспециализированные контексты

**Кандидаты на объединение:**

| Контекст | Использований | Решение |
|----------|---------------|---------|
| `SideContentContext` | 2-3 файла | Объединить с `contentStore` |
| `GuestContext` | 5 файлов | Объединить с `AuthContext` |

**Пример объединения GuestContext → AuthContext:**

```typescript
// contexts/AuthContext.tsx
interface AuthContextType {
  user: User | null;
  token: string | null;
  
  // ← Добавить гостевые данные
  guestData: {
    markers: MarkerData[];
    routes: RouteData[];
    events: EventData[];
  };
  
  // Методы
  login: (credentials) => Promise<void>;
  logout: () => void;
  
  // ← Гостевые методы
  saveGuestMarker: (marker: MarkerData) => void;
  clearGuestData: () => void;
}
```

---

### 6. Вынести типы в отдельные файлы

**Проблемный файл:**
- `frontend/src/types.ts` (смешаны все типы)

**План разделения:**

```bash
# Создать структуру
mkdir -p frontend/src/types

# Разделить типы
frontend/src/types/
  ├── marker.ts      # MarkerData, MarkerCategory, etc.
  ├── route.ts       # RouteData, RoutePoint, etc.
  ├── event.ts       # EventData, CalendarEvent, etc.
  ├── user.ts        # User, UserProfile, etc.
  ├── blog.ts        # Blog, Post, etc.
  └── index.ts       # Реэкспорт всех типов
```

**Пример:**

```typescript
// types/marker.ts
export interface MarkerData {
  id: string;
  latitude: number;
  longitude: number;
  title: string;
  // ...
}

export interface MarkerCategory {
  id: string;
  name: string;
  icon: string;
  color: string;
}

// types/index.ts
export * from './marker';
export * from './route';
export * from './event';
// ...
```

---

### 7. Удалить неиспользуемые утилиты

**Как найти:**

```bash
# Найти все экспорты
grep -r "export.*function\|export.*const" frontend/src/utils/ > exports.txt

# Для каждого экспорта проверить использование
grep -r "import.*functionName" frontend/src/ | wc -l

# Если результат = 0, то функция не используется
```

**Кандидаты на проверку:**
- `frontend/src/utils/` (все файлы)
- Старые хелперы, которые могли быть заменены

---

## 📊 СТАТИСТИКА ОЧИСТКИ

| Категория | Действий | Строк кода | Файлов |
|-----------|----------|------------|--------|
| **Удаление дубликатов** | 2 | -972 | -2 |
| **Рефакторинг Map.tsx** | 1 | -4000 | +5 |
| **Рефакторинг Map страницы** | 1 | -800 | +3 |
| **Объединение контекстов** | 2 | -200 | -2 |
| **Разделение типов** | 1 | 0 | +6 |
| **Удаление неиспользуемых** | ? | -? | -? |
| **ИТОГО** | **7+** | **-6000+** | **+10** |

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ

### 🔴 Критично (сделать в первую очередь)

- [ ] Удалить `frontend/src/mapFacade.ts`
- [ ] Удалить `frontend/src/services/mapFacade.ts`
- [ ] Обновить все импорты на `services/map_facade/index.ts`
- [ ] Устранить дублирование `LayoutContext` / `contentStore`
- [ ] Запустить тесты после удаления

### ⚠️ Важно (сделать во вторую очередь)

- [ ] Разбить `components/Map/Map.tsx` на 5 модулей
- [ ] Создать хуки для `pages/Map.tsx`
- [ ] Убедиться, что карта работает корректно

### 🟡 Опционально (сделать при наличии времени)

- [ ] Объединить `GuestContext` с `AuthContext`
- [ ] Разделить `types.ts` на домены
- [ ] Найти и удалить неиспользуемые утилиты

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ

### Неделя 1: Критичные задачи

**День 1-2: Удаление дубликатов mapFacade**
1. Создать ветку `cleanup/remove-mapfacade-duplicates`
2. Удалить файлы
3. Обновить импорты
4. Тестирование
5. Merge в main

**День 3-4: Устранение дублирования LayoutContext**
1. Создать ветку `cleanup/merge-layout-context`
2. Выбрать решение (A или B)
3. Обновить все использования
4. Тестирование
5. Merge в main

**День 5: Тестирование и проверка**
- Запустить все тесты
- Ручное тестирование UI
- Проверить производительность

---

### Неделя 2: Рефакторинг Map

**День 1-3: Разбить Map.tsx компонент**
1. Создать ветку `refactor/split-map-component`
2. Создать 5 новых файлов
3. Перенести логику
4. Обновить главный компонент
5. Тестирование
6. Merge в main

**День 4-5: Упростить Map.tsx страницу**
1. Создать ветку `refactor/simplify-map-page`
2. Создать 3 хука
3. Упростить страницу
4. Тестирование
5. Merge в main

---

### Неделя 3: Оптимизация (опционально)

**День 1-2: Объединить контексты**
- `GuestContext` → `AuthContext`
- `SideContentContext` → `contentStore`

**День 3-4: Разделить типы**
- Создать структуру `types/`
- Перенести типы
- Обновить импорты

**День 5: Cleanup**
- Найти неиспользуемые функции
- Удалить мертвый код
- Финальное тестирование

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После очистки:

| Метрика | До | После | Улучшение |
|---------|----|----|-----------|
| **Строк кода** | ~15000 | ~9000 | -40% |
| **Файлов** | 100+ | 110+ | +10 файлов (модули) |
| **Дубликатов** | 3 | 0 | -100% |
| **Макс. размер файла** | 5353 строки | ~800 строк | -85% |
| **Время компиляции** | ? | ? | Улучшится |
| **Читаемость** | Низкая | Высокая | ✅ |

---

## ⚠️ РИСКИ И ПРЕДОСТОРОЖНОСТИ

### Риски:

1. **Поломка импортов** при удалении mapFacade
   - **Решение:** Использовать grep для поиска всех использований

2. **Регрессия функциональности** при разбиении Map.tsx
   - **Решение:** Тщательное тестирование каждого модуля

3. **Конфликты при слиянии** веток
   - **Решение:** Делать маленькие PR, часто мержить

### Предосторожности:

- ✅ Создавать отдельные ветки для каждой задачи
- ✅ Писать тесты перед рефакторингом
- ✅ Делать backup перед большими изменениями
- ✅ Проверять производительность после изменений

---

## 🎯 КРИТЕРИИ УСПЕХА

### Задача считается выполненной, если:

- ✅ Все тесты проходят
- ✅ Нет ошибок в консоли браузера
- ✅ UI работает как раньше
- ✅ Код читается лучше
- ✅ Нет дубликатов
- ✅ Размер файлов < 1000 строк

---

**Следующий шаг:** Начать с Приоритета 1 (удаление дубликатов mapFacade)

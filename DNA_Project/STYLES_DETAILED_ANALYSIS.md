# 🎨 ДЕТАЛЬНЫЙ АНАЛИЗ ПРОБЛЕМНЫХ СТИЛЕЙ

> **Дата:** 22 января 2026  
> **Версия:** 1.1 (углубленная диагностика)

---

## 🔍 НАЙДЕННЫЕ КОНКРЕТНЫЕ ПРОБЛЕМЫ

### 🔴 ПРОБЛЕМА 1: Дублирование переменной `--glass-bg`
**Файл:** [frontend/src/index.css](../frontend/src/index.css)

```css
/* Строка 66 */
--glass-bg: rgba(255, 255, 255, 0.15); /* Более прозрачный */

/* Строка 108 - ПЕРЕКРЫВАЕТ первое значение! */
--glass-bg: rgba(255, 255, 255, 0.1);
```

**Риск:** Неявное поведение. Второе значение перекрывает первое, сбивая дизайн.

### 🔴 ПРОБЛЕМА 2: Тройное определение `.page-main-container`
Класс определен в **3 файлах**:

1. [frontend/src/styles/GlobalStyles.css:183](../frontend/src/styles/GlobalStyles.css)
2. [frontend/src/styles/EmbossedStyles.css:22](../frontend/src/styles/EmbossedStyles.css)  
3. Десятки вариантов `.page-container` в [frontend/src/styles/PageLayout.css](../frontend/src/styles/PageLayout.css)

### 🔴 ПРОБЛЕМА 3: Inline-стили в критических местах
**Map.tsx** содержит 200+ inline-стилей. Примеры:

```tsx
// frontend/src/components/Map/Map.tsx:2296
<div style={{ position: 'absolute', left: '50%', top: '50%', zIndex: 2000, transform: 'translate(-50%, -50%)' }}>

// frontend/src/components/Map/Map.tsx:2309
style={{
  position: 'absolute',
  right: '20px',
  bottom: '20px',
  zIndex: 1000,
  display: 'flex',
  flexDirection: 'column',
  gap: '12px',
  pointerEvents: 'none'
}}
```

### 🔴 ПРОБЛЕМА 4: Конфликтующие styled-components
Map компонент смешивает 3 системы:
- Tailwind классы
- styled-components (`MapMessage = styled.div`)
- inline styles (`style={{ ... }}`)

---

## 💡 ПРЕДЛАГАЕМОЕ РЕШЕНИЕ

### Этап 1: Создать единые токены
Создадим файл: `frontend/src/styles/design-tokens.css`

```css
/* === ЕДИНЫЕ ДИЗАЙН-ТОКЕНЫ === */
:root {
  /* Glass система - ЕДИНЫЕ значения */
  --glass-bg: rgba(255, 255, 255, 0.12);
  --glass-bg-hover: rgba(255, 255, 255, 0.18);
  --glass-bg-modal: rgba(255, 255, 255, 0.25);
  --glass-border: rgba(255, 255, 255, 0.2);
  --glass-blur: blur(10px) saturate(180%);
  --glass-radius: 16px;
  --glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);

  /* Контейнеры - ЕДИНЫЕ значения */
  --container-bg: rgba(255, 255, 255, 0.95);
  --container-border: rgba(255, 255, 255, 0.3);
  --container-radius: 20px;
  --container-shadow: 
    0 2px 8px rgba(255, 255, 255, 0.3),
    0 8px 32px rgba(0, 0, 0, 0.1),
    0 16px 64px rgba(0, 0, 0, 0.05);

  /* Z-индексы - ЕДИНЫЕ значения */
  --z-mini-popup: 1200;
  --z-modal: 2000;
  --z-tooltip: 3000;

  /* Positioning - ЕДИНЫЕ значения */
  --nav-height: 64px;
  --action-buttons-height: 106px;
}
```

### Этап 2: Унифицировать контейнеры
Создать: `frontend/src/styles/unified-containers.css`

```css
/* === ЕДИНЫЕ КОНТЕЙНЕРЫ === */
.page-main-container,
.page-container,
.map-container,
.embossed-container {
  background: var(--container-bg);
  backdrop-filter: var(--glass-blur);
  border-radius: var(--container-radius);
  box-shadow: var(--container-shadow);
  border: 1px solid var(--container-border);
  overflow: hidden;
}
```

### Этап 3: Заменить inline стили на утилиты
Создать: `frontend/src/styles/positioning-utils.css`

```css
/* === УТИЛИТЫ ДЛЯ ПОЗИЦИОНИРОВАНИЯ === */
.absolute-center {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}

.absolute-bottom-right {
  position: absolute;
  right: 20px;
  bottom: 20px;
}

.z-mini-popup { z-index: var(--z-mini-popup); }
.z-modal { z-index: var(--z-modal); }
.z-tooltip { z-index: var(--z-tooltip); }
```

### Этап 4: Обновить Tailwind config
```javascript
// frontend/tailwind.config.js
module.exports = {
  theme: {
    extend: {
      // Убрать дубли height и boxShadow
      zIndex: {
        'mini-popup': 'var(--z-mini-popup)',
        'modal': 'var(--z-modal)',
        'tooltip': 'var(--z-tooltip)',
      }
    }
  }
}
```

---

## 📊 ПЛАН МИГРАЦИИ (БЕЗОПАСНЫЙ)

### Неделя 1: Токены
1. Создать `design-tokens.css`
2. Подключить в `index.css`
3. НЕ менять существующие файлы

### Неделя 2: Контейнеры  
1. Создать `unified-containers.css`
2. Постепенно заменить классы в 2-3 компонентах
3. Скриншотные тесты

### Неделя 3: Inline → утилиты
1. Начать с Map.tsx (самый критичный)
2. Заменить 20-30 inline стилей на утилиты
3. Проверить визуальную идентичность

### Неделя 4: Финализация
1. Удалить дубли переменных
2. Удалить неиспользуемые CSS правила
3. Документировать новую систему

---

## ⚠️ РИСКИ И СМЯГЧЕНИЕ

### Высокие риски:
- **Map.tsx сломается** → Начинаем с 5 стилей, не с 200
- **Визуал поплывет** → Feature flag для новых стилей
- **Время разработки** → Только критичные места

### Смягчение:
- Скриншотные тесты до/после
- Rollback план (git реверт)
- Постепенная миграция по 1 компоненту

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

### После унификации:
- ✅ Единый источник правды для стилей
- ✅ Предсказуемое поведение
- ✅ Простота изменений (1 место вместо 3)
- ✅ Уменьшение CSS на ~30%

**Готов к началу? Предлагаю начать с Этапа 1 — создать файл токенов.**

---

## 🚨 КРИТИЧЕСКАЯ ЗАДАЧА: Исправить отображение маркеров

**Проблема:** Маркеры не отображаются или скрыты за контентом posts.tsx в двухпанельном режиме.

**План исправления:**
1. Проверить логику фильтрации маркеров по регионам
2. Исправить центрирование карты в левой части экрана (не по центру)
3. Настроить правильное отображение в двухпанельном режиме
4. Убедиться что маркеры загружаются корректно

---

**Следующий шаг:** Исправить отображение маркеров, затем создать `frontend/src/styles/design-tokens.css`
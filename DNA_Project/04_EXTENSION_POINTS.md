# 🔌 ТОЧКИ РАСШИРЕНИЯ СИСТЕМЫ

> **Дата анализа:** 22 января 2026  
> **Проект:** Best_Site (Horizon Explorer)  
> **Цель:** Определить безопасные места для добавления новой функциональности

---

## ✅ БЕЗОПАСНЫЕ ТОЧКИ РАСШИРЕНИЯ

### 1. 🗺️ Добавление новых картографических провайдеров

**Точка расширения:** `frontend/src/services/map_facade/adapters/`

**Как добавить новый провайдер:**

1. Создать новый адаптер, реализующий `IMapRenderer`:

```typescript
// frontend/src/services/map_facade/adapters/GoogleMapsRenderer.ts
import { IMapRenderer, MapConfig } from '../IMapRenderer';

export class GoogleMapsRenderer implements IMapRenderer {
  async initialize(container: HTMLElement, config: MapConfig): Promise<void> {
    // Инициализация Google Maps
  }
  
  async addMarker(marker: UnifiedMarker): Promise<void> {
    // Добавление маркера
  }
  
  // ... остальные методы IMapRenderer
}
```

2. Зарегистрировать в `MapContextFacade`:

```typescript
// frontend/src/services/mapFacade.ts
private switchToRenderer(context: MapContext): void {
  switch (context) {
    case 'osm':
      this.currentRenderer = new OSMMapRenderer(this.deps);
      break;
    case 'yandex':
      this.currentRenderer = new YandexPlannerRenderer(this.deps);
      break;
    case 'google': // ← НОВЫЙ ПРОВАЙДЕР
      this.currentRenderer = new GoogleMapsRenderer(this.deps);
      break;
  }
}
```

**Требования:**
- ✅ Реализовать интерфейс `IMapRenderer` полностью
- ✅ Не нарушать существующие адаптеры
- ✅ Добавить тесты для нового адаптера

---

### 2. 🎮 Добавление новых источников XP (геймификация)

**Точка расширения:** `frontend/src/constants/xpSources.ts`

**Как добавить новый источник XP:**

1. Добавить в конфиг `xpSources`:

```typescript
// frontend/src/constants/xpSources.ts
export const xpSources = {
  // ... существующие источники
  
  // ← НОВЫЙ ИСТОЧНИК
  'review_photo_added': {
    baseXP: 5,
    maxDaily: 20,
    requiresModeration: true,
    description: 'Добавление фото к отзыву'
  }
};
```

2. Использовать через `gamificationFacade`:

```typescript
// В компоненте или сервисе
await gamificationFacade.addXP({
  source: 'review_photo_added',
  userId: user.id,
  contentId: reviewId,
  metadata: { photoCount: 3 }
});
```

**Требования:**
- ✅ Указать `baseXP` (базовый опыт)
- ✅ Указать `maxDaily` (дневной лимит)
- ✅ Указать `requiresModeration` (нужна ли модерация)
- ✅ Добавить валидацию в `gamificationFacade`

---

### 3. 📍 Добавление новых категорий маркеров

**Точка расширения:** `frontend/src/constants/markerCategories.ts`

**Как добавить новую категорию:**

```typescript
// frontend/src/constants/markerCategories.ts
export const markerCategories = [
  // ... существующие категории
  
  // ← НОВАЯ КАТЕГОРИЯ
  {
    id: 'coworking',
    name: 'Коворкинг',
    icon: 'briefcase',
    color: '#10B981',
    description: 'Места для работы'
  }
];
```

**Требования:**
- ✅ Уникальный `id`
- ✅ Понятное `name`
- ✅ FontAwesome `icon`
- ✅ HEX `color`
- ✅ Добавить PNG иконку в `public/icons/markers/`

---

### 4. 🌍 Добавление новых регионов (не РФ)

**Точка расширения:** `frontend/src/stores/regionCities.ts`

**Как добавить новую страну:**

1. Создать новый файл:

```typescript
// frontend/src/data/worldCities.ts
export const worldCities = [
  {
    id: 'paris',
    name: 'Париж',
    country: 'Франция',
    coordinates: [48.8566, 2.3522],
    zoom: 12
  },
  // ...
];
```

2. Расширить `regionsStore` для поддержки стран:

```typescript
// frontend/src/stores/worldRegionsStore.ts
import { create } from 'zustand';
import { worldCities } from '../data/worldCities';

export const useWorldRegionsStore = create<WorldRegionsState>((set) => ({
  selectedCountries: [],
  selectedCities: [],
  // ... аналогично regionsStore
}));
```

**Требования:**
- ✅ Создать отдельный store для мировых регионов
- ✅ Не трогать `regionsStore` (он только для РФ)
- ✅ Использовать ту же структуру данных

---

### 5. 📊 Добавление новых типов аналитики

**Точка расширения:** `frontend/src/analytics/`

**Как добавить новую метрику:**

```typescript
// frontend/src/analytics/mapAnalytics.ts (новый файл)
export const trackMapAction = (action: string, metadata?: Record<string, any>) => {
  mapFacade().trackEvent({
    action,
    context: 'map',
    metadata
  });
};

// Использование
trackMapAction('marker_click', { markerId: '123', category: 'restaurant' });
```

**Требования:**
- ✅ Использовать `mapFacade().trackEvent()` для единообразия
- ✅ Добавить типизацию для событий
- ✅ Не дублировать существующую аналитику

---

### 6. 🔧 Добавление новых фильтров маркеров

**Точка расширения:** `frontend/src/hooks/useMarkerFilters.ts` (создать)

**Как добавить новый фильтр:**

1. Расширить интерфейс фильтров:

```typescript
// frontend/src/types/filters.ts
export interface MarkerFilters {
  categories: string[];
  radius: number;
  radiusOn: boolean;
  
  // ← НОВЫЕ ФИЛЬТРЫ
  rating?: number;      // Минимальный рейтинг
  hasPhotos?: boolean;  // Только с фото
  verified?: boolean;   // Только проверенные
}
```

2. Добавить логику фильтрации:

```typescript
// frontend/src/hooks/useMarkerFilters.ts
export const useMarkerFilters = (markers: MarkerData[], filters: MarkerFilters) => {
  return useMemo(() => {
    let result = markers;
    
    // Фильтр по рейтингу
    if (filters.rating) {
      result = result.filter(m => m.rating >= filters.rating!);
    }
    
    // Фильтр "только с фото"
    if (filters.hasPhotos) {
      result = result.filter(m => m.photo_urls && m.photo_urls.length > 0);
    }
    
    return result;
  }, [markers, filters]);
};
```

**Требования:**
- ✅ Добавить UI для нового фильтра в `MapFilters.tsx`
- ✅ Сохранять фильтры в `appliedFilters` state
- ✅ Не нарушать существующие фильтры

---

### 7. 🌐 Добавление новой локали (язык)

**Точка расширения:** `frontend/src/i18n/translations/`

**Как добавить новый язык:**

1. Создать файл переводов:

```json
// frontend/src/i18n/translations/de.json
{
  "map": {
    "title": "Karte",
    "search": "Suchen",
    "filters": "Filter"
  }
}
```

2. Зарегистрировать в i18n:

```typescript
// frontend/src/i18n/index.ts
import de from './translations/de.json';

i18n.use(initReactI18next).init({
  resources: {
    ru,
    en,
    de // ← НОВЫЙ ЯЗЫК
  }
});
```

**Требования:**
- ✅ Полное покрытие всех ключей (как в `ru.json`)
- ✅ Добавить флаг языка в UI
- ✅ Тестирование всех страниц

---

### 8. 📱 Добавление новых типов контента в панели

**Точка расширения:** `frontend/src/stores/contentStore.ts`

**Как добавить новый тип контента:**

1. Расширить `ContentType`:

```typescript
// frontend/src/stores/contentStore.ts
export type ContentType = 
  | 'map' 
  | 'planner' 
  | 'calendar' 
  | 'posts' 
  | 'chat' 
  | 'feed' 
  | 'friends' 
  | 'profile' 
  | 'test'
  | 'bookmarks' // ← НОВЫЙ ТИП
  | null;
```

2. Добавить компонент:

```tsx
// frontend/src/pages/Bookmarks.tsx
const Bookmarks: React.FC = () => {
  return <div>Закладки</div>;
};
export default Bookmarks;
```

3. Зарегистрировать в роутере:

```typescript
// frontend/src/routes.tsx
<Route path="bookmarks" element={<Bookmarks />} />
```

**Требования:**
- ✅ Добавить иконку в `Sidebar.tsx`
- ✅ Обновить логику открытия панелей
- ✅ Не нарушать существующие типы

---

## 🚫 МЕСТА, ГДЕ НЕЛЬЗЯ РАСШИРЯТЬ

### ❌ НЕ ТРОГАТЬ

| Файл | Почему нельзя трогать |
|------|------------------------|
| `services/projectManager.ts` | Singleton, изменение нарушит архитектуру |
| `stores/regionCities.ts` | Справочник РФ, полный и неизменный |
| `contexts/AuthContext.tsx` | Ядро авторизации, зависят все страницы |
| `services/api.ts` | Базовый API client, используется везде |

**Правило:** Если компонент используется в ≥10 местах - создавать обертку, а не изменять напрямую

---

## 📋 ЧЕКЛИСТ ПЕРЕД ДОБАВЛЕНИЕМ НОВОЙ ФУНКЦИИ

### ✅ Что проверить:

1. **Есть ли точка расширения?**
   - Да → использовать её
   - Нет → создать новый модуль

2. **Не нарушит ли это существующую функциональность?**
   - Запустить тесты
   - Проверить зависимости

3. **Нужен ли новый сервис/контекст/store?**
   - Если функция используется в ≥5 местах → Да
   - Иначе → локальный state

4. **Добавлена ли типизация?**
   - TypeScript interfaces/types
   - Валидация данных

5. **Обновлена ли документация?**
   - Обновить ДНК проекта
   - Добавить примеры использования

---

## 🎯 ПРИМЕРЫ БЕЗОПАСНЫХ РАСШИРЕНИЙ

### ✅ Хорошо

```typescript
// Добавление нового адаптера карты
class MapboxRenderer implements IMapRenderer {
  // Реализация интерфейса
}

// Добавление нового источника XP
xpSources['photo_with_description'] = {
  baseXP: 10,
  maxDaily: 30
};

// Добавление нового фильтра
const useMarkerFilters = (markers, { hasWifi, ...filters }) => {
  // Расширение существующей логики
};
```

### ❌ Плохо

```typescript
// Изменение projectManager напрямую
ProjectManager.getInstance().someNewMethod(); // НЕТ!

// Изменение regionCities
regionCities.push({ id: 'test' }); // НЕТ! Только чтение

// Изменение AuthContext
AuthContext.value.customLogic = ...; // НЕТ!
```

---

## 📊 СТАТИСТИКА ТОЧЕК РАСШИРЕНИЯ

| Область | Точек расширения | Сложность |
|---------|------------------|-----------|
| **Картографические провайдеры** | 1 | 🟡 Средняя |
| **Геймификация** | 1 | 🟢 Простая |
| **Категории маркеров** | 1 | 🟢 Простая |
| **Регионы** | 1 | 🟡 Средняя |
| **Аналитика** | 1 | 🟢 Простая |
| **Фильтры** | 1 | 🟢 Простая |
| **Локализация** | 1 | 🟢 Простая |
| **Типы контента** | 1 | 🟢 Простая |
| **ВСЕГО** | **8** | - |

---

**Следующий шаг:** План очистки проекта
